# OS / editor cruft — never publish these
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~

# Python bytecode cache (machine- and version-specific — never commit)
__pycache__/
*.py[cod]

# syntax-check throwaway (see CLAUDE.md "Commands")
_c.js

# local preview helpers
.tmp.driveupload/
