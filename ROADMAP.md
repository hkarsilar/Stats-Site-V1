# StatsCapybara — Powerhouse Roadmap

**Vision:** the site every statistics student gets sent to — and actually enjoys. Not just "learn a t-test," but the *entire journey of a research student*: design the study → collect and clean the data → run the right analysis → report it properly → do it all ethically. Every step interactive, correct, free, and calm.

This file is the master plan. The copy-paste prompts that execute it live in **[PROMPTS.md](PROMPTS.md)** — run them one per Claude Code session, roughly in the recommended order below. Both files live at the repo root, committed alongside CLAUDE.md, so they sync to every machine via GitHub Desktop.

---

## End state

| | At kickoff | Now (Jul 2026, post-P27) | Powerhouse |
|---|---|---|---|
| Courses | 4 (Stats 1–4) | 9 (Methods, Data, Ethics, ML, Writing all complete) | **9** in two tracks |
| Lessons | 45 | 95 | **~95** |
| Tool pages | 8 | 16 (+ power calc, APA formatter, datasets, flashcards, which-chart, planner, correlation, progress dashboard) | **~16** |
| Long-form guides | 0 | 0 | 4 cornerstone guides |
| Personas served | "stats course student" | + first-time researcher, data cleaner, ML-curious | + thesis writer |

**Two tracks on the homepage:**

- **The Statistics Core** — Stats 1–4 (exists, complete).
- **The Research Toolkit** (new) — everything around the statistics:
  - **Methods** · *Research Design* — 12 lessons (questions → designs → sampling → replication crisis)
  - **Data** · *From Raw to Ready* — 10 lessons (tidy data → cleaning → reshaping → privacy)
  - **Ethics** · *Responsible Research* — 8 lessons (consent → QRPs → AI ethics → fraud & self-correction)
  - **ML & AI** · *Machine Learning for Researchers* — 12 lessons (train/test → trees → clustering → LLMs)
  - **Writing** · *Reporting Your Research* — 8 lessons (IMRaD → APA numbers → figures → discussion)

**Suggested course accents** (verify visually in both themes before committing; violet `#8b5cf6` stays reserved for the checks block, orange `#f97316` for highlights): Methods amber `#f59e0b` · Data cyan `#06b6d4` · Ethics slate `#64748b` · ML purple `#a855f7` (shift toward fuchsia `#d946ef` if it reads too close to Stats-1 indigo) · Writing lime `#84cc16`.

---

## Guiding principles (non-negotiable)

1. **No build step, no frameworks, no external dependencies.** Plain HTML/CSS/JS + the two generated/injected assets (`search-index.js`, FAQ blocks). Everything in CLAUDE.md stays true.
2. **Statistics must be exact.** Every quantitative claim in prose, FAQ, or interactive is verified against published values (G*Power, tables, `node -e` cross-checks) before it ships.
3. **Frozen-noise interactives.** Sliders transform the *same* data; only explicit "New sample" buttons reseed. Round-trip test every slider.
4. **Calm, kind, capybara-tasteful.** No ads, ever — Ko-fi in the footer only. No dark patterns, no imposed quizzes, no red-alert UI. Serious topics (ethics history) get a serious tone.
5. **Each lesson stands alone** and is fully integrated: curriculum entry, checks, FAQs, quip, glossary, quiz question, snippet/software where applicable, sitemap, search index, per-page SEO.
6. **One prompt = one session = one reviewable diff.** Leave the working tree uncommitted; Hakan reviews and pushes via GitHub Desktop.

---

## Phases & prompts

| Phase | Prompts | What it delivers |
|---|---|---|
| **0 — Platform enablers** | P1–P3 | `tools/audit.js` health check · multi-track curriculum support · power & sample-size calculator |
| **1 — Methods course** | P4–P7 | 12 lessons, Research Design |
| **2 — Data course** | P8–P10 | 10 lessons, From Raw to Ready |
| **3 — Ethics course** | P11–P13 | 8 lessons, Responsible Research |
| **4 — ML & AI course** | P14–P17 | 12 lessons, ML for Researchers |
| **5 — Writing course** | P18–P20 | 8 lessons, Reporting Your Research |
| **6 — Tools expansion** | P21–P26 | APA formatter · practice datasets · flashcards · chart chooser · analysis planner · correlation explorer |
| **7 — UX & platform** | P27–P33 | progress dashboard + certificates · accessibility · performance · print handouts · per-course OG images · homepage v2 · offline PWA |
| **8 — Growth & SEO** | P34–P37 | cornerstone guides · printable cheat sheets · schema upgrade · Search-Console feedback loop |
| **9 — Maintenance loops** | P38–P39 | quarterly health audit · content refresh (recurring) |
| **10 — Human-voice edit** | P40–P46 | VOICE.md charter + `tools/prose-lint.js` · six de-AI rewrite batches (lessons, FAQs, guides, tools, homepage, meta descriptions) |
| **11 — Feel & instructors** | P47–P48 | interaction-feel polish pass · For Instructors page + lesson embed mode + viz PNG export |
| **12 — Requests & polish** | P49–P57 | lesson-interactive bug-fixes & control tweaks (Stats 1–4) · two dead toolkit interactives repaired · site-wide clickable-link affordance · Toolbox prominence + homepage declutter · "Capybara says" callout · new Psychometric Functions lesson |
| **13 — Requests round 2** | P58–P60 | Signal Detection Theory lesson (Stats 4) · non-parametric per-test logic + tabbed playgrounds · front door v2 (track-based nav dropdowns, grouped Toolbox menu, resume banner into hero) |
| **14 — Depth & reach** | P61–P68 | course landing pages · quiz exam mode + deeper bank · worked-problems library (Stats 1–2, then 3–4 + toolkit) · touch ergonomics · math regression gate + link checker · lesson presets for instructor embeds · trust touches |
| **15 — Footing** | P69–P72 | privacy page + honest claims + README rewrite · GA interaction events for the loops · search v2 (typo tolerance, glossary answers) · capstone worked-project guide |

**Recommended order** (content and tools interleaved so the site visibly improves every week):

> P1 → P2 → P3 → **Methods** P4–P7 → P21 (APA formatter) → **Writing** P18–P20 → P22 (datasets) → P25 (analysis planner) → **Data** P8–P10 → **Ethics** P11–P13 → **ML** P14–P17 → P23, P24, P26 → P27–P31 → P32 (homepage v2 — after several new courses exist) → P33 → P34–P36 → P37–P39 as recurring loops.

Hard dependencies: **P2 before any new course** (P4+). **P32 after at least two new courses.** Everything else can be cherry-picked.

**Post-buildout order (Phases 10–11, added 11 Jul 2026):** P40 first (it creates the standard and the measuring stick), then P41–P46 in any order but **P46 last** (it's the sitewide finisher), then P47 → P48.

---

## Phases 10–11 addendum (11 Jul 2026) — make it feel human

The build-out is done (P1–P36, with P37 waiting on real Search-Console data). The remaining risk isn't a missing feature — it's that the site *reads* like what it is: 95 lessons written by one AI in two weeks. The prose is accurate and warm, but its constructions repeat with machine regularity, and a reader who spots the pattern discounts everything else, statistics included. Measured on 11 Jul 2026 as the Phase-10 baseline:

- **Em-dashes:** median lesson page carries **24**, the worst **49** (min 13); `tools/faq_data.py` holds **272** more. A human editor uses a handful per page.
- **The self-answered verdict opener:** FAQ answers and prose beginning **"No — " / "Yes — "** ≈ **80×** sitewide.
- **The contrast punch** ("isn't X — it's Y" / "it's not about X — it's Y"): **21×**.
- **Stock moves:** "Notice how/that" 17× · "Here's the/why" 17× · "quietly" 37× · "The point is" 9× · "Think of it as", "the good news", rule-of-three lists everywhere.
- **Templated openers:** nearly every lesson's first paragraph rides the same em-dash-contrast rhythm ("Most of the pain in data analysis isn't the statistics — it's the shape of the spreadsheet…").
- **One meta-description formula:** 40 of 120 descriptions are built on "…do X **and watch** Y…".

Phase 10 (P40–P46) fixes this the way everything else on this site got fixed: a written standard (**VOICE.md**), a zero-dependency measuring stick (**`tools/prose-lint.js`**, so progress is a number, not a vibe), then six bounded editing passes that de-template the prose **without touching a single verified statistic, link, or interactive**. The goal is not to sand the personality off — quips, capybaras, and the playful chrome stay — it's variance: pages that read like they were written by the same person on different days, not by the same process in one pass.

Phase 11 (P47–P48) is the product-side counterpart: an interaction-feel pass (anchor-scroll offsets, canvas cursor affordances, unified copy-button feedback, animated `<details>` — the small tactile stuff that separates hand-made from generated), then the instructor play (a For Instructors page, `?embed=1` lesson embeds for slides/LMS, one-click PNG export of any viz) — because professors are the multiplier audience the human checklist already targets.

Phase 12 (P49–P57) is a batch of hand-picked fixes and upgrades from real use (Hakan's 16 Jul 2026 review of the live site): a run of lesson-interactive bugs and control tweaks across Stats 1–4 (P49–P52), two dead toolkit interactives repaired (P53), and four higher-blast-radius items — making in-prose links actually look clickable everywhere (P54), giving the Statistics Toolbox the prominence its growth earned and un-collapsing it on the homepage (P55), turning the flat "Why it matters" box into a branded "Capybara says" moment (P56), and a new specialized lesson on fitting psychometric functions / the PSE for psychophysics supervision (P57). These are independent and cherry-pickable; the two site-wide front-of-house items (P54, P55) have the highest impact-per-effort. **Each prompt in PROMPTS.md carries a recommended model and effort** — model: **Powerful** (Claude Sonnet, for well-scoped single-area work) vs **Extra Powerful** (Claude Opus, for site-wide / new-lesson / judgment-heavy work); effort: **High** (one focused pass) · **Extra** (elevated reasoning; multiple changes or light diagnosis) · **Max** (maximum single-agent reasoning + verification; high blast radius) · **Ultra** (multi-agent deployment — parallel build + independent verification/review).

Phase 13 (P58–P60) is the second requests round (Hakan's 17 Jul 2026 review). Two teaching-content upgrades: a **Signal Detection Theory** lesson appended to Stats 4 (the natural sibling of P57's psychometric functions, and the theory underneath the ML track's ROC lesson — the two must agree on AUC = Φ(d′/√2)), and a rebuild of Stats 2.6 so each non-parametric test gets its **logic explained and its own tabbed mini-playground** instead of a name-drop. Plus the front-door follow-through P55 started: **front door v2** — the nav reorganized around the homepage's own structure (Statistics Core / Research Toolkit / Statistics Toolbox, each a hover menu listing courses/grouped tools; the Curriculum and About tabs retired), the Toolbox dropdown grouped exactly like the toolbox page, the resume banner promoted into the hero with real presence, and the persona cards retired. Same model/effort legend as Phase 12. The two lesson prompts share registries (`curriculum.js`, `faq_data.py`, the search index) — run them one at a time.

---

## Phase 14 addendum (18 Jul 2026) — depth & reach

P1–P60 built the site out, made it sound human, and rebuilt the front door. A full-site review on 18 Jul found the growth surface has moved to three quieter axes:

- **Reach.** There is no course-level URL: `statscapybara.com/stats-1/` is a 404, the nav's course rows drop visitors into lesson 1.1 with no orientation, and there is nothing to put on a syllabus that says "this course" rather than "this lesson". Nine landing pages fix the 404s, give every course a shareable front door with its own OG card and Course JSON-LD, and print as a one-page syllabus (P61).
- **Depth.** For an exam-driven audience the assessment layer is thin: the checks, the quiz (~128 questions, instant-feedback only) and the flashcards all test *recognition*. Nothing on the site makes a student produce a number by hand and then shows the full worked path — the thing exams actually demand. Exam mode plus a deeper bank (P62) and a two-stage worked-problems library with instructor print-without-solutions handouts (P63–P64) close that.
- **Hardening.** `audit.js` checks the site's wiring exhaustively but not one line of its math — the core promise ("statistics must be exact") has no regression test — and external links were only hand-checked in quarterly P38 runs. **P66 (done)** shipped both guard scripts: `tools/math-check.js` (218 assertions on `viz.js`, under a second, a required gate whenever the math moves) and `tools/extlinks.js` (the P38 link leg, never a commit gate). The math passed clean on its first run — the site's statistics were already right; they're now held that way. **P65 (done)** covered the finger-first pass: all seven drag canvases declare `touch-action` (the suspected `ml/classification-metrics` offender turned out to have set it from JS since P15, so nothing actually scrolled mid-drag — it was normalised into the markup and given the missing `pointercancel`), grab targets widen to a finger pad on coarse pointers via `VIZ.grabRadius`, and no page scrolls sideways at 360px.

Plus the instructor multiplier P48 opened: preset lesson states so an embed can carry its exact configuration. **P67 (done)** shipped `SC.preset(map)` — an opt-in helper wired on eleven flagship lessons, with a `{ el, scale }` applier form so a URL speaks the units the lesson *prints* (`?z=1.96`, `?r=0.3`) while the helper snaps and clamps to the control's own step/min/max, and `teachers.html` gained a preset-carrying embed snippet plus the params table. Verified against the strongest available oracle: for all eleven lessons the preset state is **byte-identical to setting the same controls by hand**, under `?embed=1` and under the subpath server alike, with the round-trip law intact and every malformed param inert. Next: a small trust round — a feedback link in the footer, PWA shortcuts, section share-links (P68). Same model/effort legend as Phase 12. **P61, P67 and P68 all edit `site.js` — run them one at a time; P64 needs P63.** Everything else is independent.

---

## Phase 15 addendum (21 Jul 2026) — footing

P61–P68 landed (plus an ad-hoc curriculum reorder). A fresh full review found **no feature gap left worth building** — all three gates pass clean (audit 0 errors, math-check 218/218, prose-lint --strict 0) and the remaining work is footing, not surface:

- **Honesty.** GA4 runs on every page and is disclosed nowhere; `teachers.html`'s twitter:description still promises "no tracking". A calm privacy page, a quieter GA posture where it costs nothing, a sitewide claims sweep, and a README rewrite (the public repo still says 51 lessons / 5 courses) — **P69**.
- **Signal.** The recurring loops decide what to refresh with page views as their only input — no event says whether exam mode, PNG export, embeds, or the presets are ever used. A small, anonymous, taste-checked GA event set makes P37/P39 data-driven — **P70**.
- **Search.** Exact substring matching in a field where students type "hetroscedasticity". Typo tolerance, glossary instant answers, kind zero-result states — **P71**.
- **The keystone.** Every stop of "the entire journey of a research student" exists; no page walks the whole journey once. The fifth guide carries one shipped dataset from research question to APA paragraph to limitations, linking every course and tool where a student would actually reach for it — **P72**.

After P72 the roadmap IS the loops — P37 (still waiting on a GSC export), P38 quarterly (**its next run should sweep the P61–P68 surfaces**: landing pages, exam mode, the problems print modes, share-links, presets), P39 refresh — plus the human-only checklist, which is now the actual growth engine: distribution, not construction. One big build option deliberately NOT prompted: a **Turkish mirror** — real reach, but it roughly doubles a ~130-page maintenance surface; that's a strategic call for Hakan, not a session.

---

## The human-only checklist (no AI can do these)

- [ ] **Google Search Console** — verify statscapybara.com, submit `sitemap.xml`, check weekly. Do this first; it feeds P36/P37.
- [ ] **Bing Webmaster Tools** — same, 10 minutes.
- [ ] **Backlinks** — ask colleagues to link it from course pages/syllabi (.edu links are gold); submit to **MERLOT** and **OER Commons**; answer real questions on r/AskStatistics, r/statistics, Cross Validated with links to the *specific* interactive lesson.
- [ ] **One big launch post** when the Research Toolkit track is live — r/InternetIsBeautiful, Hacker News (Show HN), relevant teaching newsletters.
- [ ] **Tell your students** — real usage + feedback beats everything above.
- [ ] Skim GA4/GSC monthly; paste interesting query data into prompt P37.
- [ ] **A real photo in the About section** — replace the "HK" initials avatar; nothing says "a human made this" like a face. Consider adding one candid line to the bio about *why* a cognitive scientist built a capybara statistics site.
- [ ] **The read-aloud test** — after Phase 10 lands, read two or three rewritten lessons aloud (or better: have a colleague or student read them cold) and mark anything that still sounds generated; feed the marks into the next P39 refresh run.

---

## Status tracker

Tick these as sessions complete them (each prompt ends by updating this list).

> **Where things stand (11 Jul 2026):** 33 of 36 one-shot prompts done. Phase 0 complete; **all five toolkit courses (Methods, Data, Ethics, ML & AI, Writing) are complete** — the Writing course finished with 5.7 abstracts & titles and 5.8 the final checklist (accent lime `#84cc16`). Phase 6 tools underway: **P21 shipped `apa.html`, the APA Results Formatter** (t / ANOVA / χ² / correlation / regression → correct APA 7 sentence with a statcheck-style consistency check; also adds a BASE-aware "Format your own numbers →" link at the foot of every injected APA block), and **P22 shipped `datasets.html`, the Practice Datasets library** — 8 seeded, reproducible CSVs in `assets/data/` (t-test, one-way ANOVA, correlation+outlier, 2×2 factorial, Likert/reliability, messy-cleaning, wide/RM, logistic) generated by the committed `tools/make-datasets.py` (master seed `20260709`, byte-identical on rerun), each with a story, variable table, 3 lesson-linked exercises and a worked solution whose numbers are computed from the shipped file; cross-linked from `descriptives.html`. **P23 shipped `flashcards.html`, Glossary Flashcards** — a 3-box Leitner spaced-repetition tool (`sc-cards` localStorage) over the glossary, with a flip card, Again/Good/Easy rating (1/2/3 keys), a course filter, box tallies + due count, and a reset; the same session also **refactored the glossary term array out of `glossary.html` into `assets/js/glossary-data.js` (`window.GLOSSARY`)** as the single source of truth both the glossary and flashcards read (identical glossary output; `build-search-index.py` updated to read the new file). **P24 shipped `which-chart.html`, the "Which Chart Should I Use?" chooser** — the visualization sibling of `which-test.html` reusing its `wt-*` decision-tree pattern (breadcrumbs, step counter, Back, start-over): a distribution/comparison/relationship/composition/change-over-time × data-type tree with 11 leaves (histogram, box plot(s), bar, horizontal bar, grouped/stacked bar, scatter, scatter+trend, line, heatmap/mosaic, resist-the-pie, table), each with a live theme-aware canvas thumbnail (`VIZ.fit`/`VIZ.onTheme` + `ResizeObserver`), when-to-use text, the classic mistake, a "consider instead" nudge, and Learn-more links to `stats-1/visualizing-data` + `writing/tables-and-figures`, plus a goal × data-type cheat-sheet table. **P25 shipped `plan.html`, the "Plan My Analysis" wizard** — which-test's grown-up sibling (same `wt-*` decision tree) that ends in a **printable analysis plan**: recommended test + why, assumptions-to-check with links, a **live sample-size** at 80% power / α = .05 from the *exact noncentral math copied from `power.html`* (verified d=0.5 two-sample → 64/group, r=.3 → 84, f=.25/k=3 → 159, χ² w=.3/df=1 → 88), an effect-size control (Cohen presets + a "no idea?" note), an APA write-up skeleton, and four deep links (lesson · its `#run-it` SPSS/JASP anchor · an `apa.html?a=&v=` preset · a prefilled `power.html?sc=&es=`); print CSS renders just the plan card with a capybara header + URL footer, and `?test=<slug>&d=/r=/f=/w=/es=&k=&df=` query-params let lessons deep-link a scenario. Enabling edits: `site.js` gives the injected SPSS/JASP box `id="run-it"` (+ async hash-scroll); `apa.html` accepts `?a=`/`?v=`, `power.html` accepts `?sc=`/`?es=`; "plan your analysis →" cross-links added to `which-test.html` and `power.html`. P20 added the **"thesis pathway"** cross-link block to `toolbox.html` (Methods → Data → Stats → Power → Writing → Ethics). **P26 shipped `correlation.html`, the "Correlation & Regression Calculator"** — a paste-two-columns companion to `descriptives.html` (same decimal-comma/header-tolerant parsing, also accepts a two-column block pasted into the X box): a hi-DPI, theme-aware scatterplot with the least-squares line + equation, Pearson *r* with its two-tailed *t*-test *p* and **Fisher-z 95% CI** (verified r=.5, n=30 → [.17, .73]), tie-corrected **Spearman ρ**, R², the slope with SE/*t*/95% CI, a residual mini-plot, and a **leave-one-out robustness readout** (r range across all single deletions + the most-influential point named, click any scatter point to see *r* without it — the culprit auto-highlights in orange). Two copy-ready APA sentences (correlation + regression slope) and a built-in **Anscombe's quartet** panel (four mini-canvases, identical r≈.82 / ŷ = 3.00 + 0.50x, click to load — verified) drive home "always look at your data." Cross-linked both ways with `stats-2/correlation` and `stats-2/simple-linear-regression`, and from `descriptives.html`. **P27 shipped `progress.html`, the "My Progress" dashboard** — a `noindex` personal-state page that reads the existing `sc-progress`/`sc-checks`/`sc-last` (+ `sc-cards`) localStorage to draw per-course completion rings (reusing a new `window.SC = { ring, capy }` export from `site.js`, so the ring/mascot drawing isn't duplicated), overall done/remaining totals, a Continue-where-you-left-off button, per-course best-check scores, a flashcards summary, and an honest empty state for new visitors — plus a per-course **certificate** unlocked at 100% completion (canvas, parchment A4-landscape, course-accent double border, the `capy()` mascot ported to canvas, typed name persisted in a new **`sc-name`** key, date + `statscapybara.com`; drawn at 2× for a crisp PNG download and a print path that shows only the certificate). A "My progress →" link sits beside the homepage resume banner; the page is registered in `TOOLBOX` (group `practice`), `SEARCH_PAGES`, the `build-search-index.py` page list, and `audit.js`'s `ROOT_PAGES` — but deliberately kept **out of `sitemap.xml`** (a noindexed URL doesn't belong in a sitemap). The site now has **9 courses / 95 lessons / 16 tools**, audit passing clean. (5.4–5.8 are conceptual writing lessons — no software.js/snippets entries, warn-only in the audit; the static homepage lesson count is 95.) **All course content (Phases 1–5) and all of Phase 6 — Tools expansion (P21–P26) are done; Phase 7 — UX & platform polish is now underway.** **P28 delivered the accessibility pass (WCAG 2.1 AA), almost entirely in the shared layer:** `site.js`'s `injectA11y()` now also makes every `.stat-row` an `aria-live="polite"` region (slider readouts announce), derives an `aria-label` for every `.control` slider/select from its visible `<label>` (the markup never wired `for`/`aria-label`), and builds richer canvas `aria-label`s from `.viz-title` + `.viz-sub`; the **search overlay became a focus-trapped `aria-modal` dialog** that restores focus to its opener and closes on Escape (Escape now also closes the mobile menu, returning focus to the hamburger); toggle `.seg`s got `aria-pressed`. **Contrast:** `--text-faint` was retuned per-theme to clear 4.5:1 on every surface (light `#5f6e86`, dark `#94a3b8` — the old values were ~2.5–3.9:1); mobile nav tap targets are now ≥44×44px. **Motion:** added `VIZ.reducedMotion()` + `site.js` `scrollBehavior()` so JS scrolls/animations can honour `prefers-reduced-motion` (CSS already neutralises transitions). **Keyboard:** the one genuinely drag-only viz (`stats-3/assumptions-of-regression`) and the 3-D rotate in `ml/dimensionality-reduction` are now focusable with **arrow-key control**, and `wireLessonKeys` no longer hijacks `←/→` while a canvas/field is focused; `quiz.html`'s answer feedback is a live region. An **honest accessibility statement** was added to the About section, and the a11y conventions are documented in CLAUDE.md. Audit passing clean. **P29 was a measured performance pass (before → after, worst-case mobile 721 px width):** the big find was **lesson CLS ≈ 1.03 → ≤ 0.08** (homepage & tool pages were already 0). Root causes fixed in the shared layer: (1) the JS-injected mobile sidebar rendered *above* the article and shoved all content down ~800 px — fixed with `order` so the article comes first and the sidebar fills in below (CSS-only, also better mobile UX); (2) canvases (fixed-height/fluid-width, sized late by `VIZ.fit`) painted at the 2:1 intrinsic default then shrank — fixed with `.viz canvas { aspect-ratio: var(--viz-ar, 3/1) }` reserving a near-final box (per-instance `--viz-ar` override available; the two 330–340 px canvases needed none); (3) the Inter font-swap reflow — fixed with a metric-matched `@font-face "Inter Fallback"` (local Arial + ascent/descent/size-adjust overrides) first in the `--font` stack, so the swap causes no reflow. Also: **`VIZ.fit` (and the 6 pre-VIZ local `fit()` copies) now cap `devicePixelRatio` at 2** (quadratic backing-store savings on 3× screens, no visible loss); a new **`VIZ.rafThrottle`** coalesces the `resize` + `ResizeObserver` double-redraw, wired into the canvas tool pages (`tables`/`power`/`distributions`/`effect-sizes`/`descriptives`/`correlation`); confirmed `search-index.js` stays lazy and GA stays async; **no build step added.** Conventions documented in CLAUDE.md; audit passing clean; `search-index.js` rebuilt byte-identical. **P30 shipped print handouts — every lesson now prints as a clean, professor-ready sheet, entirely from the shared layer:** one comprehensive `@media print` block in `styles.css` + `site.js`'s new `setupPrint()`. **Kept in print:** prose, callouts, formulas, the frozen `.viz` canvas + readouts, and the reference blocks (APA write-up, R/Python snippet). **Hidden:** all chrome (nav/sidebar/footer/Ko-fi/search/resume), the sidebar capybara **quip** (so Ethics lessons print with their serious tone intact — the consistent decision), the viz **controls** (sliders/buttons/`.seg` + the R/Python language toggle), and the two interactive injected blocks (`.checks` + the SPSS/JASP `.software` tabs). The print block also **redefines the surface/text/border/shadow CSS vars to a light palette for both `:root` and `html[data-theme="dark"]`**, fixing a latent bug where a dark-mode reader printed dark cards. `setupPrint()` (lesson pages) injects a **"Print this lesson"** button beside mark-complete (`.print-btn`, hidden in print) + a discreet fixed **`.print-footer`** with the page's clean canonical URL per sheet, and on **`beforeprint`** force-opens the FAQ `<details>` (skipping the hidden `.checks`) and **snapshots each `.viz canvas` → `<img>`** (some browsers print `<canvas>` blank), reverting both on `afterprint` (double-fire-guarded, with a `matchMedia('print')` fallback for Safari). `formulas.html` inherits the block (kept its own print button); `glossary.html` got a matching print button. Verified via CSSOM `media.mediaText` flip + `beforeprint` dispatch + screenshot (preview has no native print emulation) on a rich lesson, an APA/software lesson, and dark mode; audit passing clean. **P31 shipped per-course Open Graph images** — a shared link now looks distinct per course instead of every page showing the same card. New committed, reproducible **`tools/make-og-images.py`** (Pillow) is the single source of truth: it writes the brand default **`assets/og.png`** (homepage + all 16 tool pages, evergreen subtitle, signature indigo→teal gradient) **plus one accent card per course, `assets/og-<slug>.png`** (1200×630, the course accent as the dominant colour via a dark-accent→accent gradient tuned so white text clears 4.5:1 even on the brightest accents lime/amber, a `THE STATISTICS CORE`/`THE RESEARCH TOOLKIT` eyebrow + course title + subtitle + `statscapybara.com`, and the same `capy()` cream capybara with a soft drop shadow so it lifts off any background). Titles/subtitles/accents are read straight from `curriculum.js` (with `var(--x)` accents resolved from `styles.css`) — nothing to hand-sync; fonts resolve from a portable Arial/DejaVu candidate list. The script's second job (`--retag-only`, no Pillow) repointed all **95 lessons'** `og:image`/`twitter:image` at their own course card (homepage/tools keep `og.png`); it's idempotent, so the adding-a-lesson flow just reruns it. `audit.js` was extended (CHECK 2/3 + a `siteAssetExists` helper) to enforce that every lesson's `og:image` = `twitter:image` = its course image **and** that the file resolves on disk (root pages' `og:image` must resolve too) — negative-tested against a wrong-course and a missing-file case. `search-index.js` rebuilt byte-identical (og:image isn't indexed; only its mtime needed refreshing after the retag). CLAUDE.md's OG-image section, the Commands block, and the adding-a-lesson/adding-a-COURSE checklists updated; audit passing clean (0 errors). **P32 shipped homepage v2** — the calm redesign for the two-track, 9-course reality: the hero keeps the capybara + live bell-curve demo but its CTAs are now **“Start with Stats 1”** (→ 1.1) and **“Plan my analysis”** (→ `plan.html`); three quiet **“Start here” persona cards** (`.personas`, static HTML above the grid) route “first stats course” → Stats 1.1, “writing my thesis” → a 5-stop Design→Power→Data→Writing→Ethics mini-trail, and “curious about ML & AI” → ML 4.1; each track heading gained a one-line **`desc`** (new field in `window.TRACKS`, rendered as `.track-desc`) and course cards now show a computed “· N lessons” beside the subtitle; the homepage toolbox went from all 16 cards to a **5-star strip** (new `star: true` flags in `TOOLBOX`: plan / tables / power / apa / datasets) plus a dashed “All 16 tools →” card whose count self-updates — `toolbox.html` still shows the full grouped grid; resume banner / search / GA / JSON-LD untouched (meta counts already correct at 9 courses · 95 lessons, audit-enforced). Verified in light + dark + mobile and on the subpath server (all persona/CTA/strip links resolve); search index rebuilt; audit passing clean (0 errors). **P33 shipped the offline PWA** — the site is now installable and works offline once visited, with no stale-content risk. **`site.webmanifest`** was rewritten to use only **relative** paths (`start_url`/`scope`/`id` = `"./"`, icons `assets/icon-*.png` incl. a `maskable` variant, `lang`/`categories` added) so it survives subpath hosting; `site.js`'s `injectHead()` now also emits **two media-scoped `theme-color` metas** (indigo light / `#0b1120` dark) and **registers the service worker** (`registerSW()` — `BASE + "sw.js"`, feature-detected, deferred to `load`, errors swallowed). New **`sw.js`** (site root, zero-dependency) precaches the app shell (`styles.css`, `site.js`, `curriculum.js`, `viz.js`, the font, favicons+icons, manifest, `offline.html`) into a **`CACHE_VERSION`**-named cache (`sc-v1`), serves **cache-first for `/assets/`** and **network-first for HTML navigations** (online readers never see a stale lesson; cache is fallback-only), with an **`offline.html`** fallback (self-contained like `404.html` — inline capybara, and it lists the pages this browser has cached via the Cache API). `activate` deletes non-current caches + `clients.claim()`s; `install` `skipWaiting()`s. A quiet **“Works offline once visited”** line was added to the homepage About section; CLAUDE.md documents the PWA + the `CACHE_VERSION`-bump deploy step. **Verified end-to-end in the preview** (this is where PWAs rot, so it was tested for real): registration + full precache, an **offline lesson load with the dev server stopped** (nav/sidebar/prose/font all from cache), the **offline.html fallback** for an uncached navigation, the **`CACHE_VERSION`-bump update path** (`sc-v1`→`sc-v2` → old cache purged on activate, then reverted to `sc-v1` for shipping), and correct **subpath scoping** (SW scope `…/Stats-Site-V1/`, `start_url` resolving to the subpath) on the `subpath` server — light + dark + mobile, zero console errors; `sw.js`/`offline.html` are self-contained (like `404.html`), so they're kept out of `audit.js`'s `ROOT_PAGES` and `sitemap.xml`. Audit passing clean (0 errors). **Phase 7 — UX & platform polish is now complete (P27–P33).** **P34 shipped the four cornerstone guides** — long-form (~2,000-word) search magnets at `guides/<slug>/index.html`, a new page type documented in CLAUDE.md: **`analyze-thesis-data-jasp`** (import → variable types → descriptives → assumptions → t-test → APA, worked end-to-end on the real `sleep-experiment.csv` — its t(78) = 2.02, p = .047, d = 0.45 and Levene F(1, 78) = 0.10, p = .758 recomputed from the shipped file), **`spss-output-to-apa`** (the five classic analyses as annotated mock SPSS output → the exact APA sentence, teaching the Levene two-row trap, the Robust-Tests/Welch table, df = N − 2, and the p = .000 ban; t-test/ANOVA/correlation numbers computed from the shipped practice CSVs — the ANOVA teaches Welch *because* study-methods.csv really fails Levene (p = .030) — chi-square/regression designed examples verified internally consistent via `node -e` + VIZ), **`choose-statistics-dissertation`** (the three-questions narrative decision tree, a 14-row test map with verified section numbers, honest sections on Likert/nested/mixed/missing messiness, prediction-vs-testing, and plan-before-you-collect), and **`clean-survey-data`** (the eight-step cleaning order worked on `wellbeing-survey.csv` — 51 missing cells, 75/120 complete cases, α = .90, composite M = 2.88/SD = 0.93, all recomputed from the shipped file). Platform: guides mark themselves with **`body[data-guide]`** (lesson-depth BASE from `site.js`, no sidebar/progress), carry Article + BreadcrumbList JSON-LD, a static `lesson-toc`, and a related-links footer; they're registered in `TOOLBOX` under a new **group `"read"`** ("📚 Read the guides" on toolbox.html; the homepage all-card now self-updates to "All 16 tools & 4 guides →"), `SEARCH_PAGES` (tag "Guide"), `QUIPS`, `build-search-index.py` (rerun — 20 pages), `sitemap.xml`, and a new **`GUIDES` array + CHECK 3b in `audit.js`** (SEO, JSON-LD types, data-guide marker, links, sitemap + search-index presence). `sw.js` `CACHE_VERSION` bumped `sc-v1` → **`sc-v2`** (site.js is a precached shell asset; update path re-verified in the preview — old cache purged). Audit passing clean (0 errors). **P35 shipped three printable one-page cheat-sheet posters** — the pin-it-up/hand-out assets, a new print-first page flavour (standalone HTML, self-contained inline poster CSS, zero deps): **`cheat-test-chooser.html`** ("Which Test? One-Page Chooser" — the which-test map as four decision grids: compare-means / rank-based swaps / relationships / predict-&-model, each row → test + lesson §, plus a "before you trust the answer" footnote block), **`cheat-apa.html`** ("APA 7 Statistics Reporting" — the report-exactly-like-this patterns for t/F/χ²/r/regression with correct typography [Latin italic via `<em>`, Greek η²/χ²/β upright], a worked one-sentence example matching the JASP guide's t(78)=2.02/p=.047/d=0.45, the italics list, the p-value rules, a decimals-&-leading-zeros table with green ✓/red ✗, and the **p = .000 ban** callout), and **`cheat-assumptions.html`** ("Assumption Checks" — per test family [t-tests & ANOVA · correlation & regression · chi-square]: assumption + how to check [Q–Q, Levene, residual plots, VIF, Mauchly] → the fix when violated [Welch / Greenhouse–Geisser / non-parametric / robust SEs / transform] + lesson §, plus an "order of operations" note). Each has a nice screen card (light + dark) with a big Print button, a tiny-capybara + URL footer, full per-page SEO (canonical/OG/Twitter targeting "statistics cheat sheet printable" etc.), and an **obsessive `@media print` block that genuinely fits ONE page** — verified by CSSOM-flipping the print media to screen and measuring: at A4 (192 mm wide) the posters are 177 / 162 / 152 mm tall (budget 279), at US Letter (198 mm) they're 175 / 158 / 145 mm (budget 261) — comfortably one page each, print chrome hidden, two-column poster layout. Integrated in **six places** (they're `group:"guide"` tool pages): `TOOLBOX` (now **19 tools**, homepage all-card self-updates to "All 19 tools & 4 guides →"), `SEARCH_PAGES` (tag "Reference"), `QUIPS`, `build-search-index.py` page list (rerun — 23 pages), `sitemap.xml`, and `audit.js`'s `ROOT_PAGES`; cross-linked from `formulas.html`, `which-test.html`, and the two relevant lessons (`stats-2/assumptions-and-when-they-break`, `writing/reporting-statistics-apa`). `sw.js` `CACHE_VERSION` bumped `sc-v2` → **`sc-v3`** (site.js is a precached shell asset). Verified in the preview (screen + dark + print, zero console errors); audit passing clean (0 errors). **P36 shipped the structured-data & metadata upgrade** — pure machine-readability, no visible content changes: the homepage's site-wide `Course` JSON-LD became **Organization (carrying the founder Person) + WebSite (the `?q=` SearchAction kept) + an ItemList of nine `Course` objects** (each: name `Title: Subtitle`, a `N interactive lessons — part of <track> track.` description, first-lesson URL, provider, free offer, `educationalLevel`), generated from `curriculum.js`; lesson `educationalLevel` was made sensible per track — stats-1 Beginner, stats-2 Intermediate, stats-3/4 Advanced, **all five toolkit courses Intermediate** (50 lesson heads retagged; `isPartOf` course naming verified consistent on all 95); `quiz.html` gained a minimal **Quiz** block (`about`: the nine courses) and **every tool page a BreadcrumbList** (Home → Statistics Toolbox → Tool; `toolbox.html` two levels); `audit.js` grew **CHECK 7** (per-type JSON-LD required-fields validation on every page — name/url, well-formed `itemListElement` ListItems, FAQPage `mainEntity` + `acceptedAnswer.text` — plus a deep homepage-ItemList↔curriculum match, per-lesson `isPartOf`/`educationalLevel` enforcement, and the BreadcrumbList/Quiz presence rules) and **CHECK 8** (meta hygiene: sitewide meta-description dedupe as an error, the `Thing — StatsCapybara` title pattern, root-page description lengths) — **negative-tested with 12 deliberate corruptions**, each raising the right error; the metadata review rewrote **24 over-length meta descriptions** to 50–160 chars (16 tool pages + 3 Ethics + 5 Writing lessons, with lessons' og/twitter/LearningResource copies kept in sync) and normalised `progress.html`'s title to the standard pattern. No GSC export was pasted, so P36's CTR-rewrite item waits for P37. Verified in the preview (homepage / quiz / lesson / cheat-poster JSON-LD all parse in-browser, zero console errors, homepage visually unchanged); search index rebuilt; audit passing clean (0 errors). Next up: the recurring loops P37–P39 (P37 needs a Google Search Console export — see the human-only checklist).

- [x] P1 · audit.js
- [x] P2 · multi-track platform
- [x] P3 · power calculator
- [x] P4 · Methods 1–3 (+ scaffold)
- [x] P5 · Methods 4–6
- [x] P6 · Methods 7–9
- [x] P7 · Methods 10–12
- [x] P8 · Data 1–3 (+ scaffold)
- [x] P9 · Data 4–6
- [x] P10 · Data 7–10
- [x] P11 · Ethics 1–3 (+ scaffold)
- [x] P12 · Ethics 4–6
- [x] P13 · Ethics 7–8
- [x] P14 · ML 1–3 (+ scaffold)
- [x] P15 · ML 4–6
- [x] P16 · ML 7–9
- [x] P17 · ML 10–12
- [x] P18 · Writing 1–3 (+ scaffold)
- [x] P19 · Writing 4–6
- [x] P20 · Writing 7–8
- [x] P21 · APA formatter tool
- [x] P22 · practice datasets library
- [x] P23 · glossary flashcards
- [x] P24 · which-chart tool
- [x] P25 · analysis planner wizard
- [x] P26 · correlation explorer
- [x] P27 · progress dashboard + certificates
- [x] P28 · accessibility pass
- [x] P29 · performance pass
- [x] P30 · print handouts
- [x] P31 · per-course OG images
- [x] P32 · homepage v2
- [x] P33 · offline PWA
- [x] P34 · cornerstone guides
- [x] P35 · cheat-sheet posters
- [x] P36 · structured-data upgrade
- [ ] P37 · GSC feedback loop (recurring)
- [ ] P38 · quarterly health audit (recurring)
  - **Run `node tools/extlinks.js` for the external-link leg** (P66) instead of hand-checking — it groups ok / bot-blocked-known-fine / broken and names the files each link sits in. Its `ALLOW` table holds the ko-fi 403 / LinkedIn 999 entries with a "last verified in a browser" date: **re-verify those two by hand each run and update the date**, since an allow-listed host is the one place a real break could hide. Run `node tools/math-check.js` too — it replaces the by-hand statistical spot-audit of the shared math (the per-lesson prose spot-check is still a human job).
  - **11 Jul 2026 — first run, site healthy.** audit.js 0 errors (21 warn = conceptual-lesson snippets, by design); 122 pages / 484 inline scripts + all JSON-LD + all shared JS syntax-clean; external links alive (LinkedIn 999 / ko-fi 403 are curl bot-blocks — button image 200, page verified fine); dependency purity clean (GA + Ko-fi only; CSS/manifest fully self-contained); localStorage schema consistent (sc-progress/sc-checks/sc-last write-paths only in site.js, progress.html read-only, sc-cards/flashcards, sc-name/certificate, no drift); statistical spot-audit passed — 5 random lessons (stats-1/independent-samples-t-test: FAQ power ns 26/64/394 per group + Welch APA sentence t(57.7)=2.30/p=.025/d=0.59/CI[0.6,8.8] all recomputed exactly; stats-4/bayesian-thinking Beta-posterior update textbook-correct; methods 1.1, writing 5.7, ethics 3.4 — Sweeney 87% figure correct) and both tool gates re-run live in-browser (tables.html z/t/χ²/F criticals match authoritative tables; power.html reproduces G*Power 64/group · r=.3→84 · f=.25,k=3→159 · w=.3→88 · inverse 80.1%); CLAUDE.md accurate except one stale TOOLBOX group list (fixed: +read); perf spot check CLS = 0 on all three page types at 721 px (P29 target ≤.1), loads <320 ms; browser pass homepage/lesson/tool light+dark+mobile zero console errors, SW scope correct. Also removed a stale P16-era git worktree (`.claude/worktrees/infallible-einstein-158707`, branch fully merged, nothing unique). **Deferred:** real Lighthouse runs remain a human-checklist item (preview-based CLS/timing used here).
  - **23 Jul 2026 — second run, site healthy; the only fixes were to the docs.** Gates: `audit.js` **0 errors, 21 warn, 64 info** (the warn/info baseline is unchanged — conceptual lessons with no snippet/software entry); `math-check.js` **270 assertions, 0 failures**; `prose-lint --strict` **exit 0**. Syntax: **137 HTML pages · 534 inline scripts · 235 JSON-LD blocks · 9 shared JS files, all clean** (JSON-LD parsed, not just tokenised). External links: `extlinks.js` finds **10 unique URLs — 8 ok, 2 bot-blocked-known-fine, 0 broken**; the P69 Consent-Mode doc link is new since the last run and resolves 200. **ALLOW re-verified by hand and re-dated:** ko-fi loaded in a real browser (Tip button + statscapybara.com link present), so the 403 is confirmed Cloudflare; LinkedIn's vanity URL resolves with no 404 but a logged-out browser hits the auth wall, so the entry now says so honestly and asks the next run to confirm the profile while signed in. Dependency purity clean — the **only** externally *loaded* resources are the GA tag and the Ko-fi button image; every other external URL is an ordinary hyperlink (Scholar, RUG, LinkedIn, JASP, Google's consent-mode doc). localStorage schema consistent, no drift: writers are `site.js` (theme/`sc-progress`/`sc-last`/`sc-checks`), `flashcards.html` (`sc-cards`), `quiz.html` (`sc-exam`), `progress.html` (`sc-name` only) — and `privacy.html`'s table lists all seven keys, so the published inventory is still complete. **Statistical spot-audit** — 5 random lessons across 5 courses: `stats-1/central-limit-theorem` (skewed population = Exponential(mean 16) clamped at 100, true mean **15.97**, so "about 16" is right), `stats-3/manova` (5 outcomes at α=.05 → **22.6%** ≈ "roughly 23%" ✓; the two-group Hotelling *T*² → *F* transform verified two independent ways — the T² route and the Wilks' Λ determinant route agree **exactly**; the ✨demo's claim is enforced by construction, p₁,p₂ > .15 and p_M < .001), `stats-4/bayesian-estimation` (Beta(19,13) posterior mean **.59375** and 90/95/99% credible intervals **[.450,.731] / [.422,.755] / [.369,.797]** — recomputed in Node and confirmed to 6 dp by independent Simpson integration, then read back off the live page identically), `stats-2/regression-diagnostics` and `ml/clustering-kmeans` (OLS and Lloyd's alternation both correct; k-means converges in 3 iterations and stays converged). Both tool gates re-run live in-browser: `power.html` reproduces **all four G*Power anchors exactly** (64/group · 84 · 159 · 88) plus 34 for paired/one-sample and both inverse directions (n=64 → 80.1%, → *d* = 0.50); `tables.html` matches printed tables (z ±1.960, t.975,10 = 2.228, t.975,28 = 2.048, χ².95,3 = 7.815, χ².95,10 = 18.307, F.95,3,20 = 3.098, F.95,1,20 = 4.351 = t².975,20) and the statcheck case t = 2.05, df = 28 → **.0498**. **P61–P68 sweep (what the last run flagged for this one) all pass:** course landing page = BASE `../`, no sidebar/prev-next/quip, ring + CTA + lesson list, Course+BreadcrumbList JSON-LD, og:type website; exam mode draws 10 unique questions without replacement, reveals nothing mid-run, writes `sc-exam` under the sorted signature `ethics+stats-1`, links every missed question to its course landing page, and a **retry-the-missed run is correctly not recorded**; `problems.html` print modes exact — "Print problems" hides all **43** solutions even the 2 the reader left open, "Print with solutions" opens all 43 with `::details-content` at full height, and the revert restores *exactly* the reader's own two; section share-links carry unique ids matching the mini-TOC, reveal on `:focus`, are in-flow at 1280px, cause **zero** layout shift on click, and are print-hidden; **all 11 preset lessons apply their params**, with garbage (`0x10`, `NaN`, `12abc`, unknown keys) silently ignored, out-of-range values clamped to each control's own min/max, and `?embed=1` composing cleanly (bare widget, one GA tag, `target="_top"` back-link to the absolute canonical). Search v2 (P71) spot-checked: "hetroscedasticity" and "bonferroni" return glossary instant answers, "chisquare" finds the lesson, "what is power" strips the question form, nonsense gets the honest empty state. Perf: **CLS = 0** on all six page types measured (home, lesson, tool, course landing, problems, guide); **24 pages checked at 360px — zero horizontal scroll, zero console errors**. **Fixed (docs only, no site file touched):** `CLAUDE.md`/`AGENTS.md` said **96** lessons (97), **19** tool pages (21 — `problems.html` and `progress.html` post-date the line), `PROMPTS.md` "P1–P39" (P1–P72), `problems.html` "22 problems, 12 Stats 1 + 10 Stats 2" (43 across five sets), and an "Adding a COURSE" step still saying to bump `95 hands-on lessons` (97); `AGENTS.md`'s mechanical Claude→Codex mirror had rewritten real filesystem paths to `.Codex/launch.json` / `.Codex/worktrees` / `.Codex/` — restored to `.claude/`, since those paths don't change with the reader. `audit.js` ↔ `prose-lint.js` `ROOT_PAGES` and `GUIDES` verified in sync (24/24 and 5/5) — the drift CLAUDE.md warns about has not happened. **Noted, not actioned:** (1) `stats-2/regression-diagnostics` re-randomises its cloud on every scenario click, so returning to "Healthy" gives a different *R*² (.86 → .81 → .77) — the math is exact and the lesson has no slider, so the frozen-noise law isn't violated as written, but a student comparing scenarios can't tell the scenario apart from the new sample; seeding shared x/noise pools would fix it. (2) **Five** stale agent worktrees under `.claude/worktrees/` (was one last run); four sit on commits already in main's history, one (`cool-shaw-e6dba5`) is on a detached commit that is **not** an ancestor of main, and three carry uncommitted edits — left alone for Hakan rather than deleted during a health pass. **Deferred:** real Lighthouse runs remain a human-checklist item; LinkedIn's profile render needs a signed-in check.
- [ ] P39 · content refresh loop (recurring)
  - **SUBJECT LEDGER — LAP 2 IS UNDER WAY. Lap 1 closed at run 20 with all 97 lessons taken as subjects; lap 2 re-reads the same pages along a THREAD rather than in staleness order, so this line tracks threads, not remaining lessons. Update it every run; do not re-derive it from git.** **Lap-2 threads taken so far: run 21 — the confidence interval as a reported object** (`apa.html`, `cheat-apa.html`, `writing/reporting-statistics-apa` §3.2, `stats-1/effect-size-and-power` §1.13, `stats-2/correlation` §2.8), which cleared the nine-run-old "`apa.html` emits no confidence interval"; **run 22 — the picture and what it licenses you to conclude** (`stats-1/visualizing-data` §1.4, `stats-2/assumptions-and-when-they-break` §2.5, `writing/tables-and-figures` §3.3, `stats-4/meta-analysis` §4.10, `which-chart.html`), five pages that each ask the reader to judge by eye and never calibrate the eye; it cleared `which-chart.html` as the weakest inbound target (2 → 7 lesson sources) and two of run 21's four standing items. **Standing inputs for the next lap-2 run.** P37 is still unrun, so there are no Search-Console queries yet. The FAQ opener census keeps drifting the right way (`faq-audit.js` prints it). **`cheat-assumptions.html` is now the weakest genuine teaching target** at 4 inbound prose links, with `distributions.html` and `glossary.html` beside it at 3 (both reached from the nav by design). Four things run 22 measured and left: **`which-test.html`'s decision-tree leaves, `quiz.html`'s question bank, `software.js`'s injected blocks and `apa.html`'s runtime strings are none of them search-index sources**, so a term living only there is unfindable by the site's own search (`rowMeanNaRm` is the current example; `search-reach.js` reports the class); **`Tukey HSD` still returns nothing** because the corpus writes `Tukey's HSD` and reaching it means skipping a letter, which `flexRe` refuses on purpose; **31 glossary terms are reachable only through the glossary page itself**, all of them phrase artifacts today (`Mann-Whitney U test` against a lesson that writes `Mann-Whitney`) but a useful tripwire; and **`metafor` is not installed on this machine**, so the one R line added to the meta-analysis snippet (`predict(res)`) was taken from the package's documented API rather than executed, unlike every other R line this run shipped. **Lap-2 thread taken by run 23 — the second test, what you run when the first one will not do** (`stats-2/one-way-anova` §2.1, `stats-2/post-hoc-tests` §2.2, `stats-2/repeated-measures-anova` §2.4, `stats-2/non-parametric-alternatives` §2.6, `cheat-assumptions.html`), which cleared `cheat-assumptions.html` as the weakest inbound target (3 → 8 lesson sources) and closed four prescribe-but-never-teach gaps (Welch's ANOVA, Games–Howell, Dunnett, Dunn, plus ε itself). **Standing inputs for the next lap-2 run.** P37 is still unrun. `distributions.html` and `glossary.html` are now the lowest inbound targets at 3, both nav-reached by design, so the next link-health leg should probably switch to the outbound question. Run 22's four measured-and-left items stand, with one added by run 23: **a bare Greek symbol is unsearchable** (`squash()` strips it), so a lesson that teaches a quantity only as `ε` or `Λ` is unreachable by the site's own search unless the name is spelled out. `AGENTS.md` is regenerated from `CLAUDE.md` — regenerate it whenever `CLAUDE.md` changes.
  - **29 Jul 2026 — twenty-third run (the second test, four fixes the site prescribed and never taught, and a checker for prescriptions the prose does not honor).** The ledger's standing input named `cheat-assumptions.html` as the weakest genuine teaching target, and the thread picked itself from that poster's own fix column: five pages that tell a reader what to run when the first test will not do, and stop before saying what it is. `stats-2/one-way-anova` (§2.1), `stats-2/post-hoc-tests` (§2.2), `stats-2/repeated-measures-anova` (§2.4), `stats-2/non-parametric-alternatives` (§2.6) and `cheat-assumptions.html`, the thread's hub. **Absences, each confirmed by a sitewide grep excluding `.claude/worktrees` and the generated index, with dash and apostrophe variants checked after the first grep gave a false zero on `Test–retest`.** (1) **`Welch's ANOVA` appeared in exactly two files and no lesson** — this poster's fix cell and a `snippets.js` comment — while `Brown–Forsythe` returned zero, on a course whose own practice file `study-methods.csv` is built with deliberately unequal group SDs. (2) **`Games–Howell` appeared only in `software.js`**, where an SPSS tip tells readers to click it, and **`Dunnett` returned zero sitewide**. (3) **`ε` and `epsilon` appeared in no prose anywhere**, while `problems.html` hands Problem 16 a `Greenhouse–Geisser ε = .71` as given data and `software.js`'s own APA tip says "name it and give ε". (4) **`Dunn` returned zero**, on a site that teaches Kruskal–Wallis across eleven files and never says what follows a significant one. **Every number was computed twice, in Node against `VIZ` and independently in R 4.5.2.** §2.1 gained **"When the spreads are not equal"**, worked on the site's own dataset: Levene *F*(2, 102) = 3.54, *p* = .033, ordinary *F*(2, 102) = 11.57 against Welch *F*(2, 66.43) = 13.10, both *p* < .001, the two agreeing because *n* is equal. The part the textbook warning omits was simulated at **100,000 samples per row**: at 20/20/20 with SDs 1/1/3 the ordinary *F* false-alarms **7.4%** against Welch's 5.2%; at 10/20/30 with SDs 3/2/1 it reaches **16.1%**; reverse the pairing and it falls to **2.1%**, which is the same failure pointing the other way. What Welch costs when the variances really are equal was measured rather than asserted: **about one point of power at 20 per group and three at 10** (40,000 reps). §2.2 gained **"Two cases Tukey does not cover"**: Games–Howell against Tukey on the same three groups (**.00014 vs .00004**, **.00032 vs .0017**, **.609 vs .560**), which disagree in *both* directions because the two tight groups no longer carry the noisy group's variance, and Dunnett's two-sided .05 cutoff at four groups and 20 error *df* — **2.54 against Bonferroni's 2.61 and Tukey's 2.80** — computed with `mvtnorm::qmvt` and matching the published Dunnett table exactly. §2.4 gained **"What ε is, and what it costs"**: the floor 1/(*k* − 1), and Problem 16's own *F* = 5.84 read four ways — **uncorrected (2, 22) *p* = .009, Huynh-Feldt ε = .785 → *p* = .016, Greenhouse-Geisser ε = .71 → *p* = .020, the floor ε = .50 → *p* = .034** — with the HF ε computed from the GG ε rather than quoted. §2.6 gained **"After a significant H"**: Dunn's formula, and a fifteen-value example (the numbers 1–15, so every value is its own rank) where **Dunn and pairwise Mann–Whitney disagree on the same data under the same Holm correction** — *H* = 10.5, *p* = .005; Dunn puts B against C at *z* = 2.12, *p* = .068, while three Mann–Whitney tests return *p* = .024, because every B beats every C in isolation and the two groups sit adjacent in the full ladder. **FAQ freshness: two swaps, two lessons deliberately left whole.** Out: §2.2's "Which post-hoc test should I use?" (**ECHO 100%**, every content word inside the lesson's own title, and its answer's Games–Howell advice is now a section), replaced by **"My design compares several treatments against one control. Do I still use Tukey?"** (ECHO 13%, REPRISE 4%), which carries the family arithmetic the section does not — ten Tukey pairs against four Dunnett comparisons at five groups — plus the timing constraint. Out: §2.4's "What do I do if sphericity is violated?" (**ECHO 50%, REPRISE 14%**, now answered in full by the new section), replaced by **"Mauchly's test came back non-significant. Does that settle it?"** (ECHO 0%, REPRISE 1%), which is the run's second simulation: at a true ε = .707 built from an AR(1) structure with growing variances, **Mauchly rejects 53% of the time at 12 participants, 82% at 20, 96% at 30**, while the damage stays flat at **6–7% false positives at every *n*** and Greenhouse-Geisser returns it to 5% each time. §2.1 and §2.6 kept all six of theirs. Still **291 Q&As, 291 unique question strings, 3 per lesson**. **software.js: rotated to four untouched by runs 20–22** — `repeated-measures-anova`, `non-parametric-alternatives`, `assumptions-and-when-they-break`, `independent-samples-t-test` — against **SPSS 32.0.x** (32.0.0.0 GA 23 Apr 2026, no fix pack found) and **JASP 0.98.1** (7 Jul 2026), both re-checked against the vendors' own pages rather than from memory; no menu renames since run 22. Three of the four reproduce exactly (Mauchly χ²(2) = 9.4 → *p* = .009 and *F*(1.56, 45.2) = 8.75 → *p* = .001 with ηp² = .23, which also pins the design at *n* = 30 and *k* = 3; Levene *F*(1, 58) = 8.42 → *p* = .005; the Welch *t*(57.7) = 2.30, *p* = .025, *d* = 0.59, CI [0.6, 8.8]). **One genuine defect, of the run-21 shape:** the Mann–Whitney sentence gave *U* = 245.5 with *z* = −2.13, *p* = .033 and *r* = .27, and **an exhaustive scan over every group-size pair from 5 to 60 found none that produces both** — the nearest, *n* = 18 and 42, is a lopsided design and still misses. It was rebuilt at 30 per group with *U* = 306, which reproduces **z = −2.13, *p* = .033** exactly, and now reports the **rank-biserial *r* = .32** the lesson teaches, with the *z*/√*N* value of .27 moved into the tip that already tells readers to say which one they computed. **Snippets: four entries extended, every R line executed here on R 4.5.2 exactly as shipped.** §2.1 gained `car::leveneTest` + `oneway.test(var.equal = FALSE)`; §2.2 a dependency-free **Games–Howell** built from `ptukey` with per-pair Welch df, which reproduces the section's published .609/.00032/.00014 exactly, plus a commented `multcomp::glht(..., mcp(group = "Dunnett"))` line **disclosed as taken from its documented API rather than run, since multcomp is not installed here**; §2.4 the base-R `car::Anova` route that prints Mauchly's *W*, both epsilons and both corrected *p*-values; §2.6 a base-R **Dunn's test** that returns the lesson's own −3.18/.0044, −1.06/.2888, 2.12/.0678. The three shipped R blocks were extracted from `snippets.js` and run verbatim. **Link health, measured.** The zeroes are navigation pages reached from the nav by design. `cheat-assumptions.html` was the weakest genuine target at **3** lesson/guide sources and gained five natural placements (§2.1, §2.2, §2.4, §3.8, §2.10) for **8**. `distributions.html` and `glossary.html` now sit lowest at 3, both nav-reached. **Quips: twenty-six reviewed, three shortened**, all for the recurring defect of a closing clause that only relabels the one before it (`which-test` lost "That's literally why this page exists", `cheat-assumptions` lost "never after", `datasets` lost "it has nowhere to be"). **Backlog item — no Backlog section exists (P37 is still unrun), so as in runs 11–22 the item became a checker. `tools/prescription-terms.js`: does the site tell a reader to RUN a method it never TEACHES?** `advice-terms.js` asks this of `software.js` and `checks.js` and stops there; this extends it to the four prescription surfaces it excluded — the posters' fix column, `problems.html`'s worked solutions, and `which-test.html`/`plan.html`'s leaves. The rule gates the **whole name-plus-head-noun pair** rather than the surname, which is load-bearing: "Welch" is taught, so a surname gate misses the defect the tool was written for. **Three things measured, not guessed:** dash normalization is load-bearing (without it four of the best-taught names on the site flag on nothing but en-dash-versus-hyphen); a bare capitalized token gave **22** flags and **10** even after a stem gate, all ordinary English and scenario names, against **1** for the compound-or-head-noun rule; and `snippets.js` comments were added, measured (617 → 1,006 chunks, one extra flag, `Iglewicz-Hoaglin`, which `data/outliers-in-practice` teaches) and dropped. It ships with an **empty `ACKED`** and found one thing on its own: `problems.html` labels a step **Welch–Satterthwaite degrees of freedom** and the site joined those two names nowhere else, so §2.2 now names the formula. **Proved on two independent defect shapes:** deleting §2.1's Welch section fired `Welch's ANOVA` and exit 1, and writing a fresh unexplained prescription into a poster fix cell (`Hartley's test`) fired that and exit 1, both restores byte-identical and back to exit 0. **Verification.** `audit.js` **0 errors / 20 warnings / 64 info** (baseline), `math-check.js` **297 assertions, 0 failures**, and `prose-lint`, `faq-audit`, `widget-terms`, `link-promises`, `advice-terms`, `untaught-names`, `worked-examples`, `search-reach` and the new `prescription-terms` all **exit 0 under `--strict`**. **Zero em-dashes were added to any page** — per-page counts held at 8 / 7 / 8 / 4 and `cheat-assumptions.html` fell 4 → 3, with §2.4's FAQ em-dashes falling 1 → 0 — while the four lesson subjects grew by **436 / 300 / 290 / 363 words**. **137 pages · 534 inline scripts · 235 JSON-LD blocks, 0 failures**; all shared JS, `sw.js`, the ten tools and `faq_data.py` parse. Rebuilt index **942 KB, 97 lessons, 37 pages**; searching it with the matcher lifted verbatim from `site.js` returned **19 HIT, 0 MISS of 19**, including from the poster. Browser pass over all seven changed pages via same-origin iframes at **1200px, 375px and 360px**: every expected string present, **0 console errors, 0 leading-slash links, 0 horizontal scroll at any width**, all ten link targets 200 and all five new fragments naming a real static id. Dark mode verified: bg `#0b1120`, `--link` `#6f7bf2`. The poster still fits one page, measured the P30 way: **A4 155.5 mm of 279, US Letter 152.1 of 261**. **`CACHE_VERSION` bumped sc-v41 → sc-v42** (`site.js` changed and is precached). **Two notes for the next run.** The index-search harness has a trap worth knowing: `flexRe` must be fed `norm(query)`, not the raw string, because its own `[^a-z0-9 ]+` strip removes **capital letters**, and the `pages` half of the index is an array of `{u, txt}` objects while `lessons` is a plain slug→text map — getting either wrong produces confident false MISSes, which it did twice here before the harness was fixed. And **a bare Greek symbol is not searchable at all**: `squash()` strips it to nothing, so `ε` returns zero however the corpus is written, which is why the new section spells "epsilon" out. Worth a decision next lap: teach symbols by their spelled name, or widen the matcher.
  - **29 Jul 2026 — twenty-second run (the picture and what it licenses you to conclude, four folk rules turned into numbers, and a checker for terms the site's own search cannot find).** The ledger asked for a thread, and this one picked itself: five pages that hand the reader a visual judgment and never say where the line is. `stats-1/visualizing-data` (§1.4), `stats-2/assumptions-and-when-they-break` (§2.5), `writing/tables-and-figures` (§3.3), `stats-4/meta-analysis` (§4.10) and `which-chart.html`, the thread's hub and the weakest inbound target on the site. **Absences, each confirmed by a sitewide grep excluding `.claude/worktrees` and the generated index.** (1) **`prediction interval` returned zero sitewide**, on a lesson whose own prose says "the diamond's width *is* its confidence interval" and stops there. §4.10 gained **"The diamond is not a forecast"**: for a literature shaped like the playground's defaults (*k* = 8, pooled *d* = 0.40 with SE 0.10, τ̂ = 0.20, I² ≈ 50%) the diamond runs **[0.20, 0.60]** while the 95% prediction interval runs **[−0.15, 0.95]**, so a new trial finding nothing is entirely consistent with the literature — and the interval is wider than the true-effect distribution it describes ([0.01, 0.79]) because with 6 df the multiplier is 2.45, not 1.96. (2) **`Sturges` and `Freedman–Diaconis` appeared only inside §1.4's own FAQ answer, named and never stated**, and `Scott's rule` was absent entirely, on the lesson whose whole interactive is a bin dial. §1.4 gained **"How wide should a bin be?"** with the three formulas worked on the widget's own bell (400 observations, SD 13, range ≈ 80 → **10, 13 and 17 bins**, bracketing the dial's default of 12), plus the counterintuitive part that had to be measured: on the two-peak shape Scott and Freedman–Diaconis both fall to **9** bins, *fewer* than they asked for on the bell, because the gap between the humps inflates the SD and the IQR alike. A claim that the coarser bins therefore hide the peaks was **simulated and abandoned** — at 9 bins the dip is plainly visible (counts 86/82/30/81/86), and it only disappears once the peaks are closer than about 2σ, where the true density is unimodal anyway. (3) **`Cumming`, `arm length` and `standard error bars` all returned zero**, while §3.3 asserted twice, in prose and again in its own FAQ, that "overlapping bars don't automatically mean no difference" without a number. §3.3 gained **"What overlapping bars actually mean"**: for two independent groups of 30, ±1 SE bars that just touch give *t* = 1.41, ***p* = .16**, 95% CIs that just touch give *t* = 2.83, ***p* = .006**, and *p* = .05 sits where two 95% CIs **overlap by 59% of one arm** or two SE bars **clear each other by 0.83 SE** — proportions that barely move with *n* (.007 at 20/group, .006 in the limit). The section ends on the case that breaks the rule outright: 20 people scoring 50 and 54 with a between-person SD of 12 give 95% CIs of **[44.4, 55.6] and [48.4, 59.6]**, near-total overlap, against a paired ***t*(19) = 4.47, *p* = .0003**. (4) **`quantile-quantile`, `envelope` in the Q-Q sense and `pointwise` returned zero**, on a lesson that told readers "if the points fall on a straight line, the data is normal" and "trust your eyes". §2.5 gained **"How crooked does normal data look?"**, and the sentence it walks back is now "behaving like a normal sample". At *n* = 20 the top point is **drawn at 1.87** and genuinely normal data lands it anywhere in **[1.29, 2.71]** while the middle point holds [−0.38, 0.26], so **the ends wobble 2.2× the middle**; and **49% of perfectly normal samples at *n* = 20 (66% at *n* = 50) put at least one point outside a pointwise 95% band**. **Every number was computed twice, in Node against `VIZ` and independently in R 4.5.2**, agreeing throughout (Q-Q envelope 1.29/2.71 vs 1.32/2.78 at 40k reps and 1.29/2.71 at 200k; spurious-bimodality 62.5% R vs 62.0% Node, 11.2% vs 11.3%; the overlap arithmetic and the paired *t* to 4 dp; bin rules against R's own `nclass.Sturges`/`nclass.scott`/`nclass.FD`). **`which-chart.html` gained the branch it was missing**: the chooser had no route for a within-subjects design and sent those readers to a grouped bar, which is exactly the display §3.3 now warns about. A **Slopegraph (paired lines)** leaf, its thumbnail, and a cheat-table row; the `bar` leaf's mistake string now carries the .16/.006 pair. **FAQ freshness: two swaps and one revision, scored with `faq-audit.js` then judged by hand.** Out: §3.3's "Should error bars show SD, SE, or a confidence interval?" (**ECHO 60%** against its own h2 "Say what the error bars show", **REPRISE 22%**, and its closing caution is now a whole section), replaced by **"My figure is in color. Does that matter?"** — `colorblind`, `grayscale`, `black and white` and `line style` all returned zero, and the answer carries a number computed from the site's own palette: the red and green it uses for good-versus-bad status, #ef4444 and #22c55e, have a grayscale contrast ratio of **1.65:1** against the 3:1 accessibility asks. Out: §1.4's "How many bins should a histogram have?", which the new section now answers in full, replaced by **"I only have 15 data points. Is a histogram still the right picture?"** (Sturges asks for 5 bins at *n* = 15, and **62% of 50,000 normal samples show a spurious second peak** at that setting against 11% at *n* = 400). And §2.5's normality FAQ was **revised rather than swapped** — it ended on "trust your eyes", which the new section contradicts — so it now points at the calibration instead. Still **291 Q&As, 291 unique question strings, 3 per lesson**. **software.js: rotated to four untouched by runs 19–21** — `meta-analysis`, `factorial-anova-two-way`, `post-hoc-tests`, `transformations-and-recoding` — against **SPSS 32.0.x** (GA 23 Apr 2026, no fix pack) and **JASP 0.98.1** (7 Jul 2026), both re-checked against the vendors' own pages rather than from memory; JASP's `rowMeanNaRm` was confirmed against its release notes. **Two genuine defects, both of the run-21 shape.** The 2×2 walkthrough reported simple effects of *F* = 15.2 and 0.31 beside a main effect of 9.31 and an interaction of 6.87, and **in a balanced 2×2 those four are locked together**: *F*_A + *F*_A×B must equal the two simple effects, 16.18 against 15.51. Holding the published effect sizes fixed, the simple effects are **16.09 (*p* < .001) and 0.09 (*p* = .76)**. And the meta-analysis sentence gave *k* = 18 studies of *N* = 3,842 with τ = 0.21 and a CI implying SE = 0.071, which **no set of 18 studies that large can produce** (that SE implies about 85 people per study, N ≈ 1,530). It was rebuilt from an actual DerSimonian–Laird fit over 18 studies totalling **N = 2,104**: *d* = 0.42, 95% CI [0.29, 0.55], *z* = 6.28, *Q*(17) = 38.62, *p* = .002, I² = 56%, τ = 0.21 — and it now carries the **95% prediction interval [−0.04, 0.89]** the lesson teaches. `post-hoc-tests` was checked and needed nothing (its two Tukey pairs reproduce *q* = 5.05 → *p* = .003 and *q* = 5.67 → *p* < .001 exactly). The Box–Cox tip's "usually half a unit wide or more" was replaced by the measured widths from `MASS::boxcox` (**0.8 at *n* = 50, 0.6 at *n* = 100**). **Snippets: three entries extended, every R line executed here on R 4.5.2 exactly as shipped** (`nclass.*` returning 10/13/18 on the shipped generator; `car::qqPlot(residuals(fit), envelope = .95)` on car 3.1.5). The exception is disclosed: **`metafor` is not installed here**, so `predict(res)` was taken from its documented API rather than run. A scan of all 77 snippets for deprecated APIs found nothing beyond run 20's single known false positive. **Link health, measured.** No teaching page sits at 0 inbound; the zeroes are navigation pages reached from the nav by design. `which-chart.html` was the weakest genuine target at **2 lesson sources** and gained five natural placements (§3.3, §1.3, §2.5-data, §2.4, §2.8) for **7**. **Quips: nine reviewed, two shortened**, both for the recurring defect of a closing clause that only relabels the one before it (`tables-and-figures` lost "Same three points, taller lie.", `descriptives` lost "The capybara will not judge it."). **Backlog item — no Backlog section exists (P37 is still unrun), so as in runs 11–21 the item became a checker. `tools/search-reach.js`: can the site's own SEARCH find the things the site TEACHES?** Run 13 found by hand that 87 of 97 lessons were truncated in the index and a quarter of the site's prose was unsearchable; the prompt has asked every run since to re-search the index for the terms it just wrote. This generalizes that step to all **262 glossary headwords** — the site's own declaration of what it teaches — and runs in about a second, **lifting `norm`/`squash`/`flexChar`/`flexRe` verbatim out of `site.js` by brace-matching** so it tests the shipped matcher rather than a copy. **Two stricter definitions were measured and discarded**: splitting compound headwords gave an identical 0 misses, and checking each term against the specific lesson its own back-link names flagged **46 of 262**, every spot-check a phrase artifact (`Alpha` against a lesson that writes α, `1.5 × IQR rule` against one that writes `1.5 × IQR`, `k-means clustering` against one that writes `k-means` nineteen times) — an 18% rate needing a 46-entry `ACKED`, which is the inventory VOICE.md rule 12 calls the mistake. **It found two genuine defects on its first run, both fixed in `site.js`.** **`Cramér's V` returned ZERO hits, accent typed or not** — `norm()` strips diacritics from the query while the corpus is matched raw, the mirror image of run 21's apostrophe bug — so `ACCENT_FOLD` now lets each squashed query letter accept its own accented forms (0 → 7 hits, all 24 control queries identical). And **`Wilks lambda` returned nothing**, because the site writes `Wilks' Λ (lambda)` and no flat one-separator budget crosses that gap; `flexRe` now allows up to three separators **at the reader's own word boundaries** while keeping one inside a word, which is the safe version of the change run 21 measured and rejected — `[^a-zA-Z0-9]{0,3}` cannot swallow a letter, so a match still never crosses a whole word. Over 31 queries only `Wilks lambda` 0 → 2, **`90% CI` 0 → 3 (run 21's other standing item)**, `p value` 46 → 58 and `test data` 2 → 4 moved by more than one; `ab`/`xy`/`zz`/`qq`/`test`/`data`/`mean`/`sd`/`Cohen's d`/`chi-square`/`of the`/`in a` were identical and 20 full-corpus passes stayed at 11 ms. Verified in the live overlay: Cramér's V 7 results, Wilks lambda 2, 90% CI 3, with the glossary card, the typo path (`hetroscedasticity` → Heteroscedasticity) and the honest dead end all unchanged. The checker ships with an **empty `ACKED`** and was **proved on two independent defect shapes** — blanking `ACCENT_FOLD` fired `Cramér's V` and exit 1, reintroducing the pre-run-13 glossary cap fired **26** terms and exit 1, both restores byte-identical and back to exit 0. Its own first run also caught **`Scott's rule` as a once-only eponym** in `untaught-names.js`, fixed by naming the bin rules in `descriptives.html` beside the rule that page actually uses. **Verification.** `audit.js` **0 errors / 20 warnings / 64 info** (baseline), `math-check.js` **297 assertions, 0 failures**, and `prose-lint`, `faq-audit`, `widget-terms`, `link-promises`, `advice-terms`, `untaught-names`, `worked-examples` and the new `search-reach` all **exit 0 under `--strict`**. **Zero em-dashes were added to any page** — per-page counts held at 7 / 9 / 6 / 6 and `tables-and-figures` fell 7 → 6 — while the four lesson subjects grew by 309 / 298 / 353 / 275 words. **137 pages · 534 inline scripts · 235 JSON-LD blocks, 0 failures**; all shared JS, `sw.js`, the eight tools and `faq_data.py` parse. Rebuilt index **933 KB, 97 lessons, 37 pages**; the term search returned **30 HIT, 2 MISS of 32**, and both misses are explained rather than defects (`pointwise` is a word the prose deliberately avoids, `rowMeanNaRm` lives only in `software.js`, which is not an index source). Browser pass over all eleven changed pages via same-origin iframes at **1200px, 375px and 360px**: every expected string present, **0 console errors, 0 leading-slash links, 0 horizontal scroll at any width**, all link targets 200, and the new chooser branch driven end to end with its thumbnail painting in both themes. Dark mode verified: bg `#0b1120`, `--link` `#6f7bf2`. **`CACHE_VERSION` bumped sc-v40 → sc-v41** (`site.js` changed and is precached). **`AGENTS.md` regenerated from `CLAUDE.md`**, clearing the standing item run 21 left.
  - **29 Jul 2026 — twenty-first run, and the first of LAP 2 (the confidence interval as a reported object, a self-contradiction between two of the site's own pages, and a checker for worked examples that do not hold together).** The ledger was empty, so this run took a thread rather than a staleness order: five pages that all promise an interval and stop short of one. `apa.html`, `cheat-apa.html`, `writing/reporting-statistics-apa` (§3.2), `stats-1/effect-size-and-power` (§1.13) and `stats-2/correlation` (§2.8). **The thread was chosen from the site's own words:** §3.2 tells readers that means, mean differences, correlations and regression coefficients "have no excuse at all" for omitting an interval, and the site's own APA formatter omitted them for all four. **Absences, each confirmed by a sitewide grep excluding `.claude/worktrees` and the generated index.** (1) **`Fisher z` appeared exactly once on the whole site** — as a parenthetical label in `correlation.html`'s readout, "95% CI for *r* (Fisher *z*)" — with `r-to-z`, `Fisher's z` and `z transformation` all at zero and the transformation itself explained nowhere; that is `widget-terms.js`'s defect shape on a surface `widget-terms.js` does not scan. §2.8 gained **"The interval around *r*"**, worked on the lesson's own numbers: *r* = .42 at *n* = 60 gives *z* = .448, SE = 1/√57 = .132, interval [.188, .707] on the transformed scale and **95% CI [.19, .61]** back on the correlation scale, sitting .23 below the estimate and .19 above it. Halving that width to ±.10 takes **n = 263**. (2) **`90% CI` returned zero sitewide**, on a site that teaches η² in five lessons; §3.2 gained the paragraph, with the reason rather than the rule — the *F* test is one-tailed, so at 90% the lower limit leaves zero exactly when *p* reaches .05, while a 95% interval holds on to zero until *p* reaches .025 and a result significant at .05 but not at .025 therefore gets a 95% interval starting at exactly zero. For §3.2's own ANOVA the two are **90% CI [.02, .21] against 95% CI [.01, .23]**. (3) **`stats-1/effect-size-and-power` had zero occurrences of "confidence interval"** on a lesson that teaches Cohen's *d*; it gained **"How precisely do you know *d*?"**, worked on the playground's own dials: at the defaults (*d* = 0.50, 30/group) the test gives *t*(58) = 1.94, *p* = .058 and an interval of **[−0.02, 1.01]**; at the 64/group the same widget asks for at 80% power, *t*(126) = 2.83, *p* = .005 and **[0.15, 0.85]** — a study that worked and still cannot separate small from large. Pinning *d* to ±0.20 takes **199 per group**, three times what 80% power costs. (4) **`cheat-apa.html` mentioned confidence intervals nowhere**, while its own card ④ told readers to pair *p* with an effect size "ideally a CI"; it gained card **⑥ The interval, in brackets**, and still fits one page (A4 200.5 mm of 279 mm, US Letter 200.5 of 261, measured the P30 way by flipping every `@media print` rule's `mediaText` to include `screen`). **THE BUILD: `apa.html` now emits an interval for every pattern, which was the oldest item on the list at nine runs.** `viz.js` gained the effect-size CI block — `nctCI` / `varExpCI` / `vCI` / `rCI` plus the two solvers underneath — because for *d*, partial η², R² and Cramér's *V* there is no ± formula and the interval has to be found by inverting the test. **Every published number was reproduced independently in R 4.5.2** by inverting `pt`/`pf`/`pchisq(ncp=)` with `uniroot`, agreeing to **4 dp on all eight anchors**, and the intervals were then checked for **coverage by simulation at 5,000 reps: .949 / .900 / .902 against nominal .95 / .90 / .90**. `math-check.js` grew a seventh section and went **270 → 297 assertions**. **The R cross-check earned its keep immediately: the first draft clamped the *t*'s noncentrality at zero**, which is right for the λ of a χ² or *F* (a sum of squares) and wrong for a signed δ, and it was reporting *d* ≥ 0.00 for a *p* = .058 result whose honest answer is *d* ≥ −0.02. `deltaSolve` now brackets on both sides and math-check asserts the sign behavior in both directions, including that δ_L passes through exactly 0 at the critical *t*. The tool also gained a **`k` = min(rows, cols) − 1 field** (Cramér's *V* cannot be bounded without the table's shape) and a second consistency note beside the statcheck one, for η² and R² against *F*. **A SELF-CONTRADICTION BETWEEN TWO PAGES, found by trying to write one sentence.** §1.13's power playground reported **n ≈ 63 per group for 80% power at *d* = 0.5** while `power.html`, `plan.html`, `problems.html` and `math-check.js` all say **64** (the G*Power anchor): the widget's readouts were normal-approximation, undeclared. The three printed numbers are now exact noncentral-*t* (**48% power, ±0.52 critical *d̂*, ≈ 64**), the two drawn curves stay normal because that is what makes the picture readable, and the code comment now says which is which. The `n80` search starts from the normal estimate, which measurement showed lands 0–3 below the exact answer across the whole slider range, and is cached on (*d*, α, tails) so dragging the sample-size slider never recomputes it. Verified in-browser: both sliders round-trip to byte-identical readouts. **FAQ freshness: two swaps, and two lessons deliberately left alone.** Out: §2.8's "What sample size do I need for a reliable correlation?", whose "estimates only start stabilizing around n ≈ 150–250" was folklore the new section replaces with an exact number; and §3.2's "Do I italicize *p* and *t*?" (**ECHO 67%**, answered in full by the lesson's own h2, again by the poster's card ③ and again by the formatter, and closing on an unverifiable superlative, "the most frequently flagged APA error there is"). In: **"My two groups both show a correlation, but one is bigger. Can I test whether they really differ?"** — `compare two correlations`, `two correlations` and `dependent correlations` all returned zero — carrying *r* = .55 against *r* = .30 at *n* = 60 each, **z = 1.65, *p* = .099**, intervals [.34, .71] and [.05, .51], and **168 per group** to detect that gap at 80% power (all four numbers run in Node and again in R); and **"My output has no confidence interval on the effect size. Do I leave it out?"**, which is precise about SPSS being inconsistent with itself (one-way ANOVA has printed an *ANOVA Effect Sizes* table with a 95% interval since v27, GLM Univariate still prints partial η² with none) and ends on what not to do. §1.13's three were kept whole. Still **291 Q&As, 291 unique question strings, 3 per lesson**. **software.js: rotated to four untouched by runs 18–20** — `correlation`, `one-way-anova`, `simple-linear-regression`, `chi-square-tests` — against **SPSS 32.0.x** (still current, GA 23 Apr 2026) and **JASP 0.98.1** (7 Jul 2026, still current; 0.98.0 added JASP AI and 0.98.1 is a Repeated Measures ANOVA fix, no menu renames), both re-checked against the vendors' own release notes rather than from memory. Every statistic re-derived: correlation *r* = .42/df 58 → *t* = 3.53, *p* < .001 with CI [.19, .61] exact; one-way *F*(2, 87) = 5.42 → *p* = .0060, η² = .1108; regression *b*/*SE* = 4.053 → 4.05 with CI [1.16, 3.46] exact and R² = .25 matching *F* = 16.40 to .2547. **One genuine defect:** the chi-square entry reported "68% of treated patients recovered versus 45% of controls" beside χ²(1, *N* = 120) = 6.25, and **no integer 2×2 table with *N* = 120 produces both** (an exhaustive scan over every row split found none). It now reads 41 of 60 against 27 of 60, **χ² = 6.65, *p* = .010, φ = .24, 90% CI [.09, .39]**. The other three entries gained the intervals `apa.html` now emits. Still 34 entries. **Snippets: both subject entries extended in step**, with **every R line executed here on R 4.5.2 exactly as shipped** (the two-correlation Fisher-*z* test returns *p* = .0992; the `uniroot` interval on *d* returns [0.15, 0.85], and the bracket was narrowed to *t* ± 6 specifically to stop R emitting a `pnt{final}` precision warning a student would not know to ignore). The Python correlation snippet moved to the modern `stats.pearsonr(...).confidence_interval()` (SciPy 1.11+). **Link health, measured.** No teaching page sits at 0 inbound; the six zeroes are navigation and utility pages reached from the nav by design. Six natural placements raised run 20's two flagged targets: **`cheat-apa.html` 2 → 3, `correlation.html` 2 → 3, `apa.html` 3 → 5**. **Quips: five reviewed, three rewritten.** `reporting-statistics-apa`, `cheat-apa` and `apa` all opened "Italic *t*, upright η², …" — one template, three surfaces; two now open differently and the third lost a clause. **Backlog item — no Backlog section exists (P37 is still unrun), so as in runs 11–20 the item became a checker, and this run's own hand-finds defined it. `tools/worked-examples.js`: does a published worked example hold together?** `audit.js` CHECK 9 recomputes the *p* beside a statistic; nothing checked the **effect size** in the same sentence, and this run found four of those by hand — apa.html's R² default (.34 against an *F* implying .31), apa.html's one-sample *d* default (0.42 against a *t* implying 0.41), software.js's chi-square percentages, and cheat-apa.html's worked sentence (M/SD pairs producing *t* = 2.44, not the *t* = 2.02 printed two clauses later; its SDs are now 2.5 and 2.8, which reproduce *t* = 2.022, *p* = .0466 and *d* = 0.452 exactly). **Two of the four live in a tool's field defaults**, so the script renders `apa.html`'s nine default sentences under a DOM shim and checks those too. **Rounding is handled exactly rather than with a fudge factor**: a published *F* of 5.42 stands for [5.415, 5.425), the identity is monotone in *F*, and the flag fires only when the implied band fails to overlap the band the published effect size stands for — no tolerance constant appears in the file. **Three gates were arrived at by measuring.** Requiring no other statistic between the halves kills the one false pairing in the corpus (a greedy window hands `F(1, 76) = 9.31`'s η² to the `F(1, 76) = 0.31` three clauses earlier). **Reading only forward missed every R² on the site, including the defect the script was written for**, because `R² = .31, 90% CI […], F(3, 96) = 14.20` is the ordinary APA order — so it reads both directions. And both windows stop at the nearest **clause** boundary rather than a full stop, which is precisely what separates the legitimate backward pair from the false one (a colon). Plain η² is the one judgment call and ships with **one `ACKED` entry** (a repeated-measures example where η² is legitimately smaller than partial η²). **Its blind spots are in the header rather than smoothed over: two of the four defects that motivated it are outside its reach** — recovering an integer 2×2 table from two rounded percentages, or a *t* from two M/SD pairs without the group split, is a search rather than an identity. **Proved on four reintroduced defects across three surfaces** (both apa.html defaults, an η²p in lesson HTML, an η² in a software.js walkthrough): each fired with exit 1, each restore returned the file byte-identical and the exit code to 0. It reports **31 pairs** today. **A SEARCH DEFECT THE INDEX-SEARCH STEP CAUGHT, and it was not about this run's terms.** Searching the rebuilt index for everything added, "Fisher's transformation" came back empty in the live overlay while sitting plainly in the index. `flexRe` allowed **only a space or hyphen** between query characters while `squash()` strips **every** non-alphanumeric out of the query, so any apostrophe in the corpus blocked a match and **`Cohen's d` — about as ordinary a query as this site can receive — returned nothing at all**. Widening the class to any single non-alphanumeric took **9 of 15 natural possessive queries from zero hits to hits** (Cohen's d 0 → 15, Levene's test 0 → 7, Cook's distance 0 → 5, Tukey's HSD 0 → 3, McNemar's test 0 → 2) while `ab`/`xy`/`zz`/`qq`/`test`/`data`/`mean`/`sd` returned **identical** counts and the full-corpus scan stayed at 15 ms per 20 passes. It stays bounded at **one** separator: two would let `90% CI` match but would also let a query drift across unrelated words, so that case is noted for the next run rather than fixed. **Verification.** `audit.js` **0 errors / 20 warnings / 64 info** (baseline), `math-check.js` **297 assertions, 0 failures**, and `prose-lint`, `faq-audit`, `widget-terms`, `link-promises`, `advice-terms`, `untaught-names` and the new `worked-examples` all **exit 0 under `--strict`**. **Zero em-dashes were added to any page's prose** — per-page counts held at 9 / 8 / 6 / 5 and `cheat-apa.html` fell 4 → 3 — while the five subjects grew by **434 / 338 / 212 / 130 / 128 words**; every em-dash in an added line is pre-existing text swept up by a rewritten line, which the unchanged counts confirm. The diff read-back caught two things no linter sees: a signposting sentence opening the *d*-interval identity ("One identity is worth carrying out of this"), deleted outright, and a FAQ answer opening "Yes, and…", which is the verdict reflex VOICE rule 7 bans in its comma form. **137 pages · 534 inline scripts · 235 JSON-LD blocks, 0 failures**; all shared JS, `sw.js`, the seven tools and `faq_data.py` parse. Rebuilt index **925 KB, 97 lessons, 37 pages, 0 lessons truncated** (longest 16.3k against the 20,000 cap), and the term search returned **22 HIT, 2 MISS of 24** — the two misses are a snippet comment and a runtime note string, neither of which is a search-index source. Browser pass over all five changed pages via same-origin iframes at **1200px, 375px and 360px**: every expected string present, **0 console errors, 0 leading-slash links, 0 horizontal scroll at any width**, all seven new or edited link targets returning **200**, both new `h2` anchors present as static ids, and the formatter driven end to end (nine analyses, the optional means producing *95% CI of the difference* [0.33, 4.07], the `k` field rescaling *V*'s interval, and the new η²/R² warning firing on a deliberately wrong R²). Dark mode verified with `localStorage.theme` set before load: bg `#0b1120`, `--link` `#6f7bf2`. **`CACHE_VERSION` bumped sc-v39 → sc-v40** (both `viz.js` and `site.js` changed and both are precached). **Two notes for the next run.** The lap-2 thread list is in the ledger line above. And `AGENTS.md` is a two-day-stale copy of `CLAUDE.md` that nothing checks — worth either syncing or deleting.
  - **28 Jul 2026 — twentieth run (the Stats 4 psychophysics pair, the folk claim that turned out to be conditional, and a checker for names the site drops and never picks up). LAP 1 IS COMPLETE: all 97 lessons have now been subjects.** The ledger named the last two and this run took them: `stats-4/psychometric-functions` (§4.11) and `stats-4/signal-detection-theory` (§4.12). They share a defect shape, which is what made them a genuine pair rather than a leftover: **each names the procedure that produced its data, or the assumption its arithmetic rests on, and stops before the machinery.** **Five absences, each confirmed by a sitewide grep excluding the stale clones under `.claude/worktrees` and the generated `search-index.js`.** (1) **`staircase` appeared on the site three times and never as a psychophysical method** (the other two are a Kaplan–Meier step function and a regression tree), with `1-up`, `transformed up-down`, `Levitt`, `QUEST`, `method of limits`, `method of adjustment` and `adaptive method` all at zero — on a lesson whose own parenthetical says adaptive procedures "reach the threshold with fewer trials". §4.11 gained **"How adaptive procedures find the threshold"**, and **the folk claim did not survive being simulated, which is the most useful thing this run did.** On the lesson's own observer (PSE 500 ms, σ 80 ms, 280 trials, matching its 7 × 40 ladder), the staircase's *placement* is superb — **97% of its trials land within 80 ms of the true PSE against the ladder's one level in seven**, and by Fisher information the **160 trials at 200/300/700/800 ms carry 7%** of what the experiment learns while the 40 at 500 ms carry 44%. But the textbook estimator gives it all back: **averaging the last eight reversals recovers the PSE with SD 24 ms against the ladder's 11 ms, and stops improving after about forty trials**, because eight reversals are eight numbers however long you run. **Fit the psychometric function to all 280 staircase trials and the SD drops to 6 ms, so the fixed ladder needs about 790 trials, 2.8×, to match it.** The honest moral, which is now the section's ending, is that adaptivity buys placement and freedom from having to know the answer in advance, not a free pass on the analysis: with a true PSE of 525 ms and a sharp σ of 25 ms, only one rung of the ladder falls on the rising part of the curve and **constant stimuli returns 505 ms, out by twenty, while the staircase lands on 525.0**. Levitt's transformed rules are stated with their convergence points (`p^k = ½` → **70.7%** and **79.4%**) and confirmed by simulation at **.705** and **.790**. Everything here was run twice, in Node against `VIZ` and independently in **R 4.5.2** (R gave SD 10.5 / 22.9 / 6.2 and 800 trials to match, against Node's 10.6 / 23.7 / 6.3 and 791). (2) **`2AFC` and `chance level` returned zero, `forced choice` zero in this sense, and `guess rate` appeared once — inside §4.11's own FAQ answer, undefined** — while §4.12's FAQ leans on "a two-alternative comparison" as its anchor for what a *d′* means. §4.11 gained **"When the curve cannot start at zero"**: the ψ(x) = γ + (1 − γ − λ)F(x) form, the 75% threshold convention, and the damage measured rather than asserted. Fitting a plain two-parameter curve to 2AFC data from the lesson's own observer puts the threshold at **280 ms against a true 500** and inflates the JND from 54 ms to 160 ms; holding γ at .5 recovers it to within a millisecond. The lapse case is gentler and aimed at the number people report: a **4% lapse rate leaves the PSE at 500 ms and takes the JND to 73 ms against a true 54, a third too wide** (R independently: 73.2 ms, +36%), returning to 53 ms once the lapse is in the model — which puts a magnitude on a claim §4.11's own FAQ has always made without one. The section also lands the identity nothing on the site stated, **Pc = Φ(d′/√2), the same formula as the AUC identity**, so 76% correct is *d′* = 1 (.7602) and 92% is *d′* = 2 (.9214) — the two anchors §4.12's FAQ was already quoting. (3) **`Weber's law` appeared exactly once sitewide**, in a subordinate clause of §4.11 ("the lifted-weight experiments that gave us Weber's law"), with the law never stated; `Fechner` once, `Stevens' power law` and `absolute threshold` zero. It now gets a paragraph, and it earns its place by explaining a fact the lesson already asserted and never justified: **if equal ratios feel equally different, the bisection point sits at the geometric mean**, √(200 × 800) = 400 ms, because 400 is double 200 and 800 is double 400. (4) **`optimal criterion` returned zero and `prior odds`/`posterior odds` appeared only in `problems.html`**, on a lesson that says criterion placement "is where costs, rewards, and base rates enter the decision" and gives no rule. §4.12 gained **"Where the criterion should sit"** with β_opt = [P(N)/P(S)] × [cost of a false alarm / cost of a miss] and *c*_opt = ln(β_opt)/*d′*, **every case confirmed by a numeric scan of expected value over λ to four decimals**: at the playground's own *d′* = 1.5, equal base rates and equal costs give *c* = 0 (the slider's home position), a 10% base rate gives β = 9 and *c* = **+1.465** (hits 23.7%, false alarms 1.3%), and a smoke alarm whose miss costs twenty times a false alarm gives β = .05 and *c* = **−2.00**. The case worth teaching is the one where both forces pull: **5 in 1,000 prevalence against a miss costing a hundred times a recall gives β = 199/100 = 1.99 and *c* = +0.459**, barely conservative, hit rate 61.4%, false alarms 11.3% — and the live widget at its nearest step (*c* = 0.45) reads **61.8% / 11.5%**, so the section is checkable on its own page. (5) **`zROC` returned zero and every one of the 14 "unequal variance" hits was Welch or homoscedasticity**, on a lesson that states "for the equal-variance model, AUC = Φ(*d′*/√2)" and never says how you would know. §4.12 gained **"When the two bells are not the same width"**, and the table it now carries is the reason the section exists: with the signal distribution at 1.5 with SD 1.25 against standard-normal noise (zROC slope **.800**, intercept **1.200**, both exact), **plain *d′* walks from 1.10 to 1.60 across six criterion placements** — one observer, one true sensitivity, six answers, and the only thing that changed is willingness to say yes. **d_a = 1.325 and A_z = .8256**, verified three independent ways: closed form, numeric integration under the ROC (.825672), and a **forty-million-draw Monte Carlo in R (.825570)**. Equal-variance readings of that same curve would have claimed areas from .782 to .871. **FAQ freshness: two swaps, one deliberate keep, scored with `faq-audit.js` and then judged by hand.** Out: §4.11's "What is the difference between the PSE and the JND?" (**ECHO 67%** against its own h2 "Two numbers: PSE and JND", which answers it in full) and §4.12's "What is the difference between *d′* and the criterion *c*?" (**ECHO 50%** against "Sweep the criterion…", **TWIN question 50%** with `reporting-statistics-apa` #2, and answered outright by the lesson's own "Skill or strategy" section). Both were "what is the difference between X and Y" openers, so the census the ledger tracks drops by two. In: **"Can I pool everyone's trials into one curve?"** — `pooling across`, `aggregate the data` and `pooled curve` all returned zero, and the answer carries an exact identity plus a simulation (a mixture of shifted cumulative Gaussians has spread **√(σ² + τ²)**; 24 observers with a true JND of 54 ms whose PSEs scatter with SD 60 ms give a pooled curve reading **67 ms, a quarter too wide**, closed form 67.45, while the PSE comes back correct) — and **"My task was two-alternative forced choice, not yes/no. Does *d′* still work?"**, which is where the Pc = Φ(*d′*/√2) conversion belongs. **§4.12's "What is a good *d′* value?" was kept despite the highest twin on the page** (question **67%** with `roc-curves-and-auc` #1) because only the template twins: the two answers share nothing in substance and this is the exact query students type. Still **291 Q&As, 291 unique question strings, 3 per lesson**. **software.js: rotated to four untouched by runs 17–19** — `psychometric-functions`, `ancova`, `factor-analysis-pca` and `survival-analysis` — against **SPSS 32.0.x (GA 23 Apr 2026, still current; its headline addition is a native *Analyze → Mediation Analysis*, which a previous run had already caught in the mediation entry)** and **JASP 0.98.1 (7 Jul 2026, still current; 0.98.1 is a Repeated Measures ANOVA fix plus help-file translations, no menu renames)**. Every statistic re-derived: ancova **F(1, 57) = 6.84 → p = .0114 → .011**, ηp² = 6.84/(6.84 + 57) = **.107 → .11**, slopes check **F(1, 56) = 0.42 → p = .5196 → .52**, and the df pair 57/56 is exactly right for N = 60 with one factor and one covariate; factor analysis **χ²(190) = 1438.2 → p ≈ 1.8e-190**, and df 190 implies **20 items**, consistent with the 9 + 7 that load; survival **χ²(1) = 8.42 → p = .00371 → .004** with the **HR CI log-symmetric** (ln 1.83 = .6043 against a midpoint of .6047) implying z = 2.86, p = .0042, coherent with the log-rank; psychometric **z = 3.46 → p = 5.4e-4**, CI [−72, −20] centred exactly on Δ = −46 with an implied SE of 13.30. **One genuine drift fix:** the JASP steps for `survival-analysis` stopped at Kaplan–Meier while the tips told readers to report a hazard ratio, leaving a JASP reader with no way to get one — the module has offered **semi-parametric Cox regression since 0.19.2 and parametric survival since 0.95**, and the steps now say so. Still 34 entries. **Snippets: both subject entries extended in step with the new sections** — a fixed-guess-rate ML fit for 2AFC and the zROC → *d_a* → *A_z* chain, in both languages — with **every R line executed here on R 4.5.2 exactly as shipped** (the zROC block run on the rounded rates printed in §4.12's own table returns slope **.801**, intercept **1.199**, *d_a* **1.324**, *A_z* **.825**, which is an independent check that the published table is internally consistent). A scan of all 77 snippet entries for deprecated APIs (`aes_string`, `groupby().apply`, `np.float`, `distplot`, `gather`/`spread`, `funs()`, `sklearn.cross_validation`, …) found **nothing** beyond the pandas fix run 18 made; the one hit was `list.append` in a toy Markov snippet, a false positive. **Link health, measured.** No teaching page sits at 0 inbound; the four zeroes are navigation and utility pages reached from the nav by design, as in runs 18–19. **No lesson sits at 0 or 1 outbound** (the minimum is 2), so both of step 7's questions are answered and the next weakest target is inbound: `tables.html` was at **4** and gains a genuinely natural placement in §4.12, whose worked example does two z-transforms by hand. **`cheat-apa.html` and `correlation.html` sit at 2 each and were deliberately left**, since forcing either into a psychophysics lesson is the over-linking step 7 warns against; they are flagged in the ledger for the run that takes a lesson where they fit. **Quips: five reviewed, three kept, two shortened**, both for the same defect — a closing clause that only labels the joke it follows. §4.11's lost "That's the PSE." and `variables-and-operationalization` lost "That's operationalization.", which is the shorter fix VOICE.md asks for. **Backlog item — no Backlog section exists (P37 is still unrun), so as in runs 11–19 the item became a checker, and this run's own first find defined it. `tools/untaught-names.js`: does the site NAME a method and never say what it is?** The three existing term checkers read what an *interactive prints*, what *advice strings say* and what a *link anchor promises*; ordinary lesson prose was the surface none of them touched, and `Weber's law` sat in it, used once, explained nowhere. **The rule:** an eponym (capitalized surname + possessive + a head noun from a closed list that makes it a method) occurring **exactly once sitewide** is a name the site drops and never picks up. **Five definitions were measured against the corpus and discarded first.** "Never wrapped in `<strong>`" as the proxy for untaught gave **10** flags including `Levene's test` and `Cohen's kappa`, which the site plainly teaches — bolding is a first-use convention, not a completeness guarantee. Any possessive plus any lowercase word gave **69** flags of "That's the", "Let's build", "Marek's neighbor". Not normalizing apostrophes inflated the once-only list **from 2 to 8**, because lesson HTML writes `Welch's` straight and `software.js` writes it curly, so half that list was one term counted as two. **An acronym class was built, measured and dropped:** after filtering ordinary words set in capitals (`PARTICIPATE`, `CONSENT`, `OMNIBUS`) and indexed series (`PC3` in "variance in PC1-PC3"), **26** flags remained and they were `BBC`, `USB`, `USD`, `API`, `OS`, `UX`, `WCAG`, `WWII` beside `MCMC`, `JAGS` and `QRP` — what makes an acronym owed an explanation is its *domain*, which cannot be read off its shape, and the alternative is the hand-kept inventory VOICE.md rule 12 documents as the mistake. **And the first shout filter lower-cased the text before collecting its vocabulary, so every acronym inserted its own lowercase form and masked itself** — GDPR, MCMC and SDT were all silently filtered and only acronyms containing a digit ever fired. **That bug was caught by the reintroduce-the-defect step and by nothing else, which is exactly why the step exists.** Development also walked straight into the glossary trap CLAUDE.md documents for `build-search-index.py`: `glossary-data.js` is plain text whose definitions contain `p < .05`, and running it through the HTML tag-strip ate most of the deck and made `Simpson's paradox` look like a once-only name when the glossary defines it perfectly well. **On its first clean run it found one defect on its own:** `software.js` called the MANOVA statistic **Hotelling's trace** while `stats-3/manova` only ever wrote **Hotelling–Lawley trace**, so a reader following the SPSS steps met a name the lesson never used and could not search for; both files now bridge the two names. It ships with an **empty `ACKED`** table. **Proved on two independent defect shapes:** deleting the paragraph that states Weber's law fired `Weber's law` and exit 1, and writing a fresh unexplained eponym (`Levitt's rule`) fired that and exit 1, both returning to exit 0 on restore with the file byte-identical to its pre-test state. Its blind spots are in the header rather than smoothed over: a name made of **ordinary lowercase words is invisible** to it, which is precisely how `staircase`, `guess rate` and the equal-variance assumption had to be found this run, by reading. **A REGRESSION THIS RUN CAUSED AND THE INDEX SEARCH CAUGHT.** The two subject lessons grew to **16,281 and 14,538 characters**, past `LESSON_MAX_CHARS`, and because the "Common questions" block sits at the END of the article both were truncated **exactly on their FAQ answers** — the identical defect run 13 found and fixed by raising the cap from 4,500 to 12,000, back after seven runs of 300-to-600-word additions. Searching the rebuilt index for every term this run added is what surfaced it (**4 MISS on the first pass**, two of them the new FAQ answers). **`LESSON_MAX_CHARS` raised 12,000 → 20,000**, which indexes every lesson whole (longest 16.3k) for **0.8% more index text**; the cap is a runaway guard, not a budget, and both the script and CLAUDE.md now say to check it first when a term cannot be found. Rebuilt index **919 KB, 97 lessons, 37 pages, 0 lessons truncated, 0 missing their FAQ block**, and the term search then returned **35 HIT, 2 MISS of 37** — the two remaining misses are `Semi-parametric` and `Cox regression` as written in a `software.js` walkthrough, and **`software.js` has never been a search-index source at all**; `Cox`, `proportional hazards` and `hazard ratio` all resolve to `survival-analysis`, so nothing is unreachable, but that whole injected surface is unindexed and is noted in the ledger. **Verification:** `audit.js` **0 errors / 20 warnings / 64 info** (baseline restored after the rebuild), `math-check.js` **270 assertions, 0 failures**, and `prose-lint`, `faq-audit`, `widget-terms`, `link-promises`, `advice-terms` and the new `untaught-names` all **exit 0 under `--strict`** — with **zero em-dashes added to any subject** (§4.11 stayed at 6 and its FAQ went 1 → 0; §4.12 stayed at 10 and its FAQ went 3 → 2) while those two pages grew by **1,048 and 747 words**. The diff read-back found 5 em-dashes in added lines and every one is pre-existing text swept up by a rewritten line or a regenerated FAQ block, which the unchanged per-page counts confirm; it also caught one thing the linter cannot see, a two-sentence build to a reveal opening the criterion section, and the shortest fix flattened it into one plain sentence. **3 changed pages · 12 inline scripts · 6 JSON-LD blocks, 0 failures**; all shared JS, `sw.js`, the seven tools and `faq_data.py` parse. Browser pass over all three changed pages via same-origin iframes at **1200px, 375px and 360px**: every expected string present, **0 console errors, 0 leading-slash links, 0 horizontal scroll at any width**, every lesson `h2` carrying an id, all 8 new or edited link targets returning **200**, the new `.ref-table` auto-wrapped in `.hscroll`, and §4.11's sliders round-tripping to the exact same six readouts (frozen-noise law intact). Dark mode verified with `localStorage.theme` set before load: bg `#0b1120`, `--link` `#6f7bf2`, canvases painted, and the new table measured at **8.91:1** body and **7.34:1** header contrast with the new prose link at **5.19:1** against the surface and **3.06:1** against body text, clearing both gates `--link` is tuned for. **`CACHE_VERSION` bumped sc-v38 → sc-v39** (two QUIPS changed and `site.js` is precached; `snippets.js` and `software.js` also changed but are lazy-loaded and never precached). **Three notes for the next run, which is the first of lap 2.** The ledger is empty and lap 1 is over, so read the lap-2 note under the P39 prompt before choosing anything. **`apa.html` still emits no confidence interval** — eight runs old, and now the only item on that list. And a standing observation this run chose not to act on: run 19 kept §4.1's Tuskegee FAQ at REPRISE 42% for the same reason this run kept §4.12's *d′* question, that a high score on a question students actually type is not a reason to swap it; two runs have now made that call, and it is probably time to say so in `faq-audit.js`'s header rather than rediscovering it a third time.
  - **28 Jul 2026 — nineteenth run (the inferential-foundations arc, five lessons that name an assumption and never say how you would know, and a checker for the terms the site's own advice uses without teaching).** The ledger named the thread and this run took it: `stats-1/sampling-distributions` (§1.7), `stats-1/central-limit-theorem` (§1.8), `stats-2/non-parametric-alternatives` (§2.6), `stats-3/assumptions-of-regression` (§3.8) and `stats-4/generalized-linear-models` (§4.4), which read as one argument about what a model assumes of its data. **95 of 97 lessons have now been subjects, and every course except the Stats 4 psychophysics pair is finished.** They share a defect shape, which is what made them a coherent five: each states a condition and stops before the number that would tell you whether the condition holds. **Five absences, each confirmed by a sitewide grep that excluded the stale clones under `.claude/worktrees` and the generated `search-index.js`.** (1) **`relative efficiency`, `efficient estimator`, `efficiency of the`, `standard error of the median`, `consistent estimator` and `biased estimator` all returned zero**, on a lesson whose widget offers *Sample median* as a collectible statistic, prints its standard error, and says only that the distribution "changes shape and width". §1.7 gained **"Which estimate should you trust?"**, naming unbiasedness, consistency and efficiency, with the trade measured on the widget's **own three populations** (400,000 draws each at *n* = 25): uniform **SE 5.77 vs 9.61**, bimodal **4.68 vs 13.45**, skewed **3.16 vs 3.23**. Two of those three rows are teaching points in their own right and both were verified against closed form (uniform σ/√n = **5.774**, bimodal **4.682**). The bimodal row is the extreme case because almost nobody in that population sits near 50, so **74% of sample medians land outside the 40-to-60 band**; the skewed row is where efficiency is the wrong question outright, since that population's mean is **15.96** and its median **16·ln2 = 11.09**, so the two statistics estimate different numbers. The normal-population anchor is exact: **√(π/2) = 1.2533**, so a median costs **π/2 ≈ 1.57×** the participants (simulation gave 1.2434 at n = 25, 1.2356 at n = 50). (2) **`finite variance` and `infinite variance` returned zero, and `Cauchy` appeared once, as a Bayes prior scale in `software.js`** — on a lesson whose lede says "almost any distribution" and states no condition at all. §1.8 gained **"The fine print on 'almost any'"**: the Cauchy counterexample, measured (400,000 repeats, the middle half of the averages spans **2.00 units at n = 1 and 2.02 at n = 1,000**, against the exact Cauchy IQR of 2), and then the question the folk rule dodges. On the playground's **own** Skewed population (exponential, mean 15.96, skewness **1.8403**), an ordinary 95% *t* interval covers **90.2 / 92.8 / 93.6 / 94.3 / 94.7%** at n = **10 / 30 / 50 / 100 / 200** over 300,000 simulated studies each, and the failures are lopsided: at n = 30 the interval lands entirely **below** μ 6.4% of the time against **0.8%** above. The mechanism is stated and checked rather than asserted — the sampling distribution of the mean carries the population's skewness divided by √n, measured **0.336** at n = 30 against a predicted 1.8403/√30 = **0.336** — so leftover skew fades on the same schedule the standard error does. A named rule of thumb (Cochran's n > 25γ₁²) was drafted and **cut** because its published guarantee could not be reproduced against these coverage numbers; the measured table ships instead. (3) **`rank-biserial` appeared in exactly ONE file sitewide, `software.js`** — where the JASP walkthrough tells readers to tick *Effect size* and the tip offers "*r* = *z*/√*N* (or the rank-biserial correlation from JASP)" as if the two were interchangeable. Nothing on the site defined either. §2.6 gained **"The effect size your software prints"**, anchored to the ledger widget's own default tab, whose readouts were then read back off the live page to confirm them: rank sums **23 / 55**, ***U*** **= 2**, ***z*** **= 2.48**, ***p*** **= .013**, **P(superiority) = .94**. So *r*<sub>rb</sub> = 2 × .944 − 1 = **.8889 → .89**, independently reproduced in R from `wilcox.test()`'s own *W* (**1 − 2U/n₁n₂ = 0.8889**), against ***r*** = *z*/√*N* = 2.4820/√12 = **.7165 → .72** — two different numbers for one dataset, which is exactly why the tip needed fixing. (4) **`hat matrix`, `studentized residual`, `DFBETA`, `DFFITS`, `White test`, `Huber` and `2p/n` all returned zero**, on the lesson whose widget prints a leverage readout. The find underneath was better than the absence: the widget graded the point "low / moderate / high" at hardcoded cutoffs of **0.15 and 0.28**, which are exactly **p/n = 2/14 = 0.1429** and **2p/n = 0.2857** for its 13 fixed points plus the dragged one — the textbook rule, encoded in the UI and stated nowhere — **and the "high" branch could never fire**, because the dragged point is clamped to x ∈ [5, 95] and tops out at **h = 0.2737**. The readout now prints the hat value and its multiple of average leverage (verified live: **0.07 (0.5× average)** parked at center, **0.26 (1.8×)** at the wall, and the arrow-key round trip returns exactly), and §3.8 gained **"How high is 'high'?"**: leverages sum to *p* (checked, **2.000000** across all 14 points), average leverage is p/n, the flags are 2p/n and 3p/n, and Cook's distance in closed form **D = (r²/p) × h/(1 − h)** is the lesson's own sentence "influence requires both" written as arithmetic. **One correction to the run's own first draft:** SPSS's *Casewise diagnostics* box flags on the plain **standardized** residual, not the studentized one, which is a separate tick under *Save…*; the prose and the new glossary entry both say so. (5) **`offset`, `log exposure`, `person-time` and `incidence rate` returned zero in the modeling sense** (all 17 `offset` hits were `offsetWidth`-style CSS/JS or the word in ordinary prose), on a lesson whose lede promises "counts, yes/no outcomes, and **rates**" and whose own bullet names a "multiplicative rate ratio". §4.4 gained **"Counts are not rates"**, worked on two clinics and **fitted in R rather than asserted**: 40 falls over 500 patient-days against 60 over 1,500 gives **exp(b₁) = 0.500 with `offset = log(days)`** and **1.500 without it** — opposite findings from one dataset. The section then closes the loop the lesson's own FAQ opens by referring to "deviance far exceeding its degrees of freedom" while the body never defines deviance: the deviance/df ratio is named, tied to the site's own practice problem (**412.6 / 178 = 2.318 → 2.32**), and calibrated on the widget's exact generator (N = 32, b₀ = 0.2, b₁ = 0.28, x ~ U(0,10)) by refitting it 20,000 times in R — **mean 1.0712, with 90% of fits between 0.67 and 1.55** — so the honest warning about how noisy the ratio is at that size ships with the rule. **Glossary: ten terms added (252 → 262), which was itself a finding.** The site teaches logistic regression through a link function with no glossary entry for *Link function*; §3.8 is entirely about leverage with no entry for *Leverage*; *Cook's distance* sits in six files with none. Added: Cook's distance, Deviance, Efficiency (of an estimator), Leverage (hat value h), Link function, Offset (in a GLM), Overdispersion, Rank-biserial correlation, Studentized residual, Unbiased estimator — each pointing at a ready lesson (verified: **0 entries point at a non-ready slug**), and each therefore also a new flashcard. **FAQ freshness: four swaps and one lesson deliberately left alone, scored with `faq-audit.js` and then judged by hand.** Out: §1.7's "Does every statistic have a sampling distribution?" (ECHO 67%, and a paraphrase of the lesson's own "Different statistics, different distributions" section — run 15's documented blind spot, where restating a section in fresh words scores REPRISE 0%); §1.8's "Why does the standard error shrink with the square root of n?" (REPRISE 6% and a near-verbatim restatement of the body's own "to halve the standard error you need four times the data", plus TWIN 29% with `sampling-distributions` #1); §3.8's "What is a high Cook's distance…?" (ECHO 33% against the new section that now carries the cutoffs, and answer TWIN **26%** with `regression-diagnostics` #3); and §4.4's "Which GLM family should I use for my outcome?" (ECHO 33%, a list restating the lesson's own "The family you already know"). In: "My estimator is unbiased. Doesn't that make it the right one to use?", "My data has a few enormous outliers. Will the CLT still rescue me?", **"Is there a real test for equal variances, or do I just eyeball the residual plot?"** — which fills a confirmed zero, since **`Breusch` appeared nowhere in prose and only inside `snippets.js` code** (`car::ncvTest()`, `het_breuschpagan()`), so the site ran a formal heteroscedasticity test in a snippet and never named it — and "My people were followed for different lengths of time. Can I just divide and model the rate?". **`stats-2/non-parametric-alternatives` was left at three untouched**: none restates an h2 or duplicates another page, and its Likert and "does Mann-Whitney compare medians" answers are the page's strongest search queries. Still **291 Q&As**, no duplicate question strings, and the opener census moved the right way (**"my-…" now 22, "what-is" down two**). **software.js: rotated to four untouched by runs 16–18** — `non-parametric-alternatives`, `regression-diagnostics`, `assumptions-and-when-they-break` and `logistic-regression` — against **SPSS 32.0.0 (GA 23 Apr 2026, still current)** and **JASP 0.98.1 (7 Jul 2026, still current)**, both re-checked against release notes this run rather than recalled. Every published statistic re-derived: Mann–Whitney **z = −2.13 → p = .0332 → .033**, Kruskal–Wallis **H(2) = 7.61 → p = .0223 → .022**, Levene **F(1, 58) = 8.42 → p = .0052 → .005**, logistic **Wald = (0.85/0.21)² = 16.38** against the printed 16.40 with **OR = e^0.85 = 2.3396 → 2.34** and **CI [1.5502, 3.5311] → [1.55, 3.53]** exact, and the model **χ²(2) = 28.7 → p = 5.9e-7**. The Mann–Whitney sentence's *U*, *z* and *r* were also checked for **mutual** consistency, which nothing on the site does: a grid search over n₁, n₂ shows **U = 245.5 with n₁ = 16, n₂ = 48 gives z = −2.147 and r = .268 → .27**, so a real design sits behind all three printed numbers. The SPSS claim that the Linear Regression dialog still has **no** heteroscedasticity-consistent standard-error option was re-verified against a current source (a 2026 *AMPPS* tutorial devoted to the workarounds) rather than memory. **No drift, so one fix rather than a rewrite:** the non-parametric tip offered *r* = *z*/√*N* "or the rank-biserial correlation" as alternatives, which §2.6 now shows are different numbers; it says so. Still 34 entries. **Snippets: three extended in step with the new sections** (`hatvalues`/`cooks.distance`/`rstudent` with their cutoffs and the statsmodels `get_influence()` equivalents; the rank-biserial straight from *U* in both languages; the GLM offset and deviance/df ratio), with **every R line executed here** on R 4.5.2 / car 3.1.5 exactly as shipped. `car::ncvTest()` and `car::durbinWatsonTest()` both run clean on the current car, so the existing diagnostics snippet carries no deprecation. **Python was not executed** (this machine has no scientific Python), so the Python additions use only documented, long-stable APIs and are flagged here as read-not-run. **Link health, measured with fragments stripped (run 17's bug), and the fallback branch of step 7 applied.** **No teaching page sits at 0 inbound** — the nine zeroes are `404`, `offline`, `index`, `quiz`, `progress`, `flashcards`, `teachers`, `toolbox` and `privacy`, all navigation or utility pages reached from the nav by design. **No lesson sits at 0 or 1 outbound** (the minimum across 97 is 2). So the job was the third question, dead ends, and §3.8 was the clearest: its "When assumptions fail" list named four remedies and **linked none of them**; all four now resolve (`data/transformations-and-recoding`, `stats-4/mixed-and-multilevel-models`, `stats-4/bootstrap-and-resampling`, `data/outliers-in-practice`), taking §3.8 from 3 outbound to 7. The two 1-inbound teaching targets run 17 flagged and run 18 left were both raised **where the link was genuinely natural and nowhere else**: `glossary.html` 1 → 3 and `guides/analyze-thesis-data-jasp` 1 → 2. Manufacturing three more of each was declined on step 7's own instruction to resist over-linking; that is a judgment a later run may revisit. **Quips: five reviewed, three kept, two rewritten**, both for the recurring defect of a closing clause that only restates the one before it — §1.8's "Nature loves a bell" restated the bell curve it followed and now lands the actual surprise ("Nobody asked the moods to cooperate"), and §4.4's third clause "Underneath, same capybara" was simply deleted, which is the shorter fix VOICE.md asks for. **Backlog item — no Backlog section exists (P37 is still unrun), so as in runs 11–18 the item became a checker, and this run's own absence (3) defined it.** **`tools/advice-terms.js`: does the site's own ADVICE name a thing its PROSE never teaches?** It is the advice counterpart to `widget-terms.js` — that one reads what a lesson's *interactive* prints, this one reads what the site writes in its own voice inside shared JS: `software.js`'s APA sentences and tips, and `checks.js`'s questions and whys. Both exist because `prose-lint.js` strips `<script>` before counting a word. **Two gates carry the precision and both were measured on the corpus, not chosen.** **Scope excludes the SPSS/JASP menu steps and all `snippets.js` code**, because a menu label and a function name are quotations from the software's interface and the site owes no explanation of `ZRESID` or `ncvTest` — including steps put exactly those on the list (**8 flags, ~2 genuine**), while a word in a tip is the site promising something. And the **stem gate**: a word is untaught only when the prose holds neither it nor any word sharing its first five letters, without which ordinary English compounds flood the report (`near-powerless`, `self-efficacy`, `between-stratum`, `non-existent`) — **7 flags without it, 3 with it, both genuine defects surviving**. Its **blind spot is in the header rather than smoothed over: an untaught term made of ordinary words is invisible**, which is why it would not have caught the Breusch–Pagan snippet comment that this run found by hand. On its first run over **393 advice strings** it found **two live defects**: `HC3`, offered as the fix for heteroscedasticity with nothing on the site saying what a heteroscedasticity-consistent standard error is, and `WCSS`, used cold in a k-means answer. Both are fixed (a paragraph in §3.8; the acronym spelled out in the check), leaving **1 acknowledged-benign flag** in `ACKED`. **Proved on two independent defect shapes:** removing §2.6's rank-biserial section fired `[FLAG] rank-biserial` and exit 1, and deleting §3.8's new robust-standard-error paragraph fired `[FLAG] HC3` and exit 1, both returning to exit 0 on restore with `git diff` confirming no test residue. It stays out of `audit.js` for prose-lint's reason. **Verification:** `audit.js` **0 errors / 20 warn / 64 info** (baseline restored after the index rebuild), `math-check.js` **270 assertions, 0 failures**, and `prose-lint --strict`, `faq-audit --strict`, `widget-terms --strict`, `link-promises --strict` and the new `advice-terms --strict` all **exit 0** — with **zero em-dashes added to any subject** (7 / 7 / 4 / 4 / 6, all unchanged, and §1.8's FAQ dashes down from 2 to 1) while those pages grew by **328–521 words** each. The diff read-back earned its place a third consecutive run: it found 11 em-dashes in added lines, every one pre-existing text swept up by a regenerated FAQ JSON-LD block or a code comment, which the per-page counts confirm — and it caught three things the linter cannot see: a "quiet source of overdispersion" (rule 9's reflex in adjective form, which `britspell`-style word matching misses), a §2.6 sentence saying the rank-biserial runs "from 0 to 1" two sentences before another said its sign matters, and a "Here is what that buys" drumroll. All three rewritten. **5 changed pages · 21 inline scripts · 10 JSON-LD blocks, 0 failures**; all shared JS, `sw.js`, `faq_data.py` and the new tool parse. Browser pass over all five changed pages via same-origin iframes at **1200px, 375px and 360px**: every expected string present, **0 console errors, 0 leading-slash links, 0 horizontal scroll at any width**, every lesson `h2` carrying an id, **all 9 new or touched link targets returning 200**, the new `.ref-table`s auto-wrapped in `.hscroll` and fitting their container, the §3.8 leverage readout round-tripping exactly under arrow keys, and §1.7's `?shape=bimodal&n=25&stat=median` preset applying all three params. The **widget itself reproduces the new claim**: 40 draws on Bimodal at n = 25 gave SE(median) **13.33** against SE(mean) **3.79**, the ~3× gap the table publishes. Dark mode verified on three changed lessons (bg `#0b1120`, `--link` `#6f7bf2`, the new tables legible at `rgb(167,179,201)`). **Index rebuilt (908 KB, 97 lessons, 37 pages) and searched for all 37 terms this run added: 37 HIT, 0 MISS**, each resolving to its own page. **`CACHE_VERSION` bumped sc-v37 → sc-v38** (two QUIPS changed and `site.js` is precached; `snippets.js`, `software.js`, `checks.js` and `glossary-data.js` also changed but are lazy-loaded and never precached). **Three notes for the next run.** Lap 1 ends with **one short run on the psychophysics pair**. **`apa.html` still emits no confidence interval** — seven runs old now and the oldest item outstanding. And a standing observation this run chose not to act on: `glossary.html` and `guides/analyze-thesis-data-jasp` sit at 3 and 2 inbound prose links, which is low for teaching pages, and the honest reason they are not higher is that forcing more would be over-linking; a run whose subjects genuinely need them should take them.
  - **28 Jul 2026 — eighteenth run (the Toolkit remainder, five lessons that state a principle and stop one step short of the machinery, and a checker for links that promise a term the destination never uses).** The ledger named the thread and this run took it: the three Ethics foundations `why-research-ethics` (§4.1), `informed-consent-and-irb` (§4.2) and `questionable-research-practices` (§4.5), plus `methods/sampling-methods` (§1.8) and `writing/final-checklist` (§3.8). **90 of 97 lessons have now been subjects, and the Research Toolkit track is finished outright.** They share a defect shape, which is what made them a coherent five: each names the right rule and stops before the procedure that implements it. **Five absences, each confirmed by a sitewide grep that excluded the five stale clones under `.claude/worktrees` and the generated `search-index.js`.** (1) **`design effect` returned ZERO hits sitewide**, along with `finite population correction`, `post-stratification`, `sampling weight` and `probability proportional` — on a lesson that calls cluster sampling "far noisier" and never says how much, while `stats-3/power-analysis-for-complex-designs` poses the exact question ("30 classes of 20 buys you far less than 600 independent pupils, and how much less depends on the intraclass correlation") and leaves the arithmetic open. §1.8 gained **"Putting a number on the cluster penalty"**, with Kish's `deff ≈ 1 + (m − 1)ρ`, the effective sample size `n_eff = n / deff`, and a table answering the power lesson's question: at m = 20, ρ of **.05 / .10 / .15 / .20** gives deff **1.95 / 2.90 / 3.85 / 4.80**, so 600 pupils are worth **308 / 207 / 156 / 125** independent ones. The lesson's own city was then measured rather than described: **simulating the widget's exact algorithm** (400 populations × 400 draws) gives SRS SD **.0501** against cluster SD **.2467**, a **design effect of 24.3** (closed form 23.84, ρ = **.2370**), so its 80 clustered answers carry the information of about **3** independent ones. `quota sampling` was a smaller find of the same kind — it appears on the site **only as a distractor in a quiz question**, defined nowhere — and now gets a sentence naming why it looks like stratification and is not. (2) **`alpha spending`, `Pocock` and `O'Brien` returned zero hits, and `sequential analysis` appeared only inside a `software.js` JASP step**, on the lesson whose own fix prescribes "planned interim analyses with corrected thresholds rather than ad-hoc peeking" and names not one of them. §4.5 gained **"Peeking, done properly"**, calibrated on the lesson's **own** Monte-Carlo engine (one-sample two-tailed *t* via `VIZ.tUpper`, 300,000 trials). Five looks at n = 20…100: honest **.0499**, naive peeking **.1446**, a **Pocock constant of .0154** and an **O'Brien–Fleming sequence of .0000049 · .0012 · .0083 · .0223 · .0409** — which reproduce the published group-sequential tables (**.0158** and **.000005 · .0013 · .0084 · .0225 · .0413**) to the third decimal, an independent confirmation that the simulation is right. The trade-off is the reason to bother and is stated as one: at a true *d* = 0.4, a single final test at n = 100 has power **.978**, O'Brien–Fleming **.975 stopping at an average of 65 participants**, Pocock **.951 at an average of 52**. The widget's own 10-look default gives naive **.1980** and a Pocock constant of **.0102**. The section also reconciles this lesson with the site's own JASP walkthrough for `bayesian-estimation`, which tells readers to tick *Sequential analysis* and watch evidence accumulate; no page had ever squared those two. (3) **`statcheck` sits in four places on the site** (`apa.html`, `problems.html`, `ethics/fraud-and-self-correction`, a guide) **and zero times in `writing/final-checklist`**, the lesson whose entire subject is the consistency pass; `GRIM` likewise. §3.8 gained **"Two of those sweeps are automated now"** with the Nuijten prevalence figures (**49.6%** of psychology articles reporting NHST contain at least one inconsistent *p*, **12.9%** one large enough to flip the decision, **9.7%** of all *p*-values, over 250,000 of them across ~16,700 articles) and a GRIM worked on the lesson's **own** mock paper: at n = 20 the possible means step by 1/20, so a reported **4.32 is impossible** (4.32 × 20 = 86.4), while the paper's **28.4 and 24.1 from groups of 60 are both reachable** (1,704 and 1,446 items) — the ordinary outcome, said out loud so that a clean pass does not read as a broken check. (4) **`waiver of consent`, `alteration of consent`, `broad consent`, `secondary data`, `archival data` and `publicly available data` all returned zero**, on a site that ships a practice-datasets library and a ten-lesson Data course. §4.2 gained **"When consent is altered, waived, or arrives late"**, with the five criteria of **45 CFR 46.116(f)(3)** verified against the regulation text, deception framed as an *alteration* rather than a waiver (which is what turns the debriefing from a courtesy into a condition of approval), and the three-way split for data you did not collect. (5) **`45 CFR` returned zero and all six `Common Rule` hits were "common rule of thumb"**; `nonmaleficence`, `APA Ethics Code`, `code of conduct` and `professional code` were also zero. §4.1 gained **"From principles to the rules you actually work under"**: the Common Rule as 45 CFR 46 Subpart A, adopted in **1991 by fifteen federal departments and agencies at once**, Subparts B/C/D, the **2018 revision effective 21 January 2019**, the fact that *IRB* is an American word with national equivalents, and the professional-code layer — the APA's five General Principles (2002, amended 2010 and 2017), where Belmont's beneficence splits into **Beneficence and Nonmaleficence** and two principles appear that Belmont has no counterpart for, one of them **integrity**, which is why QRPs and authorship disputes are ethics violations with no harmed participant anywhere. Every regulatory and historical claim was checked against a source this run rather than recalled. **Four shipped defects, three of them cross-links that resolve perfectly and lie.** (a) **`stats-4/mixed-and-multilevel-models` contained ZERO occurrences of "intraclass correlation" or "ICC"** while `stats-3/power-analysis-for-complex-designs` sends readers there for exactly that. `git log -S` found the cause: **P39 run 2 swapped away the FAQ answer that held the term**, sixteen runs ago, and nothing has noticed since. Restored as prose where it belongs, tied to the widget's own controls — σ is fixed at 11 so the τ slider is an ICC dial, and at its default **τ = 8 the ICC is .3459 → .35**, at τ = 1 **.0082**, at τ = 30 **.8815**, all three confirmed in-browser against the live slider's real default and range. (b) `ml/train-test-split-and-generalization` linked the words **"k-nearest-neighbors" to the REGULARIZATION lesson**; `ml/knn-and-distance` exists. (c) `data/data-entry-and-validation` linked **"GRIM" to `methods/the-replication-crisis`**, which never mentions it; GRIM is taught in `ethics/fraud-and-self-correction`. (d) `stats-4/bayesian-thinking` only ever writes "Bayes' rule" while `stats-1/probability-basics` links the words "Bayes' theorem" at it; it now says once that the two names are the same statement. **FAQ freshness: four swaps and one lesson deliberately left alone, scored with `faq-audit.js` and then judged by hand.** Out: §4.1's "What are the three principles of the Belmont Report?" (**ECHO 67%** against its own h2, with the answer's list sitting directly beneath it); §4.2's "What's the difference between exempt, expedited, and full-board review?" (**ECHO 0% and REPRISE 11%, and still a restatement of the lesson's own three-bullet list** — precisely the blind spot run 15 documented, where paraphrasing a list scores clean); §4.5's "How do I avoid questionable research practices in my own study?" (**ECHO 60% / REPRISE 45%**, the highest on the page, restating "The fix" point for point); and §3.8's "Why do my degrees of freedom matter to the grader?" (**ECHO 50% / REPRISE 39% / TWIN 40%** with `one-sample-and-paired-t-tests` #2, the weakest answer on the page by every measure). In: "Is research ethics the same thing as research integrity?" (**`research integrity` had zero hits sitewide**), "Do I need ethics approval to analyze a dataset someone else collected?", "I ran the analysis six ways. Which ones do I report?" (which also drops §4.5 from two "what is the difference" openers to one), and "A checker says my p-value is inconsistent, but my output disagrees. Which one is wrong?" — whose worked case is verified and genuinely useful: **t(48) = 2.01 reported to two decimals implies a true statistic anywhere in [2.005, 2.015] and therefore a true *p* anywhere in [.0495, .0506]**, which straddles .05, so a rounding-band flag and a decision-changing flag are different things. **Two further replacements were drafted and dropped after grepping `faq_data.py` rather than shipped**: a preregistration-deviation question would have twinned `preregistration-and-open-science` #3, and an outlier-deletion question `outliers-in-practice` #2. **`methods/sampling-methods` was left at three untouched** — none restates an h2 or duplicates another page, and its two moderate-REPRISE answers are the page's strongest search queries. Still **291 Q&As**, no duplicate question strings. **software.js: rotated to four untouched by runs 15–17** — `independent-samples-t-test`, `repeated-measures-anova`, `power-analysis-for-complex-designs` and `meta-analysis` — against **SPSS 32.0.0 (GA 23 Apr 2026, still current)** and **JASP 0.98.1 (7 Jul 2026, still current; its release note is a Repeated Measures ANOVA bug fix plus help-file translations, with no menu rename anywhere)**. The SPSS 31+ claim that Levene's test became opt-in was re-confirmed, as was the `meta-analysis` entry's note that JASP 0.96 moved modules to an online library. Every statistic re-derived: Welch **t = 2.3025 → 2.30, df 57.67 → 57.7, p = .0249 → .025, d = .5945 → .59, CI [0.614, 8.786] → [0.6, 8.8]**; Mauchly **χ²(2) = 9.4 → p = .0091 → .009**, **F(1.56, 45.2) = 8.75 → p = .00146 → .001**, **ηp² = .2318 → .23** with ε = .78 giving exactly 1.56 and 45.2 from (2, 58); meta **Q(17) = 38.20 → p = .0023 → .002, I² = 55.5% → 55%**, and z = 5.90 consistent with its own CI; and the 2 × 3 power sentence's **N = 158 reproduced exactly** by noncentral F (df1 = 2, 6 cells, f = .25, α = .05 → power .8020, and .7993 at 157). **No drift, so one addition rather than a fix:** G*Power returns the smallest total N and does not check that it divides into your cells, so the 158 fills six cells of 26.3 and a balanced 2 × 3 needs **162** — now a tip. Still 34 entries. **Snippets: `sampling-methods` extended in step with the new section** (cluster draw, design effect and effective n in both languages), with **every R line executed here** on R 4.5.2 / dplyr 1.2.0 exactly as shipped (100 rows, deff 3.4, n_eff 29.4). **One genuine deprecation fixed**: its Python stratified sample used `df.groupby(...).apply(lambda g: g.sample(...))`, which has emitted a DeprecationWarning since pandas 2.2 and whose grouping-column behavior is being removed; `df.groupby("stratum").sample(n=20, random_state=1)` is the same thing and is not deprecated. **Link health, measured, and this run's count agrees with run 17's corrected one** (`problems.html` **56** inbound). **No teaching page sits at 0 inbound**; the four zeroes (`quiz`, `progress`, `flashcards`, `teachers`) are navigation and utility pages reached from the nav by design. The weakest teaching target was **`which-test.html` at 1 inbound**, flagged and deferred by run 17, and it was cleared this run with **five natural placements** (visualizing-data, assumptions-and-when-they-break, non-parametric-alternatives, from-question-to-hypothesis, variables-and-operationalization) taking it to **6**. On the outbound side **no lesson sits at 0 or 1**, and `ethics/why-research-ethics` — run 17's weakest lesson at one outbound link, deliberately left for the run that took it as a subject — now has six. **Quips: five reviewed, three kept, two rewritten**, both for the recurring defect of a closing clause that only restates the one before it: §1.8's ended "Also: sample the whole pond next time" after the joke had landed and now lands the lesson's actual point ("Ask a thousand more of them and it is still 100%"), and §3.8's flat "Works every time" was simply deleted, which is the shorter fix VOICE.md asks for. **Backlog item — no Backlog section exists (P37 is still unrun), so as in runs 11–17 the item became a checker, and this run's own defect (a) defined it.** **`tools/link-promises.js`: does a cross-link deliver the term its wording promises?** `audit.js` proves every link resolves and every `#fragment` names a real id; nothing checks that the concept in the anchor text exists at the destination. **The rule:** reduce an anchor to its single most **distinctive** content word (lowest document frequency sitewide) and require that word in the destination's rendered prose — anchors almost always paraphrase, so the whole phrase is hopeless and *any* word is toothless, because the common word in a paraphrase is on nearly every page. **Four broader definitions were measured against the corpus and discarded before the fifth worked, which is the part worth recording.** Deleting hyphens rather than splitting on them gave **174** flags of hyphenated test names (squashing "independent-samples" into one token never matches a page that writes "Independent Samples" with a space, and the site does exactly that in several lesson titles); keeping card-sized and sentence-length anchors gave **78**; dropping the glossary gate gave **40**, almost all ordinary English words that happen to be rare here ("scratch", "chooser", "lean", "untidy") — the promise word must name a concept the site actually teaches, and `glossary-data.js` is already that list; and widening the promise to the six words **before** the link gave **71**, because a mid-sentence fragment promises nothing coherent. The surviving rule reports **2 flags over 472 prose links**, both genuine paraphrases, both in `ACKED`. **Its blind spot is stated in the header rather than smoothed over, and it is a real one: the promise has to be in the anchor text**, and the original `stats-3` link wraps its words around an anchor reading "multilevel models", which the target does contain — so this checker would not have caught the exact instance that inspired it, and the lead-in experiment above is why it does not try. What it does catch is the same defect wherever the promise is the thing clicked, and on its first run it found defects (b) and (c) above, **two mis-targeted links nobody knew about**, entirely on its own. **Proved on two independent defect shapes**: deleting the new ICC section fired `intraclass` and exit 1, and re-pointing the GRIM link fired `grim` and exit 1, both returning to exit 0 on restore, with `git diff` confirming no test residue. It stays out of `audit.js` for prose-lint's reason. **Verification:** `audit.js` **0 errors / 20 warn / 64 info** (baseline restored after the index rebuild), `math-check.js` **270 assertions, 0 failures**, `prose-lint --strict` **exit 0**, `faq-audit --strict` **exit 0**, `widget-terms --strict` **exit 0**, `link-promises --strict` **exit 0** — with **zero em-dashes added to any subject** (6 / 6 / 3 / 3 / 3, and §4.1 went *down* from 5 to 3) while those pages grew by 280–560 words each. The diff read-back found 7 em-dashes in added lines and every one proved to be pre-existing text swept up by a regenerated FAQ block or by a sentence appended to an existing paragraph, which the per-page counts confirm; it also caught a tell the linter cannot see, three paragraphs across three lessons opening "Two X…", and the shortest fix deleted one of them. **14 changed pages · 57 inline scripts · 28 JSON-LD blocks, 0 failures**; all shared JS, `sw.js`, the six tools and `faq_data.py` parse. Browser pass over all 14 changed pages via same-origin iframes at **1200px, 375px and 360px**: every expected string present, **0 console errors, 0 leading-slash links, 0 horizontal scroll at any width**, every lesson `h2` carrying an id, **all 17 new or re-pointed link targets returning 200**, and the sampling-methods slider round-tripping exactly (frozen-noise law intact). Dark mode verified on three changed lessons with `localStorage.theme` set before load per run 16's note (bg `#0b1120`, `--link` `#6f7bf2`, canvases painted, and the two new `.ref-table`s legible). **Index rebuilt (892 KB, 97 lessons, 37 pages) and searched for all 30 terms this run added: 30 HIT, 0 MISS**, each resolving to its own page. **`CACHE_VERSION` bumped sc-v36 → sc-v37** (two QUIPS changed and `site.js` is precached; `snippets.js` and `software.js` also changed but are lazy-loaded and never precached). **Three notes for the next run.** The remainder of lap 1 is **entirely Core**, and the ledger names the arc. **`apa.html` still emits no confidence interval** — six runs old now, and the last holdout. And a standing observation this run chose not to act on: §4.1's Tuskegee FAQ scores **REPRISE 42%**, the highest on that page, and was kept anyway because it is the exact query students type and the page's strongest search magnet; a future run may disagree, and should say so rather than swap it silently.
  - **28 Jul 2026 — seventeenth run (the ML geometry spine, the counter the site showed and never named, and three rejected checkers before the one that worked).** The ledger named the thread and this run took it: the four never-opened ML lessons read as one arc about distance and shape — `knn-and-distance` (§5.8), `clustering-kmeans` (§5.9), `dimensionality-reduction` (§5.10), `neural-networks-intuition` (§5.11) — plus `data/data-privacy-basics` (§2.10) as the fifth, which is not a stretch: the classic re-identification results are a nearest-neighbour attack on high-dimensional sparse data, and the lesson's own demo shows uniqueness rising with every column kept. **85 of 97 lessons have now been subjects, and the ML course is finished.** **Five absences, each confirmed by a sitewide grep that excluded the five stale clones under `.claude/worktrees` (they make a term look present when only a dead copy has it — worth remembering, since step 2's greps are this loop's most valuable instrument).** (1) **`Manhattan distance`, `cosine similarity`, `Minkowski` and `Gower` all returned zero hits**, on the lesson titled "k-NN & Why Distance Gets Weird" whose closing paragraph tells readers to reach for "a distance metric built for your data's structure" and then names none. §5.8 gained **"Which distance?"**, worked on a pair chosen so the two rulers disagree completely: against a profile of (5, 4, 5, 4, 5), a same-shape case at a fifth of the intensity sits at Euclidean **8.28** while a different-shape case at similar intensity sits at **5.66**, so Euclidean calls the same-shape case the far one — and cosine reverses it exactly, **1.000 against 0.843**. The section also carries a measured table of distance concentration (median over 400 reps, 500 uniform points): the farthest point is **47×** the nearest at *d* = 2, **3.4×** at 10, **1.16×** at 500, with Manhattan holding marginally more contrast at every dimension (**1.20 against 1.16** at 500), which is why L1 is recommended for wide data and why the section ends by calling it a tie-breaker rather than a rescue. (2) **`hierarchical clustering`, `dendrogram` and `linkage` returned zero hits sitewide**, on the lesson whose entire pain point is that you must name *k* before it will run. §5.9 gained the method that does not, and the number that matters was **run rather than asserted**: on two interlocking moons of 100 points each, **single linkage recovers both moons perfectly (100%) where k-means with 25 restarts manages 73%** — but complete, average and Ward all land near **82%**, so the honest moral is not "hierarchical instead of k-means" but that a chaining linkage can follow a curve and centroid methods cannot, with single linkage's own weakness (one stray point bridges two groups) named in the same breath. (3) **`component scores` and `principal component regression` returned zero hits**, and `stats-3/multicollinearity-and-variable-selection` has a "What to do about it" section containing **zero mentions of PCA** while the site teaches PCA in two separate lessons. §5.10 gained **"Putting the components to work"**, verified in R: four predictors, three of them near-copies, carry **VIFs near 7** raw and **exactly 1.000** as components (largest surviving correlation between scores **1.4e-15**), and the blind spot is quantified rather than hand-waved — **PC1 holds 70.7% of predictor variance and correlates .54 with the outcome while PC2 holds 24.4% and correlates .47**, and keeping all four reproduces the raw model's *R*² to the last digit (**.5238** both ways), so nothing is gained until you drop components and dropping is where a component that mattered for *y* goes out with the trash. (4) **`ReLU`, `rectified linear`, `softmax`, `vanishing gradient`, `mini-batch` and `stochastic gradient` all returned zero hits — and `epoch` appeared in exactly one file, `neural-networks-intuition` itself, only inside its own widget's status line.** The lesson shows the reader a live counter reading "Epoch 412 · loss 0.031" and never once says what an epoch is. §5.11 gained **"The words you meet in real code"**, and the run also **corrected the lesson's own definition**: it called an activation "a squashing function that maps it to a bounded output", which is false for ReLU, the default everywhere. ReLU's win is quantified — the sigmoid's slope peaks at exactly **0.25**, so ten layers shrink the gradient by at most **0.25¹⁰ ≈ 9.5e-7** while ReLU's positive-side slope is exactly 1 — and the epoch paragraph is anchored to the widget the reader is looking at (**56 training points, one update per pass, full-batch**, verified by reading `trainStep()`, which also revealed the hidden layer is **tanh**, a third activation the prose never named). (5) **`differential privacy`, `l-diversity` and `homogeneity attack` returned zero hits**, on a lesson that displays a sensitive diagnosis column beside a *k*-anonymity readout and tells readers higher *k* is safer. §2.10 gained **"What k-anonymity does not protect"**, and the numbers come from the lesson's **own shipped 30-row table**: generalizing both age and ZIP gives 15 groups of two, so *k* = 2 and every row turns green, yet **an attacker who pins you to your pair names your diagnosis 50% of the time against a 1-in-6 base rate** (the most common diagnosis is 5 of 30); matching on sex and ZIP region alone lifts *k* to 4 and the odds only fall to **36.7%**. Identity is bounded, the sensitive attribute is not. Differential privacy is named as the modern answer, with the 2020 US Census (TopDown) as its landmark deployment. **Three shipped defects on the privacy lesson, one of them a wrong human name.** The body called her **"Latham Sweeney"**; she is **Latanya Sweeney**, spelled correctly in the lesson's own FAQ two screens below. Worse, the site stated **"roughly 87% of Americans"** as unqualified fact in three places, and that figure is from the **1990** census: **Philippe Golle's 2006 re-analysis on 2000 census data puts it at 63%** (confirmed against two independent sources, not memory). All three now carry the lower number with the census year attached, including `ethics/privacy-and-confidentiality`'s FAQ, which is not a subject this run but shares the claim. And the lesson still promised that "a whole course of this toolkit is being built around" research ethics — **the Ethics course has been complete since P13**; it now links to §4.4. **FAQ freshness: three swaps and one extension, on merit rather than quota, scored with `faq-audit.js` and then judged by hand.** Out: §5.8's "What is the curse of dimensionality?" (**ECHO 100%** — it restated its own h2 verbatim), and two on §2.10 — "Is removing names enough to anonymize data?" (**TWIN 38% question / 22% answer** with `ethics/privacy-and-confidentiality` #2, "Is deleting participants' names enough to anonymize a dataset?", which is the same question on two pages) and "What is k-anonymity?" (**ECHO 100%** against its own h2, REPRISE 20%). In: "My training set has 200,000 rows. Will k-NN still work?" (**`KD-tree` and `ball tree` had zero hits**; the answer names the ~20-feature ceiling past which a spatial index stops beating brute force), "Should I be using differential privacy?" (honest answer: for a thesis, no) and "My dataset is k-anonymous. Is it safe to share?". `clustering-kmeans` #2 was **extended rather than swapped** — it recommended DBSCAN and spectral for curved shapes and, after this run, was the only place on the lesson that did not know single linkage also works. **§5.10 and §5.11 were left at three untouched**, and the check that made that call is worth recording: §5.10 #1 ("Is PCA the same as factor analysis?") looked like a cross-page duplicate until `factor-analysis-pca`'s own three FAQs were read and found to cover keeping components, rotation and loadings, none of them this. Still **291 Q&As**, no duplicate question strings. **software.js: rotated to four untouched by runs 14–16** — `factor-analysis-pca`, `manova`, `post-hoc-tests`, `reliability-and-validity` — against **SPSS 32.0.0 (GA 23 Apr 2026, still current; its additions are the AI Output Assistant, Mediation Analysis, VAR forecasting and a Linux installer, none of which touch Dimension Reduction → Factor, GLM → Multivariate, One-Way ANOVA → Post Hoc or Scale → Reliability)** and **JASP 0.98.1 (7 Jul 2026, still current; the release notes for 0.96 → 0.98.1 document no rename or relocation of EFA/PCA, MANOVA, Post Hoc, Unidimensional Reliability or Inter-Rater Reliability)**. Every statistic re-derived: MANOVA's Pillai .24 gives exact **F(2, 77) = 12.105** against the printed 12.10, its follow-ups **p = .0038 → .004** and **.0822 → .082**; post-hoc **F(3, 116) = 7.21 → p = 1.8e-4** with **η² = .1572 → .16**; §3.10's **χ²(190) = 1438.2** has df exactly 20×19/2 for its 20 variables. **No drift found, so no edits — the honest outcome of a rotation.** Still 34 entries. **Snippets: four extended in step with the new sections** (hierarchical clustering in both languages, `metric="manhattan"`, component scores into `lm()`, and a comment naming `max_iter` as epochs beside the `activation="relu"` default), and **every R line was executed here** on R 4.5.2 before it shipped. **Link health, measured, and a measurement bug worth recording:** the first pass reported eight pages at zero inbound, which was wrong — it compared whole `href`s, so every link carrying a `#fragment` was dropped, and `problems.html` actually has **56** inbound lesson links, not zero. Corrected, **no teaching page sits at 0 inbound**; the six genuine zeroes (`quiz`, `progress`, `toolbox`, `flashcards`, `teachers`, `privacy`) are navigation and utility pages reached from the nav by design, and linking them from lesson prose would be noise. The weakest teaching targets are `which-test.html`, `glossary.html` and `guides/analyze-thesis-data-jasp` at **1 inbound each**, and the weakest lesson is `ethics/why-research-ethics` at **1 outbound** — deliberately left for the run that takes it as a subject rather than fixed by a drive-by link, since none of this run's five subjects can cite them naturally. This run's own prose added three cross-links that did not exist before, including the site's first `data` → `ml` link and the first ML → `multicollinearity` link. **Quips: five reviewed, three kept, two rewritten**, both for the recurring defect of a closing clause that only restates the one before it — §5.9's ended "Clusters are hypotheses, not facts" after the joke had already landed and now lands the lesson's real pivot instead, and §2.10's ended "Anonymity is harder than it looks" after a sentence that had already shown it. **Backlog item — no Backlog section exists (P37 is still unrun), so as in runs 11–16 the item became a checker. Three candidates were built, measured against the corpus, and REJECTED before the fourth worked, which is the part worth recording.** A **stale-forward-promise scanner** (from the "course being built" defect) found 4 sentences, *all* false positives ("as soon as possible", "research is planned"), with the single real instance already fixed. A **cross-page inconsistent-number detector** flagged 2 groups, both false positives, and would not have caught this run's defect anyway: the 87% was **consistently** wrong in all three places, not inconsistently stated. An **eponym roster** was abandoned once the corpus showed the site uses a given name exactly **twice** in 137 pages. The fourth is **`tools/widget-terms.js`**: does a lesson's interactive print a quantity its prose never explains? It reads the string literals a widget writes to `textContent`/`innerHTML` and flags capitalized labels concatenated with a computed value (`"Epoch " + epoch`) that appear nowhere in the lesson's prose. **Two properties are load-bearing and were both learned by testing against the pre-fix tree, not assumed:** widget chrome must be stripped from "prose" (the widget's own `<span>Epoch 0 · untrained</span>` otherwise counts as the lesson explaining it, which is exactly how the defect hid — without the strip the scan reports nothing at all), and **progress counters are excluded by shape rather than by name** (`"Score: " + n + " / " + total` keys on the `" / "` literal, so it generalizes to tallies nobody has written yet). Three broader definitions were measured and discarded first: every word in every rendered literal gave **282** flags of CSS class names and ordinary English, every capitalized word gave **249** dominated by button verbs, and filtering sentence-initial words dropped to 2 but **missed "Epoch"**, which sits at the start of its own literal. It ships with **4 acknowledged-benign flags** across 97 lessons (two example-data values, two self-test tallies) rather than a filter over-fitted to today's corpus, and its blind spot (lowercase labels) is documented in the header instead of smoothed over. **Proved on two independent defect shapes:** deleting the new epoch paragraph from the real tree fired `[FLAG] ml/neural-networks-intuition → Epoch` and exit 1; making `clustering-kmeans`'s widget report an undefined "Inertia" fired that and exit 1; both returned to exit 0 on restore, and `git diff` confirmed no test residue. It stays out of `audit.js` for prose-lint's reason. **Verification:** `audit.js` **0 errors / 20 warn / 64 info** (baseline restored after the index rebuild), `math-check.js` **270 assertions, 0 failures**, `prose-lint --strict` **exit 0**, `faq-audit --strict` **exit 0**, `widget-terms --strict` **exit 0** — with **zero em-dashes added to any subject** (8 / 7 / 7 / 6 / 4, unchanged, while those pages grew by 240–390 words each) and FAQ em-dashes *down* on two lessons. The diff read-back earned its place again, though differently: it surfaced 12 em-dashes in added lines that all proved to be pre-existing text swept up by regenerated FAQ blocks and rewritten paragraph lines, which the per-page counts confirmed. **6 changed pages · 24 inline scripts · 12 JSON-LD blocks, 0 failures**; all shared JS, `sw.js`, the five tools and `faq_data.py` parse. Browser pass over all six changed pages via same-origin iframes at **1200px, 375px and 360px**: every expected string present, **0 console errors, 0 leading-slash links, 0 horizontal scroll at any width**, every lesson `h2` carrying an id, **all 7 new/touched link targets returning 200**, the k-NN slider round-tripping exactly (frozen-noise law intact), the XOR widget stepping Epoch 0 → 100, and the privacy demo reproducing **k = 1 / 30 unique → k = 2 / 0 unique**, the very configuration the new 50% claim is computed from. Dark mode verified on three changed lessons (bg `#0b1120`, `--link` `#6f7bf2`, canvases painted) with `localStorage.theme` set before load, per run 16's note. **Index rebuilt (878 KB, 97 lessons, 37 pages) and searched for all 29 terms this run added: 29 HIT, 0 MISS**, each resolving to its own lesson. **`CACHE_VERSION` bumped sc-v35 → sc-v36** (two QUIPS changed and `site.js` is precached; `snippets.js` also changed but is lazy-loaded and never precached). **Three notes for the next run.** The **Ethics foundations are the coherent five** and would finish the Toolkit track. **`apa.html` still emits no confidence interval** — now five runs old, and run 16 made the lesson, its drill and `software.js` all oblige it, so the tool is the last holdout. And run 16's strongest checker candidate is still unbuilt and still looks right: **extending `audit.js` CHECK 9 to page prose and inline scripts**, whose false-positive taxonomy run 16 already worked out; this run did not take it because CHECK 9's defect class was not the class this run found by hand.
  - **27 Jul 2026 — sixteenth run (the Writing write-up spine, the confidence interval the site printed everywhere and taught nowhere, and the end of the hand-written spelling inventory).** The ledger named the thread and this run took it: five of the six never-opened Writing lessons, read as one arc — `imrad-structure` (§3.1), `reporting-statistics-apa` (§3.2), `nonsignificant-results` (§3.5), `discussion-and-limitations` (§3.6), `abstracts-and-titles` (§3.7). **80 of 97 lessons have now been subjects.** **Five absences, each confirmed by a sitewide grep, and every published number verified in both `VIZ` and R before it was published.** (1) **The APA confidence interval was the site's largest used-but-untaught term.** `writing/reporting-statistics-apa` contained the string "CI" **zero times**, `cheat-apa.html` zero, `apa.html` zero — while twelve files print `95% CI [...]`, two `checks.js` questions test a student's reading of one, §3.5's key callout calls it "the whole message", and §3.7's abstract-grader rewards the sentence `r = .28, 95% CI [.11, .43]` as its winning answer. Run 13 had already noted §3.2 "never says interval" and left it, since §3.2 was not its subject. §3.2 gained **"Where the confidence interval goes"**: the `95% CI [lower, upper]` form, the level stated once for a series (APA's own example `95% CIs [−7.2, 4.3], [9.2, 12.4], and [−1.2, −0.5]`), the fact that **in a table APA drops the brackets for LL/UL columns with the level in the header** (checked against APA's published sample tables, not memory), the rule nothing on the site taught — **an interval inherits the leading-zero rule of whatever it bounds** — and the reminder to name what the interval is *on*. The worked trio comes from the lesson's own master-patterns table and was computed twice: *d* = 0.96 → **95% CI [0.42, 1.49]** by exact noncentral *t* (bisection on `VIZ.nctCdf`, cross-checked against R's `pt(..., ncp=)` — agreement to 5 dp; the large-sample approximation gives [0.43, 1.49]), *r*(58) = .34 → **[.09, .55]** by Fisher *z*, *b* = 0.42 / *SE* = 0.14 → **[0.14, 0.70]**. One drafted sentence was **wrong and the release notes caught it**: I had written that neither SPSS nor JASP hands you an interval on η², and JASP's ANOVA does, from its effect-size options — the paragraph now says so. (2) **`TOST` and `two one-sided tests` returned zero hits**, on the lesson that names "equivalence testing" as its own idea and stops. §3.5 gained the procedure, run on the lesson's **own two studies**, whose per-group *n* I derived from the published intervals and only afterwards found stated in the lesson's interactive (40/group and 200/group — they matched). Against a bound of ±0.3: the tight null (*d* = 0.05, 200/group) gives **TOST *p* = .006, equivalence declared**; the wide one (*d* = 0.15, 40/group) gives **TOST *p* = .25, nothing declared** — same "not significant", opposite answers. The section also lands the fact everyone gets wrong, with the arithmetic to back it: at α = .05 TOST succeeds exactly when the **90%** interval fits inside the bounds ([−0.11, 0.21] vs [−0.22, 0.52]), because two one-sided tests each spend 5% in one tail. (3) **`common method` and `common-method` returned zero hits**, on the "limitations with teeth" lesson whose own running example is a single-questionnaire self-report correlation. §3.6 gained it, and the algebra was **simulated rather than asserted** (N = 500,000 per cell, agreement to 3 dp): with a shared method factor carrying share *m* of each measure's variance and true correlation ρ, the observed *r* is (1 − *m*)ρ + *m*, so **at ρ = 0 the observed correlation is exactly *m*** — a tenth manufactures *r* = .10 out of nothing, a quarter manufactures *r* = .25. Turned on the lesson's own *r* = .28: a 10% shared method implies a true **.20**, a 25% one implies **.04**. (4) **`JARS`, `Journal Article Reporting Standards` and `reporting standards` returned zero hits**, and nowhere did the site name the Method section's subsections, on the lesson that tells students to write "enough detail that a stranger could repeat the study". §3.1 gained the list, **taken verbatim from APA's own JARS-Quant Table 1 PDF** (inclusion and exclusion, participant characteristics, sampling procedures, sample size/power/precision, measures and covariates, conditions and design, analytic strategy incl. missing data and experiment-wise error) — which also made it the run's biggest cross-link hub, pointing at seven existing lessons and `power.html`. (5) **`structured abstract` returned zero hits**, and nothing said how a keyword line is formatted. §3.7 gained both, the keyword rules quoted from APA's Abstract and Keywords Guide (indented, italic label, three to five terms, lowercase, comma-separated, no closing period). **Four shipped numeric defects, three of them found by a throwaway probe that ran `audit.js` CHECK 9's own pairing logic over two surfaces it does not scan — page prose and inline-script literals.** (a) **`95% CI [.11, .43]` was wrong in four places** (§3.6's result card, §3.7's winning sentence, `checks.js`, `quiz.html`): *r* = .28 at *n* = 120 gives **[.1061, .4373] → [.11, .44]**, confirmed identically by `VIZ` and by R's `cor.test`. (b) §3.2's master-patterns regression row read `b = 0.42, SE = 0.14, β = .29, t(96) = 3.10, p = .002`, but **0.42 ÷ 0.14 = 3.00**, and neither 3.00 nor 3.10 gives *p* = .002 — now `t(96) = 3.00, p = .003`. (c) `writing/final-checklist` reported `t(116) = 2.84, p = .006` where the attainable range is [.00525, .00541] **even after the reader fixes the planted df error to 118** — and *p* was not one of its six planted errors, so an exercise about finding inconsistencies carried an unintended one. Now `.005`. (d) `apa.html`'s static example claimed `t(87) = 3.40, p < .001` where *p* = .00102. Now `p = .001`. **Two more were inside §3.2's own drill**, which nothing checks because CHECK 9 does not read inline scripts: its *corrected* paragraph asserted `t(58) = 3.72, p = .001` (true *p* = .00045) and `r(58) = .34, p < .001` (true *p* = .008). Both fixed by making the drill's own numbers true (`p < 0.001` → `p < .001` as the leading-zero error; the correlation raised to .44, whose *p* = .00044 really is < .001), and the drill **gained an eleventh error teaching the new rule** — `95% CI [0.21, 0.62]` → `[.21, .62]`, verified by Fisher *z*. Driven end-to-end in-browser: 11/11, and the final paragraph is now internally consistent in every statistic. **FAQ freshness: five swaps, scored with run 15's `faq-audit.js` and then judged by hand.** Out: §3.2's leading-zero question (**ECHO 100%** — it restated its own h2 verbatim) and its `p = .000` question (**TWIN 40% question / 27% answer with `writing-results`**); §3.5's "does non-significant mean no effect" (ECHO 50% + the lesson's highest REPRISE at 34%); §3.6's "how many limitations" (two of that lesson's three answers were about limitations and none about the Discussion's other four moves); §3.7's "write it first or last" (ECHO 50% and a near-duplicate of §3.1's answer). In: "What is the difference between N and n?", "My department uses a different style guide. Do these rules still apply?" (**`style guide` had zero hits sitewide**), "I want to claim there is no effect. Equivalence test or Bayes factor?" (JASP's Equivalence T-Tests module confirmed from JASP's own release notes), "My result contradicts a published study. How do I write that?" (the lesson's recipe raises "agree, extend, or contradict" and never answers it), and "Should my abstract mention limitations?". §3.5's surviving first answer also **carried an inconsistent example** — `t(58) = 1.30, p = .20, d = 0.24, 95% CI [−0.13, 0.61]`, where *t* and df give *d* = 0.34 and the interval implies ~57/group — now `d = 0.34, 95% CI [−0.18, 0.84]` (noncentral, R-checked). Still **291 Q&As**, no duplicate question strings. **Two candidate replacements were dropped after grepping rather than shipped**: a post-hoc-power FAQ (already covered in the power lesson, its FAQ and a practice problem) and a Mann–Whitney notation FAQ (`software.js` already shows the form). **software.js:** none of the five subjects has an entry, correctly. Rotated to four not checked in runs 13–15 — `correlation`, `simple-linear-regression`, `one-way-anova`, `chi-square-tests` — against **SPSS 32.0.0 (GA 23 Apr 2026, still current; its additions are the AI Output Assistant, forecasting and a Linux installer, none touching Correlate → Bivariate, Regression → Linear, Compare Means → One-Way ANOVA or Crosstabs)** and **JASP 0.98.1 (7 Jul 2026, still current; the 0.97–0.98.1 notes document no menu renames)**. Their numbers were re-derived and all hold (`r(58) = .42, 95% CI [.19, .61]`; `b/SE = 4.05`, `t² = F = 16.40`, `R² = .25`; χ²(1) = 6.25 → *p* = .012, φ = .23, and its 68%/45% percentages are reachable at N = 120 on an unequal split). Two edits, both obliged by the new §3.2 prose: `one-way-anova`'s JASP step now ticks **Confidence intervals beside the effect-size estimates**, and `simple-linear-regression`'s APA sentence — the site's model regression sentence, which told readers a coefficient has "no excuse" for lacking an interval while lacking one itself — gained **95% CI [1.16, 3.46]** (2.31 ± 2.01063 × 0.57, i.e. *b* ± *t*.975,48 × *SE*, R-verified). Still **34 entries**. **Snippets:** `simple-linear-regression` gained `confint(fit)` and `fit.conf_int()`, the same obligation; the R lines were **executed here** (R 4.5.2) at both the default and 0.99 levels. **Link health, measured.** Inbound counts recounted for all 26 tools, guides and posters: **no content page sits at 0 inbound**, so this run inherited step 7's second question and cleared it. `ml/clustering-kmeans` was the last lesson with **zero outbound prose links** and got three (distance → `knn-and-distance`, within-cluster spread → `one-way-anova`, stability → `cross-validation-and-overfitting`); `stats-1/central-limit-theorem` and `ethics/informed-consent-and-irb` were the last two at one. **All 97 ready lessons now have at least two outbound prose links, and none is a dead end.** **Quips:** five reviewed, three kept, two rewritten — §3.1's had an awkward middle clause that the shortest fix simply deleted, and §3.2's traded a padded "would never, ever" for the interval the lesson now teaches. **Backlog item — no Backlog section exists (P37 is still unrun), so as in runs 11–15 the item became a checker, and this run's own defects defined it.** **`prose-lint.js`'s rule 12 is now generative.** The hand-written `BRIT_SPELLINGS` inventory had failed five times; this run found three more it did not contain — **`editorialise`** (§3.1), **`parenthesised`** (§3.2) and **`unlabelled`** (`clustering-kmeans`), the last of which the inventory *did* hold as `labelled` and still missed, because `\b` does not fire inside a prefixed word. I also caught myself typing **`millimetres`** into the new §3.2 section. The inventory's polarity was backwards: words that legitimately end in `-ise`/`-our` are a small **closed** class, while words that should end in `-ize`/`-or` are **open**. So the two open classes are now matched by shape (`BRIT_GENERATIVE`) with their exceptions allow-listed (`ISE_OK`/`OUR_OK`/`YSE_OK`, the `-aise`/`-oise`/`-uise`/`-wise` families and a prefix group), the inventory keeps only `-re`, doubled consonants, the `-ce` nouns and one-offs, and its 6+-character entries now match as a **suffix**. `-iser`/`-isers` is deliberately unmatched (*Kaiser*, *adviser*, *miser*, *riser*). **The first run of it produced 31 hits; triaging every one gave ten genuine British spellings and four false-positive classes**, and the four allow-list mechanisms exist because the corpus demanded them, not by anticipation: `raises`/`raising` (the `-aise` family needed its inflections), **`Kaiser` ×4** (which is why `-iser` is unmatched), `unsupervised` (prefixes) and `imprecise` (the `im`/`in` prefixes). After the fixes the scan reports **8 → 0**, all of them real: `armour` (a guide), `Industrialised` (the WEIRD acronym in `sampling-methods`), `rigour` (`questionable-research-practices`), `centimetres` (`data-entry-and-validation`, caught by the new suffix rule), `reorganises` (the ML landing page), `personalisation` (**`privacy.html`, in the sentence about GA's consent mode**), `pedestrianise`/`pedestrianisation` (`problems.html`), plus `Dichotomising` in `checks.js` **and again in `quiz.html`**, `symmetrises` and a second `centimetres` in inline scripts. **Proved by reintroducing one defect per surface in the real tree and restoring it**: a coined `-ise` verb in a lesson, an inventory word behind a prefix, a `-our` noun in a `checks.js` string, an `-isation` noun entering through `faq_data.py` (which surfaced against its own lesson page, confirming the FAQ path), and a `-re` form inside a `snippets.js` comment — **five hits, one per surface, exit 1, and exit 0 on restore.** VOICE.md rule 12 and CLAUDE.md were rewritten in the same commit, as both files require. **Verification:** `audit.js` **0 errors / 20 warn / 64 info** (baseline restored after the index rebuild), `math-check.js` **270 assertions, 0 failures**, `prose-lint --strict` **exit 0**, `faq-audit --strict` **exit 0** — with **zero em-dashes added to any subject** (2 / 5 / 5 / 7 / 5, one *fewer* than before on §3.2, and FAQ em-dashes down on two lessons). **20 changed pages · 78 inline scripts · 35 JSON-LD blocks, 0 failures**; all shared JS, `sw.js`, the five tools and `faq_data.py` parse. Browser pass over all 20 changed pages via same-origin iframes at **1200px, 375px and 360px**: every expected string present, **0 console errors, 0 leading-slash links, 0 horizontal scroll at any width**, every lesson `h2` carrying an id, **all 22 new link targets returning 200**, the three interactives whose data changed still driving correctly, and dark mode verified on three changed lessons (bg `#0b1120`, `--link` `#6f7bf2`). One methodological note for future browser passes: flipping `data-theme` on an **offscreen** iframe updates the CSS variable but does not repaint the transitioned `color`, which reads as a false dark-mode regression — set `localStorage.theme` before load instead. **Index rebuilt (868 KB, 97 lessons, 37 pages) and searched for all 27 terms this run added: 27 HIT, 0 MISS**, each resolving to the right lesson. **`CACHE_VERSION` bumped sc-v34 → sc-v35** (two QUIPS changed and `site.js` is precached; nothing else in the shell moved). **Three notes for the next run.** The strongest checker candidate is now measured rather than guessed: **extending CHECK 9 to page prose finds real defects** (three of this run's four), and its false-positive taxonomy is already worked out — compare the printed *p* against the range implied by the **statistic's own rounding precision** (that alone cleared four of five rounding false positives), skip **arithmetic-chain lines** of the shape `F(1, 56) = 540 / 90 = 6.00` (the parser grabs the first number), and give **deliberately-planted-error pages an opt-out**, since `final-checklist`'s exercise and §3.2's drill are *supposed* to contain wrong statistics; the inline-script surface is the dangerous one for exactly that reason. `apa.html` still emits no confidence interval — now four runs old, and this run has made the lesson, the drill and `software.js` all oblige it, so the tool is the last holdout. And `flexRe` still cannot match a period, so `requirements.txt` remains unsearchable (run 14's finding, still a P71 session's job).
  - **27 Jul 2026 — fifteenth run (the ML evaluation spine, and the first tool that measures what step 4 has always done by eye).** The ledger named the thread and this run took it: five of the nine never-opened ML lessons, read as one arc rather than a scattered five — `prediction-vs-explanation` (§5.1), `regularization-ridge-and-lasso` (§5.3), `classification-metrics` (§5.4), `roc-curves-and-auc` (§5.5), `random-forests-and-ensembles` (§5.7). **75 of 97 lessons have now been subjects.** **Five absences, each confirmed by a sitewide grep, and every published number verified before it was published.** (1) **`Table 2 fallacy` returned zero hits sitewide**, on the lesson whose own FAQ warns against reading a predictor's internals as causal effects and whose closing line is "the danger is doing one while silently believing you're doing the other". §5.1 gained the named version, built on a DGP checked algebraically and by simulation at n = 200,000: with Z confounding X→Y (Z→X 0.6, X→Y 0.5, Z→Y 0.4), the adjusted model recovers **bX = 0.500** exactly as intended while handing back **bZ = 0.400** for a variable whose *total* effect is **0.70** — the table understates motivation by 43% — and the unadjusted X slope reads **0.676**, matching the closed form .92/1.36 = **0.6765**. The section's point is that adjustment buys one interpretable row and turns every other into a direct effect, which makes it a question about the causal diagram, not the output. (2) **`post-selection inference` and `selective inference` returned zero hits**, on the lesson that teaches lasso as automatic variable selection, on a site otherwise full of p-values. §5.3 gained it, and the number was simulated rather than asserted: a coordinate-descent lasso **validated two ways first** (λ = 0 reproduces `lm()` to 5 dp; the all-zero threshold equals max|x′y|/n exactly), then run on **1,200 replicates of 200 rows × 40 pure-noise predictors**. At λ = 0.60 λmax lasso keeps about six predictors *out of nothing at all*, and refitting those six by least squares returns **p < .05 for 32.8% of their coefficients** (7,293 coefficients; median p = .084) against a nominal 5%. The control settles it: the same count of predictors chosen **before** anyone looked at the outcome gives **4.7%** and a median p of **.488**. So the inflation is the selection, not the arithmetic or my code. (3) **`balanced accuracy` and `Matthews` both returned zero hits**, and **`negative predictive value` zero**, while `positive predictive value` appeared exactly **once** sitewide (a passing clause in `stats-1/probability-basics`'s FAQ, never once connected to "precision") and `PPV`/`NPV` only inside `problems.html`'s `<!-- verify -->` comment. §5.4 gained both halves: the clinical-vocabulary bridge (precision = PPV, its mirror TN/(TN+FN) = NPV, with the pointer to Bayes' rule) and a section on the two summaries a rare class cannot fool, run through the lesson's own 5%-prevalence example — the all-negative parrot scores **95% accuracy, 50% balanced accuracy, MCC 0**, while a real model at TP 30 / FN 20 / FP 50 / TN 900 scores **93% accuracy** (lower!), **77.4% balanced accuracy, MCC .44**. MCC was confirmed to be literally the Pearson correlation of the two 0/1 vectors (both **0.439732**), and the section says honestly that the parrot's MCC is a **convention** (zero denominator), not a computed correlation. (4) **`Youden` returned zero hits and `calibration` only as *calibration of claims*** (the Writing course) or a miscalibrated survey item; `Brier` was absent entirely. §5.5 gained both. Youden's J went into the existing "Reading the curve" section with the analytic result that for two equal-variance normals the maximizer is always the midpoint (dJ/dt = 0 ⟹ t = d/2), so at *d* = 1 it sits at threshold 0.50 with **TPR .6915, FPR .3085, J = .3829** — which are the viz's own default slider values, so a reader can check it on the plot. Calibration got its own section on the fact that **AUC reads only the ordering**: 100 patients with 20 events, a model scoring .55/.45 ranks perfectly at **AUC 1.00 and Brier .2025**, while a model saying "20%" to everyone ranks nothing at **AUC 0.50 and Brier .160** — the useless model wins — and recalibrating the perfect ranker to .95/.05 drops Brier to **.0025** with AUC unmoved. The benchmark named is *p̄*(1 − *p̄*) = .160. (5) **`boosting` returned zero hits as a method** (every hit was the ordinary English verb), and `gradient boosting`, `XGBoost` and `AdaBoost` zero — in a lesson titled "Random Forests **& Ensembles**" that teaches bagging only. §5.7 gained boosting, and the claim that separates it from a forest was **simulated, not repeated**: hand-rolled gradient boosting (depth-3 `rpart` trees, shrinkage 0.3) and bagged deep trees on the *same* 200 rows with 15% label noise, scored on 6,000 test cases. The forest's test error falls **.323 → .189 by fifteen trees and sits at .190 at 300**; the boosted model **bottoms out at .184 after five trees and climbs to .242 by 500, 32% worse than its own best**, having hit **100% training accuracy at tree 43**. A shrinkage sweep supplied the honest qualifier: the minimum arrives at tree **5 / 16 / 59** for rates 0.3 / 0.1 / 0.03 and every one of them turns upward, so shrinkage buys time and not immunity. Also **`partial dependence` appeared exactly once sitewide, in §5.7's own callout, never taught** — now explained in the importance section (sweep one predictor, average the prediction over the others as they actually occur). **FAQ freshness: five swaps, one per lesson, each displacing a question that restated its own lesson's title or an h2**, each duplicate-checked against all 291 first. Out: "What is the difference between prediction and explanation?" (the lesson title), "Ridge or lasso — which should I use?" (its h2), "What is the difference between precision and recall?" (the lesson's own bullet list, verbatim), "When should I use a precision-recall curve instead of ROC?" (its h2), "What is out-of-bag (OOB) error?" (its h2). In: "Can I do machine learning with only 80 participants?" (verified — 16 of 20 test cases is 80% with a Wilson interval of **[.584, .919]**, cross-checked against the exact binomial [.563, .943]), "Does regularization work for logistic regression too?" (**`penalized logistic` had zero hits**; the answer's sharpest fact is that scikit-learn's `LogisticRegression` is **already L2-penalized at C = 1**, so the unpenalized fit is the thing you must request), "My classes are very imbalanced. Should I oversample the rare class?" (**SMOTE / oversampling / class weights: zero hits sitewide**), "Model A's AUC is .82 and model B's is .79. Is A actually better?" (**`DeLong` zero hits**; `roc.test`'s paired default confirmed from pROC's documentation), and "Do I need to tune a random forest, or are the defaults good enough?" (**`mtry` existed only in a code comment and a snippet**; scikit-learn's own docs supplied the trap — `RandomForestRegressor` ships `max_features=1.0`, which the documentation itself calls "equivalent to bagged trees"). Still **291 Q&As**, no duplicate question strings. **software.js:** none of the five subjects has an entry (correct — no SPSS/JASP path exists for them). Rotated four not checked in runs 12–14 — `mixed-and-multilevel-models`, `missing-data`, `bayesian-estimation`, `generalized-linear-models` — against **SPSS 32.0.0** and **JASP 0.98.1 (7 Jul 2026, still the current release, no menu renames in its notes)**; SPSS 32's additions are Mediation Analysis, VAR models, an AI Output Assistant and passkeys, none of which touch those four paths, and `missing-data`'s claim that JASP still has no imputation module was re-confirmed (it remains an open feature request). **No step changed.** The one obligation the new prose created went to `logistic-regression`, the site's only classification walkthrough: **`Classification Table` / `cut value` / `classification cutoff` had zero hits sitewide**, so a step now names SPSS's default table and its fixed .500 cutoff, and a tip explains that its *Overall Percentage* is plain accuracy and flatters a rare outcome. Still **34 entries**. **Snippets:** four extended in step with the new sections (balanced accuracy + MCC, Brier + calibration curve + Youden coords + paired `roc.test`, partial dependence + `HistGradientBoostingClassifier`, and a comment on the lasso block warning against refitting the survivors for p-values). **caret, pROC, glmnet, randomForest and gbm are not installed on this machine**, so those R lines were verified against the packages' own documentation rather than executed, and the note says so plainly; every R line this run *could* run — the whole lasso validation, the boosting and bagging simulations, the shrinkage sweep, the Table 2 DGP and both proportion intervals — was executed here on R 4.5.2 with rpart 4.1.24. **Link health, measured:** inbound lesson links recounted for all 26 tools, guides and posters. `cheat-test-chooser.html` was the last content page at **0 inbound**, exactly as run 14 predicted; it now has five, each matched to a panel the poster actually owns (§1.1 types-of-data → the outcome-type question the whole map starts from, §2.1 one-way-anova → panel ①, §2.5 non-parametric-alternatives → panel ②, §2.6 chi-square-tests → panel ③, §3.1 multiple-regression → panel ④). **No content page is left at 0 inbound**, so the next run inherits step 7's second question: the lessons at 0–1 outbound prose links are `ml/clustering-kmeans`, `data/data-privacy-basics` and `writing/abstracts-and-titles` at zero, then `stats-1/central-limit-theorem`, `data/merging-datasets` and `ethics/informed-consent-and-irb` at one. **Quips:** five reviewed, two kept, three rewritten. All three had the recurring defect (a closing clause that only restates the opener), and §5.3's also carried a **"quietly"** — VOICE.md rule 9's banned intensifier, sitting in the one surface `prose-lint --strict` holds to rule 12 only. **Backlog item — no Backlog section exists (P37 is still unrun), so as in runs 11–14 the item became a checker, and this run's own hand-work defined it.** `tools/faq-audit.js` (zero-dependency, Node built-ins) measures the three inputs to step 4 that fifteen runs have judged by eye: **ECHO** (question content words contained in one of its own lesson's headings), **REPRISE** (share of the answer's 4-word shingles already verbatim in the lesson body — CLAUDE.md's "complement, don't repeat" with a number on it), and **TWIN** (nearest question and nearest answer on any other lesson, the criterion that caught run 13's two duplicates). It also prints the **opener-template census** PROMPTS.md flags as unmeasured: **52 of 291 "what-is", 24 "what-is-the-difference", 41 "how", 38 "is/does", 23 "why", 19 "can-i"**, with 52 in "other" — the buckets are exclusive and the difference-template one is tested first, so PROMPTS.md's count of 24 is confirmed exactly while its "59 with What is" corresponds to a wider bucket than this tool's. **It was calibrated against the tree BEFORE this run's swaps, and the calibration is reported honestly because it is partly negative:** four of the five questions already marked for replacement came back as the highest-ECHO question on their own lesson (67%, 100%, 50%, 67%; two inside the sitewide top 12), but the fifth — `classification-metrics`'s precision-vs-recall question — scored **ECHO 0% and REPRISE 0%** and the tool missed it entirely, because it restates a **bullet list** rather than a heading and paraphrases instead of copying. That boundary is documented in the script header and in CLAUDE.md rather than smoothed over. **`--strict` gates structural faults only** (not exactly 3 per lesson, a slug with no ready lesson, two identical question strings), because the honest verdict on a high ECHO is often "leave it" and a gated threshold would just teach future runs to write around the metric; it stays out of `audit.js` for prose-lint's reason. Proved by reintroducing both structural defects in a throwaway worktree at HEAD: a fourth Q&A fired `random-forests-and-ensembles: 4 Q&As (must be exactly 3)` and exit 1, a duplicated question string fired `identical question on train-test-split-and-generalization and roc-curves-and-auc` and exit 1, and both returned to exit 0 on restore. **Verification:** `audit.js` **0 errors / 20 warn / 64 info**, `math-check.js` **270 assertions, 0 failures**, `prose-lint --strict` **exit 0**, `faq-audit --strict` **exit 0** — with **zero em-dashes added to any of the five subjects** (7 / 9 / 9 / 9 / 9, unchanged), which mattered because four of them were already sitting at 9 against a budget of 10. **137 pages · 534 inline scripts · 235 JSON-LD blocks, 0 failures** sitewide (10 changed pages · 42 · 20); all shared JS, `sw.js`, the four tools and `faq_data.py` parse. Browser pass over all ten changed pages via same-origin iframes at **1200px, 375px and 360px**: every expected string present, **0 console errors, 0 leading-slash links, 0 horizontal scroll at any width** (the only element wider than its parent is the injected `.try-code pre`, which is in `HS_SELECTOR` and scrolls inside itself by design), every `h2` carrying an id with the mini-TOC picking up all five new ones, all five new link targets returning **200**, the three rewritten quips rendering in the sidebar, and dark mode verified (bg `#0b1120`, `--link` `#6f7bf2`, canvases painted). **Index rebuilt (857 KB, 97 lessons, 37 pages) and searched for all 25 terms this run added: 25 HIT, 0 MISS**, each resolving to the right lesson, and the live overlay returns §5.7 for *boosting*. **`CACHE_VERSION` bumped sc-v33 → sc-v34** (three QUIPS changed and `site.js` is precached; nothing else in the shell moved). **Three notes for the next run.** `apa.html` still emits no confidence interval — unchanged since run 13, still a tool-page job, and now two runs older than the lessons that oblige it. `flexRe` still cannot match a period, so `requirements.txt` remains unsearchable (run 14's finding, still a P71 session's job). And the read-back earned its place for the second run running: I wrote **"modelling"** into a `multiple-regression` cross-link sentence — rule 12, the one spelling `prose-lint` would certainly have caught — and found it by reading the diff back before the linter ever ran.
  - **27 Jul 2026 — fourteenth run (the Data course's raw-file-to-analysis-ready spine, and the largest unlinted prose surface on the site).** First run under the rewritten prompt, so selection came from the ledger rather than from git dates: five of the six Data lessons no run had opened, taken as one thread rather than a scattered five — `data-cleaning-workflow` (§2.4), `outliers-in-practice` (§2.5), `transformations-and-recoding` (§2.6), `merging-datasets` (§2.8), `reproducible-workflows` (§2.9). **70 of 97 lessons have now been subjects, and the Data course is down to its last one.** **Four absences, each confirmed by a sitewide grep rather than assumed, and one shipped defect.** (1) **`median absolute deviation` and `MAD` returned zero hits sitewide**, on the lesson that states the masking problem in its own z-score bullet ("a lone giant can hide by inflating the SD") and then offers no robust estimator. §2.5 gained the section, built on two verified facts: the largest standardized score possible in a sample of size *n* is (*n*−1)/√*n*, so **at *n* = 10 the ceiling is 2.846 and |z| > 3 can never fire** (confirmed by construction at nine sample sizes and by a 200,000-sample random search that never beat the bound), and a worked twenty-age example where two typos hide each other — **z(511) = 2.97 and z(498) = 2.88, so neither is flagged**, while median 23 and MAD 3 give **robust scores of 110 and 107** and the oldest genuine student (34) sits at 2.47. The 1.5 × IQR fence catches both at 37, so the new section reinforces the existing advice instead of contradicting it. (2) **`Box-Cox` returned zero hits sitewide**, on a transformations lesson offering log and √ with nothing to choose between them. §2.6 gained the ladder of powers with a verified skewness table (λ = 1 → 4.74, 0.5 → 1.60, 0 → −0.03, −1 → **−5.51, overshot into left skew** — the "stronger squeeze" framing hides that you can go too far) plus λ̂ recovery checked **two independent ways**, a hand-rolled profile likelihood and `MASS::boxcox`, agreeing to 3 dp on all five test cases. **The honest finding was simulated rather than asserted:** over 2,000 right-skewed samples at *n* = 60 the λ interval has median width **0.50 and never once contains 1**, while on already-normal samples of the same size it is **2.33 wide and contains 1 in 97% of runs** — so Box–Cox is decisive about *whether* to transform and vague about which rung, which is why the prose says round λ̂ to a readable rung rather than report 0.407. (3) **`anti-join` returned zero hits**, and `relationship =` none either, on the lesson whose entire "two habits that save you" section is about join shape. §2.8 gained the assertive form, and the sharpest fact came out of running it: **`dplyr` 1.2.0 warns on a many-to-many join and says nothing at all about a one-to-many** — the lesson's own `sessions` table is one-to-many, so its four participants silently return five rows. `relationship = "one-to-one"` errors; `anti_join` names Ann, Dan and the orphaned session 5. (4) **`sessionInfo`, `renv`, `requirements.txt` and `README` all returned zero hits sitewide**, on the lesson whose own FAQ says "the software and package versions are recorded" without ever saying how. §2.9 gained the section, anchored on a claim I checked rather than repeated: **R changed how `sample()` draws from a seeded stream in 3.6.0, so `set.seed(42); sample(1:10)` returns `1 5 10 8 2 4 6 9 7 3` on one side of that release and `10 9 3 6 4 8 5 1 2 7` on the other** (`rnorm` untouched, which is why it stays hidden). That lesson's FAQ 2 claimed a seed reproduces "on every run and every machine", which the new section falsifies; it now says "every run of the same software". **The shipped defect was in §2.4's own snippet.** Its R half called bare `distinct()` while its FAQ correctly says to define uniqueness by key — and on the lesson's own scenario those disagree: 7 rows and 5 participants where one resubmission carries a two-minute-later timestamp gives **6 rows and a mean of 65.83 from `distinct()` against the correct 5 rows and 67.4**. Both halves now de-duplicate on the key, and §2.4 gained a short section showing the case, since the lesson previously only ever demonstrated *exact* duplicates. Also §2.4: **`standardise_group()` in the visible cleaning log** (fixed, and it became this run's checker), ISO 8601 named in the date-drift bullet, and the "impossible values" bullet now hands declared missing-value codes to §2.2, which already teaches them. **FAQ freshness: five swaps, each displacing an answer that restated its own lesson's h2** and each duplicate-checked against all 291 first. §2.4's spreadsheet-vs-script answer also duplicated §2.9's whole lesson; §2.8's was one of the 24 "What's the difference between X and Y?" template questions the prompt flags. What shipped: "Two rows share an ID but give different answers. Which one do I keep?", "How many outliers is too many?" (simulated: the 1.5 × IQR fences sit at **z = ±2.698 → 0.70%** of a normal by construction, and **a perfectly normal sample of 200 gets 1.7 flags on average with 75% of samples flagging at least one** — so two flagged values in 200 people is the expected result, not a finding), "I analyzed my outcome on the log scale. How do I report the means?" (arithmetic mean 37,832 vs geometric 27,625, CI [23,941, 31,876] **asymmetric by 3,684 below and 4,251 above**, and 1 log unit = a factor of 2.72), "Can I join on more than one column?" (composite keys, zero hits sitewide), and "My co-author ran my script and got different numbers." Still **291 Q&As**; also corrected `capitalisation` in §2.8's third answer, a British spelling **the `BRIT_SPELLINGS` inventory did not contain** even though `faq_data.py` is linted. **software.js:** only one of the five subjects has an entry. **JASP is still 0.98.1 (7 Jul 2026)** and the 0.96–0.98.1 notes document **no menu renames**; SPSS 32's additions are Mediation Analysis, VAR models and a Genomic Analysis dialog, none touching the Transform or Regression paths these steps use. The one real drift found: `transformations-and-recoding`'s JASP composite step said `(q1 + q2 + q3 + q4 + q5) / 5`, which returns blank for anyone who skipped an item, while the SPSS half correctly used `MEAN(...)` and the lesson's own bullet says a mean tolerates gaps — now `rowMeanNaRm(...)`, the row-wise function JASP added in 0.95 (confirmed in the release notes and the issue tracker). Plus one Box–Cox tip. Still **34 entries**. **Snippets:** four fixed or extended and **`reproducible-workflows` gained the entry it never had** — the lesson that says "the analysis *is* the script" was in the 21-warning no-snippet list beside the Ethics and Writing conceptual lessons, so the audit baseline is now **20 warnings**. Every added R line was **executed on this machine** (R 4.5.2, dplyr 1.2.0, MASS 7.3.65), including the new reproducible-workflows script end to end against a generated `raw/survey.csv`; the Python halves could not be, this machine still having no scientific Python. **Link health, measured:** inbound lesson links counted for all 26 tools, guides and posters. `guides/choose-statistics-dissertation` was the last content page at **0 inbound**, as run 13 predicted; it now has five, each matched to a section it actually owns (§1.1 types-of-data → its opening question, `from-question-to-hypothesis` → decide-before-you-collect, `assumptions-and-when-they-break` → when-reality-is-messy, `ml/prediction-vs-explanation` → predicting-not-testing, §3.4 ancova → its `#covariates` anchor). **`cheat-test-chooser.html` is now the notable 0-inbound page**, alone among the three posters; the other zeros (quiz, glossary, flashcards, toolbox, teachers, progress) are nav destinations and not orphans. **Quips:** five reviewed, three kept, two rewritten — merging's second clause explained its first (the recurring defect) and is now "…counted the rows before and after, which is how it found the third Gerald"; §2.6's median-split tag became "Everything looked normal, including the capybara." **Backlog item — no Backlog section exists (P37 is still unrun), so as in runs 11–13 the item became a checker, and this run's own defect defined it.** `prose-lint.js` gained a **fifth surface, `inline-scripts (every page)`: 12,637 string literals, larger than the other four combined.** Run 13 closed the four *shared* JS surfaces and left every page's own inline `<script>` open — where lesson interactives print verdicts, chart labels, log lines and interpretation sentences, and where `quiz.html`'s 208-question BANK lives. None of it reaches `extractProse()`, which strips `<script>` before counting a word. **Two design corrections came from testing the checker against the unfixed tree before trusting it, and both are documented in three places:** the first version filtered to prose-like strings *before* scanning and **missed the very defect it was written for** (`"standardise_group()  — mapped "` carries one real word), so the spelling scan now reads every literal and the prose filter gates only the *other* patterns; and `\b` **does not fire inside `standardise_group`**, because `_` is a word character, so this surface matches on letter boundaries instead. Proved on three defect shapes — the snake_case log line, a British spelling in a quiz BANK question, one in a lesson verdict string — each producing exactly one hit and exit 1, with `--strict` back to 0 on restore. **On its first run it found two genuine budget-0 violations** on prose no linter had ever seen: a "Here's the rule" in `data-entry-and-validation`'s reveal text and a "That's the whole lesson" in `llms-and-ai-in-research`'s verdict string, both fixed. **A CSS gap surfaced during the browser pass and is worth remembering:** the two `<pre>` blocks this run wrote were **the site's first code blocks in lesson prose**, and both overflowed the page at 375px (644 > 371 and 449 > 371) because a bare `<pre>` neither wraps nor scrolls and the only styled `pre` on the site was `.try-code pre`. `styles.css` gained a shared `.lesson pre` rule and `site.js`'s `HS_SELECTOR` gained the selector, so the next one is styled and scroll-contained for free. **Verification:** `audit.js` **0 errors / 20 warn / 64 info** (21 → 20: the new snippet), `math-check.js` **270 assertions, 0 failures**, `prose-lint --strict` **exit 0**, with every subject inside its em-dash budget (7 / 9 / 8 / 5 / 5) — though only after the linter caught me at **13 on §2.6**, three over, from the section I had just written. **12 changed pages · 50 inline scripts · 24 JSON-LD blocks, 0 failures**; all shared JS, `sw.js`, the four tools and `faq_data.py` parse. Browser pass over all twelve via same-origin iframes at **1200px and 375px** (and 360px for the two `<pre>` pages): every expected string present, **0 console errors in both themes, 0 leading-slash links, 0 horizontal scroll after the CSS fix**, every h2 carrying an id, the mini-TOC picking up all five new h2s, all four new link targets returning 200 with `#covariates` resolving to a real id, the injected snippet and SPSS/JASP blocks carrying their new content (the JASP step renders on tab click — site.js renders only the active tab), and dark mode verified (bg #0b1120, `.lesson pre` themed). **Index rebuilt (847 KB) and searched for all 23 terms this run added: 22 HIT.** The one MISS is **`requirements.txt`** and it is a search limitation rather than a content gap — `flexRe`'s separator class allows spaces and hyphens but not a period, so a dotted query cannot match; `requirements`, `pip freeze` and `package versions` all resolve to the lesson. **`CACHE_VERSION` bumped sc-v31 → sc-v33** (two QUIPS, `HS_SELECTOR`, and `styles.css` all precached; v32 was consumed by an intermediate state mid-run and deliberately not reused). **Three notes for the next run.** `apa.html` still emits no confidence interval, unchanged since run 13 and still a tool-page job. **`flexRe` not matching a period** is a real if small P71 gap and belongs to a session that can re-verify P71's whole battery, not to a content run. And the read-back earned its place again: I wrote "which is the whole point" into §2.8 — VOICE.md rule 6 wearing a different suit, invisible to the regex — and caught it only by reading the diff.
  - **27 Jul 2026 — thirteenth run (the two courses no run had ever opened, and the discovery that a quarter of the site's prose was unsearchable).** The staleness ranking put `ml/decision-trees` first by six days (7 Jul, untouched since P15 built it) and then hit a flat 13 Jul tier of 26 files left over from the P43–P47 voice passes, so the tie was broken toward **the two courses no P39 run had ever taken as a subject**, which run 12's note had flagged: `ml/train-test-split-and-generalization` (§5.2), `ml/llms-and-ai-in-research` (§5.12), `writing/writing-results` (§3.4) and `writing/tables-and-figures` (§3.3). **65 of 97 lessons have now been subjects.** **Six absences, each confirmed by a sitewide grep rather than assumed.** (1) **`information gain` returned zero hits sitewide** and `entropy` only one, in `neural-networks-intuition`'s cross-entropy loss, on the lesson that teaches splitting; a student reading scikit-learn's `criterion=` or any textbook's ID3/C4.5 section had nowhere to land. §5.6 gained it with a worked split (parent Gini .5 and entropy 1 bit; the split leaves weighted Gini **.3125** for a drop of **.1875**, weighted entropy **.704** for a gain of **.296 bits**) and the honest comparison, simulated rather than asserted: over **4,000 datasets with all 199 thresholds enumerated, the best Gini split and the best information-gain split were the identical threshold 85% of the time**. (2) **`cost-complexity` returned zero hits**, while the lesson body and its own FAQ both told readers to prune without ever saying what pruning optimizes. §5.6 gained *R*(T) + α × leaves and the worked threshold: a four-leaf subtree missing 6 of 200 against a collapsed leaf missing 14 survives only while **α < (.07 − .03) ÷ 3 = .0133**. Both R halves were **executed here** (R 4.5.2, rpart 4.1.24): `split = "gini"` and `split = "information"` both fit, and `printcp`/`prune` reproduce the nested sequence, with rpart's `cp` scaled to the root error exactly as the new prose says. (3) **Accuracy had no uncertainty anywhere on the site.** No lesson, tool or guide put a confidence interval on a test score, and `Wilson` returned zero hits, on a *statistics* site whose ML track reports accuracy nine times. §5.2 gained "Your test score is an estimate too": **.86 on 200 test cases → [.805, .901]**, the same .86 on 50 → **[.738, .930]**, and **±5 points needs ~185 cases while ±3 needs ~514**. Every figure was recomputed in R (`prop.test(..., correct = FALSE)`) and cross-checked against the exact `binom.test` ([.804, .905] and [.733, .942]), and **McNemar's test is named for the first time on the site** (it previously appeared only in `cheat-assumptions.html`). (4) **`retrieval` and `knowledge cutoff` returned zero hits**, on the lesson about LLMs, whose mechanism paragraph said flatly that there is "no lookup" — true of a bare model, not of the tools a 2026 student actually opens. §5.12 gained a section on both, and the mechanism sentence gained one word ("no **built-in** lookup"). It is deliberately evergreen: no product names, no version numbers, and the point it lands is that **a live link is evidence the source exists, never that the sentence is true**. (5) **The confidence interval was missing from the lesson that teaches the results-sentence formula.** `writing/writing-results` contained the string "CI" zero times and `writing/reporting-statistics-apa` never says "interval", while `software.js` injects **95% CI into 16 model APA sentences** across the site. The formula went from four moves to five, and the worked example was recomputed from its own numbers before the interval was added (pooled SD 5.2521, SE 1.3561 → *t* = 3.1709, *p* = .00243, *d* = 0.8187, *t*.975,58 = 2.00172 → **95% CI [1.59, 7.01]**). The lesson's own drill was extended to match, since a five-part formula drilled in four parts teaches the wrong thing: a seventh good fragment, the CI column added to the mock output, and **a new trap on APA's square brackets** (verified in-browser: 7/7 assembles the full sentence, and picking `(1.59, 7.01)` fires the bracket note). (6) **Figure caption placement returned zero hits sitewide**, so §3.3 gained "Figures have that same anatomy now": number bold above, italic title, note below, and the APA-6 habit that leaves a thesis written half in each edition. **FAQ freshness: six swaps, not five, because two of one lesson's three duplicated another page.** `llms-and-ai-in-research` asked "Why do LLMs make up references?" and "Do I have to disclose that I used AI?" while `ethics/ai-in-research-ethics` already owned both almost verbatim, so both were replaced (the retrieval-era "the AI gave me a real citation with a working link, is the claim safe now?", and "does telling the model *don't make anything up* actually help?", answered from the mechanism rather than from folklore). The other four each displaced an answer restating its own lesson's h2: regression trees (zero hits sitewide, the step-function and no-extrapolation consequences), "my test accuracy came out higher than my training accuracy", "how do I report participants who dropped out or were excluded" (the question §3.4 gestured at in its closing line and never answered), and "can I paste my SPSS output tables straight into my thesis". Still **291 Q&As**; FAQ em-dashes fell on four of the five subjects. **software.js:** the version note these steps carried was stale — **JASP is at 0.98.1 (7 Jul 2026), four minor releases past the 0.19.x of earlier runs** — so the 0.96/0.97/0.98 release notes were read for renames across the four walkthroughs re-verified (`factor-analysis-pca`, `manova`, `logistic-regression`, `meta-analysis`). **No menu was renamed**; the one real drift is 0.96's **online module library**, so the meta-analysis JASP step now says how a module is actually installed. One step was added to `independent-samples-t-test` pointing at SPSS's *95% Confidence Interval of the Difference* column, which §3.4's new fifth move obliges. Still **34 entries**. **Snippets:** seven British spellings in comments corrected, two `#`-comment additions plus two runnable lines, and **every added R line was executed on this machine**; the two Python additions (`proportion_confint`, `cost_complexity_pruning_path`) could not be, since this machine still has no scientific Python. **Cross-pollination — the orphan was `formulas.html`**, which run 12 named and left: **0 inbound links from all 97 lessons**, now five, each a genuine formula↔lesson match (§1.4 mean/variance/SD, §1.9 the three t-formulas, §2.2 the ANOVA bookkeeping, §2.5 the four χ² rows, §3.6 the log-odds pair). `guides/choose-statistics-dissertation` remains the last 0-inbound orphan. **Quips:** five reviewed, two kept, three rewritten — all three had a second clause that only restated the first, the defect runs 9, 11 and 12 each fixed elsewhere. **Backlog item — no Backlog section exists (P37 is still unrun), so as in runs 11 and 12 the item became a checker, and this time runs 11 and 12 had already named the target in their own process notes: the JS-injected strings, "the site's one unlinted prose surface".** `prose-lint.js` now scans **2,638 strings across four surfaces** at two deliberate strictness levels: `checks.js` and `software.js` get the **full budget-0 rule set** (ordinary site prose that happens to live in a `.js` file), while `snippets.js`'s `#` comments and `site.js`'s `QUIPS` are held to **rule 12 alone** (a code comment is an annotation, quips are exempt brand voice, and spelling is neither voice nor judgment). Em-dashes there are reported and not gated, because rule 1 is per *page* and none of these are pages; `--page assets/js/software.js` inspects a surface the way `--page <lesson>` inspects a lesson. **It found 8 real defects on its first run** (seven British spellings in snippet comments, one contrast punch in a `checks.js` "why"), all fixed, and it was proved by reintroducing one defect of each class on each surface — all four fired, and `--strict` returned to 0 on restore. VOICE.md, CLAUDE.md and AGENTS.md were corrected in the same commit, since all three stated the opposite scope. **The run's largest find came from following run 12's own closing advice: search the rebuilt index for the terms you just added.** Four came back MISS — not because the words were unwritten but because **`MAX_CHARS = 4500` was truncating 87 of the 97 lessons and dropping 27% of the site's lesson prose (154 KB)**, and because the "Common questions" block sits at the END of an article, the cut landed almost exactly on the FAQ answers. **The site's own search could not find a single one of the 291 answers that are baked into HTML precisely so they can be found.** Fixed with a `LESSON_MAX_CHARS` of 12,000 against a longest lesson of 10,393, the same reasoning the `problems.html` and `glossary.html` overrides already carry. The index goes **698 KB → 856 KB** and stays lazy-loaded; a full-corpus regex pass measures **0.93 ms over 97 documents**, so P71's finding that no debounce is needed still holds. All fourteen terms this run added now resolve to the right lesson. **Verification:** `audit.js` **0 errors / 21 warn / 64 info**, `math-check.js` **270 assertions, 0 failures**, `prose-lint --strict` **exit 0** with **zero em-dashes added to any of the ten changed pages** (§3.4 in fact fell 6 → 4). **10 changed pages · 40 inline scripts · 20 JSON-LD blocks, 0 failures**; all shared JS, `sw.js`, the four tools and `faq_data.py` parse. Browser pass over the ten via same-origin iframes at **1200px and 375px**: every expected new string present, **0 console errors, 0 leading-slash links, 0 horizontal scroll at either width**, every h2 carrying an id for the mini-TOC and share-links, all six new link targets returning 200, dark mode verified (bg #0b1120, `--link` #6f7bf2), the injected SPSS and JASP boxes both carrying their new steps, and the search overlay loading the larger index and returning `decision-trees` for *information gain*. **`CACHE_VERSION` bumped sc-v30 → sc-v31** (three QUIPS changed and `site.js` is precached). **Two notes for the next run.** `apa.html`, the APA Results Formatter, **emits no confidence interval at all**, which is now out of step with §3.4's five-move formula and with the 16 model sentences in `software.js`; that is a tool gap, not a content gap, and belongs to a session that can rework the tool. And I wrote "quietly" into §3.3 and caught it reading back — **the seventh consecutive run to trip on VOICE.md's easiest rule** — though from this run forward that class of miss is at least measured everywhere except the page prose I was writing at the time.
  - **27 Jul 2026 — twelfth run (the Stats 2 correlation/regression bridge, and the first check on the site's own APA sentences).** The staleness ranking (last commit that substantively changed this file, mass passes excluded) cleared to five lessons no run had touched: `stats-2/correlation` (§2.8), `stats-2/simple-linear-regression` (§2.9), `stats-3/multicollinearity-and-variable-selection` (§3.2), `methods/reliability-and-validity` (§1.3, 4 Jul) and `ethics/deception-and-debriefing` (§4.3, 6 Jul) — **the first run to reach Stats 2's correlation/regression spine**, and 60 of 97 lessons have now been a P39 subject, leaving 37 — of which 20 are the two entirely untouched courses, ML & AI (12) and Writing (8). **Five absences, each confirmed by a sitewide grep rather than assumed.** (1) **Restriction of range.** Its only hit sitewide was `quiz.html`, which *tests* it ("compute r using only high-GPA students → weakened") while **no lesson taught it** — a student who gets that question wrong had nowhere to go. §2.8 gained a section; Thorndike's Case II was checked against a 400,000-draw simulation, ρ = .60 falling to **.3452 in the top quartile of x** (formula .3457) and **.2952 in the top decile** (.2936). (2) **Attenuation from unreliability** had zero teaching hits, and it is the bridge between two lessons in this batch. Both ends now carry it: r_obs = r_true·√(r_xx·r_yy) verified by simulation (**.4211 observed against .4200 predicted** at ρ = .60, both reliabilities .70), plus the ceiling that two scales at α = .70 cannot report above .70 however tight the true relationship. (3) **Regression to the mean** appeared only as a *wrong answer* in two quiz distractors and one passing phrase in §4.7's shrinkage callout — never taught, on the lesson **named after it**. §2.9 gained "Where the name comes from" (Galton 1886): standardizing makes the least-squares slope exactly *r*, so at a retest r of .70 a group scoring 2 SD up averages **1.4 SD** on the retest (2M-draw simulation 1.3998 against theory 1.4000) and **80.0% of them score lower the second time** (Φ(0.8402) = .7995), which is why "we tutored the lowest scorers and they improved" proves little. (4) **Tolerance.** `problems.html` Problem 29 makes the student *compute* it, `software.js` tells SPSS users to read the column and its APA sentence reports "all tolerances ≥ .43", while §3.2 defined VIF and never tolerance. Now named, with VIF > 5 ⇔ tolerance < .20. (5) **Cohen's κ returned zero hits sitewide**, on a lesson that names inter-rater reliability and gives a statistic only for internal consistency. §1.3 gained it, built on the base-rate pair: two coders at an identical p_o = .90 earn **κ = .80** when labels are balanced and **κ = .44** when one label covers 85 of 100 clips. **The run also found a defect in shipped content, which is what item 3 was for.** `software.js`'s `simple-linear-regression` APA sentence was internally inconsistent: b = 2.31 and SE = 0.57 give t(48) = 4.05 and F(1, 48) = 16.40, hence **R² = .2547 and β = .5049**, while the sentence printed **R² = .21, β = .45** and "a fifth of the variance". Corrected to .25 / .50 / "a quarter". A second, smaller one: `repeated-measures-anova` printed p = .002 where its Greenhouse–Geisser F(1.56, 45.2) = 8.75 gives .0015 (ηp² = .23 re-verified). **Backlog item — there is no Backlog section (P37 is still unrun), so as in run 11 the item became a checker, and this time the run's own defect defined it.** `audit.js` gained **CHECK 9**: it loads `viz.js` into the audit's existing bare sandbox (no DOM shim needed) and recomputes p from every t/F/χ²/z/r printed in a `software.js` `apa` string or a baked `.apa-quote` block — the statcheck test `apa.html` already offers readers, turned on the site's own content. **27 pairs, 0 errors**, and it was proved by reintroducing both bugs (caught both) before restoring. Its one design constraint is documented in CLAUDE.md and worth keeping: pairing must be **clause-local** (same clause, ≤ 80 chars, no intervening statistic), because a greedy pairing reports **eight false mismatches** on this very corpus — an APA sentence routinely runs "…, p = .011, ηp² = .08: … F(1, 76) = 15.2, p < .001". **FAQ freshness: four swaps, not five.** §2.8's three were checked against all 291 and none restated the lesson or duplicated another page (its r benchmarks and `effect-size-and-power`'s d benchmarks are complementary), and all three are strong queries, so **removing one would have been a net loss** — the gap it would have filled, **partial correlation (zero hits sitewide)**, was closed with one prose clause instead. The four that shipped each displaced an answer restating its own lesson: "How do I know whether my slope is statistically significant?" (t = b/SE on df = n − 2, the CI [1.16, 3.46] from t.975,48 = 2.0106, and the two identities that let a reader self-check — t² is the model F, and the slope test is the r test), "Every pairwise correlation is below .6. Am I safe from multicollinearity?" (four predictors where the fourth is the sum of the first three: **every pairwise r ≈ .55 while VIF = 13 and tolerance = .08**, exact and simulated, and the reason VIF exists at all), "What counts as a good Cohen's κ?", and "Is not telling participants my hypothesis the same as deception?" (incomplete disclosure vs deception — the question the lesson gestured at twice and never answered). Still **291 Q&As**; FAQ em-dashes down on two pages. **A voice observation for a future pass:** 33 FAQ questions across the site open with the identical "What is the difference between X and Y?" template, which no linter measures. **software.js:** the batch's three walkthroughs re-verified against SPSS 29–31 and JASP 0.19.x menu reality, **no step changed**; two steps added for the new κ prose (SPSS Crosstabs → Statistics → Kappa, with the same-category-codes gotcha that silently breaks the square table; JASP's Reliability → Inter-Rater Reliability) and four tips. Still **34 entries**. **Snippets:** three extended, and **every added R line was executed on this machine** (R 4.5.2, car 3.1.5) — base-R κ returns exactly 0.8 on the lesson's own balanced example, partial correlation takes the ice-cream link from .44 to −.025 once temperature is removed, and `1 / car::vif(fit)` returns tolerance .073 at VIF 13.6. `psych`, `irr` and `ppcor` are **not** installed here, which is why the κ snippet was written in **base R**: no new dependency and actually runnable. The two Python additions (`pingouin.partial_corr`, `sklearn.cohen_kappa_score`) could **not** be executed — this machine has no scientific Python at all. **Cross-pollination — the orphan was `datasets.html`**, which run 11 named and did not fix: it had **0 inbound links from all 97 lessons**. Five went in, each a real dataset↔lesson match rather than a decoration (§2.8 → screen-time, §1.3 → wellbeing-survey, §1.12 → sleep-experiment, §2.1 → study-methods, §3.7 → admissions), with every row count and column name checked against the shipped CSVs — which corrected one claim in the draft (wellbeing-survey has **two** reverse-worded items, q3 and q6, not one). Next orphans, deliberately left: `guides/choose-statistics-dissertation` and `formulas.html`, both at 0 inbound. **Quips:** five reviewed, three kept, two rewritten — multicollinearity's second sentence explained its own first (the exact defect runs 9 and 11 fixed elsewhere) and is now just "Two predictors sharing one trench coat and pretending to be different variables"; §2.9's flat "— and being right" tag became "pick the line that regrets the least". **Verification:** `audit.js` **0 errors / 21 warn / 64 info** (63 + CHECK 9's pair-count line), `math-check.js` **270 assertions, 0 failures**, `prose-lint --strict` **exit 0** with **zero em-dashes added to any of the eight changed pages** (9 / 8 / 4 / 4 / 8 unchanged on the five subjects). **8 changed pages · 34 inline scripts · 16 JSON-LD blocks, 0 failures**; sitewide **235 JSON-LD blocks and 534 inline scripts** parse; all shared JS, `sw.js`, the four tools and `faq_data.py` parse. Browser pass over all eight via same-origin iframes at **1200px and 375px**: every expected new string present, **0 console errors, 0 leading-slash links, 0 horizontal scroll at either width**, the mini-TOC picking up all four new h2s, the corrected APA sentence rendering as R² = .25 / β = .50, all 13 new link targets returning 200, all five `datasets.html` anchors present, and dark mode verified (bg #0b1120, `--link` #6f7bf2). **One check worth repeating on every future run: after rebuilding the index, search it for the terms the run just added.** Doing that here caught two of the seven as **unsearchable** — §1.3 wrote only "Cohen's κ" so a student typing *kappa* got nothing (search v2's `squash`/`flexRe` normalise separators, not Greek), and §2.8 explained the phenomenon without ever naming **attenuation**. Both now carry the Latin term, and all seven new concepts resolve to the right lesson in the rebuilt index. **`CACHE_VERSION` bumped sc-v30** (two QUIPS changed and `site.js` is precached). **Process note:** I wrote "labelling" into new §1.3 prose and caught it on read-back before linting; the JS-injected strings (tips, checks, quips) remain the site's one unlinted prose surface, so the four new tips were hand-checked rather than measured.
  - **25 Jul 2026 — eleventh run (the regression core, the shape of a dataset, and three statistics the site reported but never taught).** The staleness ranking (last commit that substantively changed this file, mass passes excluded) cleared to a 3 Jul tier of three and a 6 Jul tier: `stats-3/multiple-regression` (§3.1), `stats-3/ancova` (§3.4), `stats-4/survival-analysis` (§4.8), `data/tidy-data` (§2.1, 10:15) and `data/wide-vs-long-data` (§2.7, tie-broken toward the shape thread) — **the first run to reach Stats 3's regression spine**. **Three absences, each confirmed by a sitewide grep rather than assumed, and all three the same defect: a statistic the site reports and never defines.** (1) **Standardized coefficients.** β appears in `problems.html` (Problem 23, whose whole point is that raw coefficients aren't comparable — and which §3.1 itself links), is typeset in `writing/reporting-statistics-apa`, gets a leading-zero rule in a FAQ, and is printed three times in §3.1's own injected APA sentence (β = .38 / .21 / −.07) — while **no lesson said what it is**. §3.1 gained a section; β = *b* × SD_x ÷ SD_y was checked against a z-scored refit (identical to 6 dp), β = *r* in simple regression exactly, and Problem 23's own .419 / .310 and adj *R*² = .3287 reproduced. (2) **ANCOVA had no effect size at all** — not "η²", not the phrase "effect size" — on a lesson whose SPSS steps say to tick *Estimates of effect size* and whose APA sentence reports ηp² = .11. §3.4 gained the section: ηp² = *F*·df₁ ÷ (*F*·df₁ + df₂) verified against the sums-of-squares route exactly (0.109911 both ways), and the box's *F*(1, 57) = 6.84 → .1071. **The "partial η² comes out larger" claim was simulated, not assumed** — 2,000 randomized datasets give mean .089 against an unadjusted .049, ANCOVA larger in **79.6%** of runs, while a confounded example runs the other way (.36 → .11), so the sentence states both directions instead of the convenient one. (3) **Proportional hazards was named, assumed and never checked.** `Schoenfeld` returned **one hit sitewide, in `plan.html`**, which tells readers to check PH with residuals no lesson explains — run 9's Durbin-Watson finding exactly. §4.8 gained three checks in rising formality plus four repairs; the log-minus-log vertical gap = log HR was verified algebraically and numerically (7 dp at five times), and `cox.zph` run live in **survival 3.8.3** (*p* = .79 when PH holds, 2.8e-16 when the hazards cross). **Also:** §4.8's interactive already prints "> 36 mo" when the censoring slider hides the median, and the prose never named that state — it now teaches **not reached** and the landmark-rate alternative. §2.1 carried a dangling forward reference ("you'll meet `pivot_longer`/`pivot_wider` later in this course") **with no link**, now pointed at §2.7, plus `reproducible-workflows` for its raw-file-read-only paragraph. §2.7 gained "the shape decides who gets dropped" (wide has to be a rectangle, so RM-ANOVA deletes the whole participant a mixed model would keep) and **SPSS's Data → Restructure path**, which the lesson demanded twice and never gave. **FAQ freshness:** five swaps, each displacing an answer that restated its own lesson (§2.1's also duplicated §2.7's FAQ #1 across pages), all duplicate-checked against the full 291 first — a predictor-normality candidate was killed because `assumptions-of-regression` owns it verbatim. What shipped: "My R² is low but my predictors are significant. Is the model useless?" (*F*(1, 998) = 10.08, *p* = .002 at R² = .01, computed), "Should I analyze change scores (post minus pre) instead of using ANCOVA?" (**the SE ratio √((1 + r)/2)** confirmed by 4,000-replication simulation at five values of ρ, within .015 of theory at every one), "How many participants does a survival study need?" (Schoenfeld's 4(z + z)² ÷ (ln HR)² gives 65.3 events at HR = 2, but an independent log-rank simulation gives **77% power at 66 events and 81% at 70**, so the answer says "roughly 65 to 70" rather than parroting the formula), "My table has one row per person and several measurement columns. Is that untidy?", "My wide sheet has no ID column. Can I still pivot it to long?". FAQ em-dashes **10 → 8**. **software.js:** the batch's three entries re-verified against SPSS 31/32 + JASP menu reality, **no step changed**; four tips added where the new prose created an obligation (SPSS's *Standardized Coefficients* column; the *Partial Eta Squared* label and why it can't share a table with unadjusted η²; the two SPSS PH-check routes; "not reached"). Still **34 entries**. **Snippets — one snippet made a claim that is false for most readers.** §2.7's R half asserted `identical(wide, wide2)` with the comment "TRUE -> the reshape is lossless". Run live on **tidyr 1.3.2**: TRUE for a tibble, **FALSE for a plain `data.frame`**, which is exactly what `read.csv()` hands a student — so the one reader who actually checks concludes the pivot ate their data. Now `all.equal(as.data.frame(…))`, verified TRUE both ways, with the reason in the comment. Three snippets gained runnable backing for the new prose (β by hand, ηp² off the `Anova` table, `cox.zph` / lifelines `check_assumptions`); **every added R line was executed on this machine**, and the lifelines call was rewritten after review because Cox needs numeric covariates and the original passed it a string column. **Cross-pollination — the orphan was `plan.html`.** The site's design → test → sample-size → write-up tool was linked from **exactly one of 97 lessons** (`datasets.html` from none), so five natural spots got it: §3.1, §3.4 and §4.8 via its `?test=` slugs, `stats-2/repeated-measures-anova` (which had no tool link of any kind), and `methods/from-question-to-hypothesis`. All four slugged links were opened in-browser and land on the intended plan. **Quips:** five reviewed, four kept, one rewritten — wide-vs-long's second sentence explained its own first (the defect run 9 fixed on `regression-diagnostics`); now "Wide capybara, long capybara. Same capybara, different table." **Backlog item — the one I picked was already done, so I built the checker instead.** P38 run 2's deferred "`regression-diagnostics` re-randomises its cloud on every scenario click" was cleared by a later P39 commit; verified in-browser across eight scenario switches (good = 0.81, funnel = 0.51, curve = 0.01 returning **identically** every revisit, and "New sample" moving it to 0.86), so **P38's tracker line is stale, not the site**. The live item chosen instead attacks the class three runs have now found by hand: **a term named in a tool page, guide, problem or injected block and never named in any lesson**. A glossary-headword version was too noisy to trust (29 of 31 hits were composite headwords like "AIC / BIC" that no lesson would ever write verbatim), so the second pass matched eponym-shaped capitalized tokens and found two real ones. **`Bartlett` and `KMO` appear nowhere in §3.10 `factor-analysis-pca`** while its injected box tells readers to tick both and its APA sentence reports both with numbers; §3.10 gained a section with Kaiser's labels and the honest warning that Bartlett is a formality (verified: 20 variables and n = 100 with true correlations of only .10 give *p* = .00005, n = 300 gives *p* < 1e-16, and it correctly fails to reject at true *r* = 0, so it is a valid test with a very easy bar; KMO .914 on clean two-factor data against .507 on noise). **`listwise` and `pairwise` appear in zero lessons** while `problems.html`'s answer key, `datasets.html`'s assignment and `guides/clean-survey-data` all use them and §4.9 `missing-data` teaches the same thing as "complete-case"; §4.9 now names both plus pairwise's failure mode (simulated: **9 of 400** pairwise matrices at 35% MCAR came out non-positive-definite, smallest eigenvalue −0.10). **Verification:** `audit.js` **0 errors / 21 warn / 63 info** (baseline unchanged), `math-check.js` **270 assertions, 0 failures**, `prose-lint --strict` **exit 0** with **zero em-dashes added to any of the nine changed pages** (loads 7 / 6 / 6 / 3 / 3 / 8 / 3 / 4 / 6, all inside the ≤10 budget) and FAQ dashes down. **9 changed pages · 37 inline scripts · 18 JSON-LD blocks, 0 failures**; all shared JS, `sw.js`, both tools and `faq_data.py` parse; every internal link and `#fragment` resolves (audit-proven). Browser pass over all nine via same-origin iframes at 1200px and 375px: **every expected new string present, zero console errors, zero leading-slash links, zero horizontal scroll**, the mini-TOC picking up all four new h2s, and the injected tips and snippets confirmed in their right containers (run 8's lesson: tips render in the APA block, not the software one). **`CACHE_VERSION` bumped sc-v28 → sc-v29** (one QUIP changed and `site.js` is precached). **Two process notes:** I wrote "quietly decides" into §2.7 and caught it reading the diff back — the **sixth consecutive run** to trip on VOICE.md's easiest rule — and wrote "labelled" into a `software.js` tip, which `prose-lint` cannot see by design, so only a read-back finds it. The JS-injected strings remain the site's one unlinted prose surface.
  - **23 Jul 2026 — tenth run (the never-refreshed toolkit foundations, and the practice-problem map closed).** The staleness ranking (last commit that substantively changed this file, mass passes excluded) cleared to five lessons no run had touched as its subject, forming a coherent *measured variable → clean data* thread: `methods/variables-and-operationalization` (§1.2, 4 Jul), `methods/bias-and-blinding` (§1.10, 4 Jul), `methods/preregistration-and-open-science` (§1.12, 4 Jul), `data/codebooks-and-documentation` (§2.2, 6 Jul) and `data/data-entry-and-validation` (§2.3, 6 Jul) — the **first run to reach the Data course** as a subject rather than a cross-link target. **The gaps were the thread's own spine.** §1.2's callout said "randomization and controls exist precisely to close off rival stories" and linked neither, and its "your operational definitions *are* your data" pointed nowhere near where they get written down — now → `experimental-design-and-randomization` and → `codebooks-and-documentation`. §1.10 named "selection and attrition bias" and "better sampling" with no links; the attrition bullet now hands off to `stats-4/missing-data` (non-random dropout is the design-stage face of MNAR) and "sampling" to `sampling-methods`. §1.12's sample-size decision now reaches `effect-size-and-power` (a prereg's *n* has to be power-justified) and its "disguising exploration as confirmation" reaches `ethics/questionable-research-practices`. §2.2 closes the loop back to `variables-and-operationalization` (the codebook is where an operational definition is written down for good); §2.3's "influential outlier" clause, which jumped straight to `stats-2/regression-diagnostics`, now first hands the error-vs-genuine-extreme distinction to its sibling `outliers-in-practice`. **FAQ freshness:** five swaps, each displacing an answer that restated its lesson's own h2, duplicate-checked against all 291 existing Q&As first — **two candidates were killed before writing** (a "how do I choose between measures" answer §1.2's own FAQ #3 already owns, and a composite/reverse-scoring answer §2.6 `transformations-and-recoding` owns). What shipped: "My construct feels too rich to capture in a single number. Am I oversimplifying it?" (the reductionism worry, uncovered anywhere on the site), "How do I blind a study when I can't hide the treatment from participants?" (assessor-blinding, objective endpoints, PROBE = Prospective Randomized Open Blinded Endpoint — the lesson taught only single/double-blind), "Can I preregister a study that analyzes data I already have?" (secondary-data prereg and the prior-access attestation), "My variables come from a published questionnaire. Do I still need to document them?" (reproduce *your* scoring, not the instrument's), and "My data came from an online form, not hand entry. Do I still need validation?" (the manual-entry lesson modernized: straightlining, unit confusion, silent encoding changes, duplicate submissions). Every answer is conceptual — no numeric claim to verify this run, unlike the simulation-heavy prior batches — so PROBE's expansion and the secondary-data registry practice were the facts checked. **software.js — the sweep is complete.** My five lessons carry no `SOFTWARE` entry (correct: conceptual/toolkit). Verified run 9's four remaining outstanding entries against SPSS 31/32 + JASP 0.96/0.97 menu reality — `factorial-anova-two-way` (GLM → Univariate), `repeated-measures-anova` (GLM → Repeated Measures, Mauchly/Greenhouse-Geisser), `simple-linear-regression` (Regression → Linear, and it correctly makes *no* robust-SE claim, consistent with run 9's IBM finding), `power-analysis-for-complex-designs` (Analyze → Power Analysis 27+, G\*Power, `simr`) — all current, no changes. **All 34 `SOFTWARE` entries are now verified across runs 1–10.** **Snippets:** the two lessons with snippets (`variables-and-operationalization`, `data-entry-and-validation`) checked clean — `scale`/`rowMeans`/`zscore`, `dplyr::between`/`%in%`, pandas `.between`/`.isin`/`assert` all current. **Cross-pollination — the practice-problem back-link push, run since run 3, is finished.** The five lessons run 9 left (outliers-in-practice, reproducible-workflows, preregistration-and-open-science, types-of-data, psychometric-functions) each got a `pb-xref`, foregrounding its own angle: outliers → the "kept three ages as 999" judgment and reproducible → the log-as-audit-trail both cite Problem 41 but are framed differently (as run 9 differentiated Problem 22's two citers), preregistration → the four-QRP manuscript (Problem 40), types-of-data → the library-columns classifier (Problem 1), psychometric-functions → reading PSE/JND off logistic coefficients (Problem 34). **The map now closes: all 56 problem-cited lessons link back**, plus `guides/clean-survey-data` via the guides' own convention. **Quips:** all five reviewed, all five good, none changed (the codebook "It is still guessing", the validation "The capybara says please", and the prereg "wanted the small one" are among the site's best). **Backlog item — a stale hardcoded section number.** Built a checker cross-referencing every `problems.html` `pb-ref` §N.M against `curriculum.js` (CLAUDE.md flags these numbers as hand-maintained and drift-prone). It found **one**: the factorial-ANOVA `pb-ref` read "§2.4 Factorial ANOVA" but the lesson is **§2.3** (repeated-measures holds 2.4 after a renumber), so a reader clicking through met a number that didn't match the eyebrow they landed on — fixed, 43 labels now 0 mismatched. The same pass verified, all clean: the full `pb-xref` map is bidirectionally consistent (every back-link points at a problem that actually cites that lesson, 0 mismatches), every `pb-xref`'s stated "Problem N" matches its card (51 checked, 0 wrong), and an authoring-artifact sweep (TODO/FIXME/"link this to", the class run 7 caught) found nothing. **Verification:** `audit.js` **0 errors / 21 warn / 63 info** (baseline unchanged), `math-check.js` **270 assertions, 0 failures**, `prose-lint --strict` **exit 0** with **zero em-dashes added on any of the five** (page dash loads 7 / 3 / 4 / 5 / 3, all inside the ≤10 budget; every FAQ trio inside ≤3). All shared JS and `faq_data.py` parse; **19 JSON-LD blocks on the 10 changed pages parse**; every internal link and `#fragment` on the changed pages resolves (audit-proven). Browser pass over the localhost server on §1.10, §1.12, `types-of-data` and `problems.html`: **zero console errors**, the new FAQ questions and every new cross-link live with correct `href`s, the `pb-xref`s present, the §2.4→§2.3 fix rendering, and **zero horizontal scroll at 375px dark mode**. **No `CACHE_VERSION` bump** — no precached shell asset touched (`search-index.js` regenerated, but it is lazy-loaded, not precached). **One probe artifact, diagnosed not believed:** the pane reported horizontal overflow (`scrollWidth` 227 > `innerWidth` 0) before any screenshot forced a real viewport; resizing to 375px corrected the reading to 375 = 375, the documented 0×0 sandbox artifact.
  - **23 Jul 2026 — ninth run (the last never-refreshed inferential core, and a linter for the spelling problem).** The staleness ranking, run as "last commit that substantively changed this file, mass passes excluded", cleared to `stats-1/independent-samples-t-test` (26 Jun), `stats-2/regression-diagnostics` (27 Jun), `stats-3/manova` (3 Jul) and, from a genuine same-commit tie in the 4 Jul tier, `stats-1/effect-size-and-power` (P3) and `methods/from-question-to-hypothesis` (P4). **Three absences, each confirmed by a sitewide grep rather than assumed.** (1) **Independence.** `regression-diagnostics` listed "the four things to check" and omitted independence entirely, while `assumptions-and-when-they-break`'s own FAQ answers "which assumption matters most?" with **"Independence, without question"** — two Stats 2 lessons contradicting each other. Worse, `Durbin-Watson` appeared **only in `software.js`**, so the SPSS walkthrough told readers to tick a statistic no lesson explained. The list is now five, with a section on the check the residual plot cannot make: autocorrelation (with what the statistic's 0-to-4 range means, verified against the statsmodels docs) and clustering, handing off to `mixed-and-multilevel-models` as the repair. (2) **Hedges' g: zero hits sitewide**, on a site with an effect-size converter, an effect-size lesson and a meta-analysis lesson, and with §1.13's formula printing "/ standard deviation" without saying *which*. §1.13 now names the pooled SD and the small-sample correction. (3) **MANOVA had no effect size at all** — not "η²", not the phrase — on a lesson that lists four test statistics and a follow-up strategy. §3.11 gained η² = 1 − Λ^(1/s) with the two-group collapse worked through. Also: §2.10 stopped conflating leverage with influence and now hands both to `assumptions-of-regression`, which owns the FAQs for each; §1.12 finally names the Mann-Whitney fallback its own normality bullet implied; §1.13 points at the post-hoc-power warning that lives in §3.9; §1.1 reaches `variables-and-operationalization` and `preregistration-and-open-science`, the mechanism its own "before you collect data" refrain needs. **FAQ freshness:** five swaps, each displacing an answer that restated its lesson's prose, and **four candidates were killed by the duplicate check before being written** — a Cook's-distance answer (§3.8 owns it), a Likert t-test-vs-Mann-Whitney answer (§2.6 owns it), an optional-stopping answer (QRP owns it) and an exploratory-analysis answer (preregistration owns it). What shipped: "My two groups are men and women, not randomly assigned. Does the t-test still work?" (the arithmetic is identical and that is the trap), "How do I know whether a pattern in my residual plot is real or just noise?", "Does a significant MANOVA protect my follow-up ANOVAs from multiple testing?" (the protected-F myth; **"protected F" returned zero hits sitewide**), "I found a significant effect with a small sample. Doesn't that make it more impressive?", and "My hypothesis has three parts. Is that one hypothesis or three?" (which bridges Methods to the family-definition machinery run 8 wrote into §2.2). **Every number was derived twice.** The winner's-curse answer rests on 300,000 simulated studies per cell: with a true d = 0.30 and 20 per group, power is **15.3%** and the significant studies average **|d̂| = 0.82**, nearly three times the truth — and the same grid recomputed through `VIZ.nctCdf` gives **15.2%**, matching the simulation to 0.1 pp at every n from 10 to 50. The Hedges figures come from J = 1 − 3/(4df − 1) (**4.2%** at n = 10/group, **2.0%** at 20, **0.8%** at 50), and the simulation's mean d̂ reproduced d/J to four decimals independently. The MANOVA arithmetic (Λ = 0.62, two groups → η² = .38) and the lineup-protocol attribution (Buja et al., 2009) were both checked before publishing; a **Breusch-Pagan-in-GLM claim was dropped for being unverifiable rather than wrong**, following run 3's precedent. **software.js — run 8's five outstanding entries all verified, and one real gap closed.** SPSS 32 docs (Apr 2026) confirm `Analyze → GLM → Univariate` unmoved; JASP's `Assumption Checks → Sphericity tests` current; `psychometric-functions`' PSE = −b₀/b₁ and JND = ln(3)/b₁ re-derived from the logistic. The gap: **SPSS's Linear Regression dialog has no robust standard-error option**, confirmed by IBM's own support page and a 2026 AMPPS tutorial written because of it — while `regression-diagnostics` prose recommends robust standard errors as the fix for heteroscedasticity, sending students hunting for a checkbox that isn't there. Two tips added (the SPSS routes that do work; and that Durbin-Watson tests order, not clustering). **Snippets — one snippet that could not run.** `regression-diagnostics`' Python imported matplotlib and statsmodels, created an unused figure, and then called `fit.fittedvalues` with **`fit` never defined**. It now fits the model the way its own R half does, and both halves gained a Durbin-Watson line so the new prose has something runnable behind it. **Cross-pollination:** practice-problem back-links **46 → 51 of 57**, adding `assumptions-of-regression`, `multicollinearity-and-variable-selection`, `roc-curves-and-auc` (ML now 2), `generalized-linear-models` and `nonsignificant-results`. Every description was checked against the problem's actual sub-questions first, which is what stopped §3.8's line from simply restating §2.10's (both cite Problem 22, so §3.8's foregrounds the case-31 leverage/Cook's-D judgment instead). **Six remain:** outliers-in-practice, reproducible-workflows, preregistration-and-open-science, types-of-data, psychometric-functions, and `guides/clean-survey-data` (already linked by the guides' own convention). **Quips:** ten reviewed, eight kept, two rewritten — `regression-diagnostics` said "mirror" twice in two sentences with the second explaining the first, and `manova` was a summary ending in the "That's ‹topic›." formula that three quips in this batch shared. **Backlog item — stop finding British spellings one run at a time.** Runs 2, 6 and 7 each found another survivor of the 22 Jul word-list sweep, and run 7 declared the site "verifiably at zero"; this run found **`flavour` in four more files**, including the Stats 4 course landing page. Rather than fix four more words, **VOICE.md gained HARD RULE 12 and `prose-lint.js` a `britspell` pattern** (budget 0, driven by an explicit `BRIT_SPELLINGS` inventory — an inventory, not an `-ise`/`-our`/`-re` rule, because run 2 proved those cannot be generalized without turning *analyses* into "analyzes"). On its first run it immediately caught **three more** that all four hand sweeps had missed: `modelling` in `assumptions-and-when-they-break`, a `<h2>Flavours of each</h2>` heading in `reliability-and-validity`, and `unravelling` in `informed-consent-and-irb`. Sitewide count is now **0 and checked**, including FAQ answers linted from `faq_data.py`, which is the exact path the original regression travelled. `analyses`, `enrolled`, `enrolling`, `programmed` and `analogue` are deliberately excluded and documented as such in three places. **Verification:** `audit.js` **0 errors / 21 warn / 63 info** (baseline unchanged), `math-check.js` **270 assertions, 0 failures**, `prose-lint --strict` **exit 0** with **zero em-dashes added to any of the five** (9→9, 7→7, 9→9, 8→8, 3→3) and §2.10's FAQ trio improving 3 → 2. All shared JS, `faq_data.py`, `sw.js` and both tools parse; **22 changed pages · 88 inline scripts · 43 JSON-LD blocks, 0 failures**; all 228 internal links and fragments on the changed pages resolve. Browser pass on all twelve content-changed pages via same-origin iframes: **zero console errors, zero horizontal scroll, no leading-slash links**, every expected new string present; plus a dark-mode 375px pass on §3.11 confirming the new η² section renders its superscript, reaches the mini-TOC, and carries the new quip. **`CACHE_VERSION` bumped sc-v27 → sc-v28** (two QUIPS changed and `site.js` is precached). **Process note:** I wrote "which quietly assumes" into §1.13 and caught it by reading the diff back, not from the linter — the fifth consecutive run to trip on VOICE.md's easiest rule. One probe artifact, diagnosed rather than believed: my link checker reported `power.html?sc=anova` broken, which was the query string, not the file.
  - **23 Jul 2026 — eighth run (the never-refreshed core lessons).** The staleness ranking, run as "last commit that substantively changed this file, voice/meta/OG mass passes excluded", cleared to five lessons no previous run had touched as its subject: `stats-2/post-hoc-tests` and `assumptions-and-when-they-break` (27 Jun), `stats-3/logistic-regression` and `mediation-and-indirect-effects` (27 Jun), and `stats-4/bayesian-thinking` (29 Jun). **Three topics turned out to be missing from the whole site, not just from these pages** — a sitewide grep, not a guess, returned zero hits for each. (1) **False discovery rate.** `Benjamini`, `false discovery` and `FDR` appeared nowhere across every HTML, JS and Python file, on a site whose §2.2 "why it matters" callout already named genomics and brain imaging, the two fields that abandoned family-wise control precisely because of this. §2.2 gained a section on it with a worked ten-p-value comparison (Bonferroni asks all ten to clear .005 and returns one; BH asks .005/.010/.015/… and returns three at a stated ~5% false share), plus a callout on which rate to control and why the choice must be declared. (2) **Baron and Kenny.** Zero hits, on the recipe every mediation textbook still teaches. §3.6 gained a section retiring step one, with the two reasons a real indirect effect can sit under a null total effect (power for the total is not power for the paths; an opposite-signed direct path cancels part of the sum). (3) **MCMC.** Zero hits across two Bayesian lessons, while §4.2's own prose credited "modern computing" without saying what the computing does. It now explains why the lesson's Beta example has a closed form and realistic models do not. Also: §2.5's "big three" omitted **linearity** entirely and never linked its own sibling `regression-diagnostics`, both fixed; §3.7 had no inferential content at all despite its SPSS box telling readers to read the omnibus χ² and Nagelkerke R², so it gained the section that explains them plus the Wald-vs-likelihood-ratio caveat; §3.6 now reaches `causal-dags-and-confounding` with the assumption the three-regression recipe never states (randomizing X does not randomize M). Tool links added where they earn their place: `power.html?sc=anova`, `descriptives.html`, `effect-sizes.html` (OR ↔ *d*), `distributions.html` (the Beta the §4.2 playground draws). **FAQ freshness:** five swaps, each displacing an answer that restated its own lesson's prose, and **two candidates were killed by the duplicate check before they were written** — a "Welch by default" answer for §2.5 (§1.10 already owns it, in two of its three answers) and a residuals-vs-raw-data answer (§3.8 already owns it). What shipped instead: "Do I have to correct for every test in my whole paper?" (defining the family is a judgment you state, and redrawing it after seeing the p-values is p-hacking with arithmetic), "Can I let the assumption test decide which test I run?", "How many participants does a mediation analysis need?", "Why is my odds ratio in the millions, with a confidence interval to match?" (separation), and "How strong is my prior, in units of data?". FAQ em-dashes across the five fell 11 → 7. **Every number in them was simulated or derived, not recalled.** The Levene-then-choose answer rests on 200,000 samples per cell: with n = 45 and 15 and the larger variance in the smaller group, always pooling gives **15.2%** false positives at a nominal 5%, Welch gives **4.9%**, and letting Levene choose gives **5.7%** — better than pooling, still not what it advertises; with equal variances Welch costs **0.05 to 0.12 percentage points** of power, which is the honest reason the default is free. Mediation power came from a percentile-bootstrap simulation (500 replications × 1,000 resamples per point): two medium paths (a = b = .39) reach 80% at about **70**, two small paths (a = b = .14) at about **550**, against **398** for detecting a single *r* = .14 — so the product costs more than either half. That simulation independently reproduces Fritz & MacKinnon's published 78 and 558, and the Fisher-*z* helper reproduces the site's own G\*Power anchor (*r* = .3 → 84.9 ≈ 84). Separation was demonstrated by hand-rolling Newton-Raphson on eight perfectly split points: the odds ratio passes **4 million** by iteration 10 and the Hessian goes singular by iteration 20. The Beta answer is exact rather than simulated — a Beta(α, β) prior is worth α + β trials, which is literally what §4.2's "prior confidence" slider sets — and was **read back off the live page**: at the defaults the posterior mean is .654 = 17/26, and at confidence 80 it is .540. **software.js — the backlog item was a missing entry, not drift.** `assumptions-and-when-they-break` carried an R/Python snippet but **no SPSS/JASP walkthrough**, sitting in the audit's info list as "fine if conceptual" when it is the single most software-dependent topic in Stats 2 ("where is the Q-Q plot in SPSS?"). It now has one (Explore → Normality plots with tests, Spread vs Level with Levene, ZRESID/ZPRED for the regression case; JASP's Assumption Checks in both t-test and ANOVA, and the residual Q-Q that replaces one plot per group), taking the site to **34 entries**. Four of run 7's outstanding nine were verified with no changes needed (`logistic-regression`, `effect-size-and-power`, `multicollinearity-and-variable-selection`, `generalized-linear-models` — SPSS 27+ Power Analysis, Collinearity diagnostics, and the GLM Type-of-Model tab are all long-stable, as are the JASP Power and GLM modules). **Tally 28 of 34 verified; the five outstanding are** factorial-anova-two-way, repeated-measures-anova, simple-linear-regression, power-analysis-for-complex-designs, psychometric-functions. Tips were added where the new prose created an obligation: the FDR note on §2.2, and separation plus Wald-vs-LR on §3.7. **Snippets — one genuine error.** §2.5's Python labelled a line "Welch fallback for unequal variances" and then called `scipy.stats.f_oneway`, which is Fisher's equal-variance ANOVA; its own trailing comment admitted as much. It now calls `pingouin.welch_anova`. §2.2 gained `p.adjust(method = "BH")` and `multipletests(..., method="fdr_bh")` so the new FDR section has something runnable behind it. The other three lessons' snippets checked clean. **Cross-pollination:** back-links **41 → 46 of 56**, adding `assumptions-and-when-they-break` (Problem 17, the two clinics whose SDs are 3.1 and 6.9) plus five orphans — effect-size-and-power, independent-samples-t-test, regression-diagnostics, `ml/classification-metrics` (taking ML off zero) and `writing/writing-results`. Every pb-xref description was checked against the problem's actual sub-questions, and the first draft for §1.13 was rewritten after that check showed it described the wrong three sub-questions. **Ten remain**, now concentrated in data (1/3) and the two stats-3/stats-4 pairs. **Quips:** ten reviewed, eight kept, two rewritten — `effect-size-and-power`'s "isn't just significant. The effect size is massive" was VOICE.md's banned contrast punch with a period doing the dash's job (quips are linter-exempt, which is exactly why they need reading), and `writing-results`' was a 30-word summary rather than a line, the same defect run 2 fixed on two other pages. **Verification:** `audit.js` **0 errors / 21 warn / 63 info** (info fell 64 → 63: one fewer missing SOFTWARE entry), `math-check.js` **270 assertions, 0 failures**, `prose-lint --strict` **exit 0** with **zero em-dashes added on any of the five** and every page inside budget (§4.2 started at exactly 10 of 10 and stayed there). All shared JS, `faq_data.py` and `sw.js` parse; **10 changed pages · 40 inline scripts · 20 JSON-LD blocks, 0 failures**; all 77 internal links and every `#fragment` on the changed pages resolve. Browser pass on desktop light, desktop dark and 375px mobile: **zero console errors across all ten pages, zero horizontal scroll at 375px**, every new h2 picked up by the mini-TOC, both new quips live, and the injected blocks confirmed showing the new SPSS entry, the FDR tip, the separation tip and the fixed Python snippet. **`CACHE_VERSION` bumped sc-v26 → sc-v27** (two QUIPS changed and `site.js` is precached). **One process note:** the first in-browser probe reported the new tips and snippets missing, which was the probe's fault, not the site's — `software.js`'s `tips` render in the **APA block**, not the `.software` block, and the snippet block defaults to its R tab so Python text is absent from `innerText` until the toggle is clicked. Re-probed against the right containers, all four landed. Run 4's lesson generalizes: check what the negative result actually measured before believing it.
  - **23 Jul 2026 — seventh run (the research-integrity block).** `plagiarism-authorship-and-citation`, `privacy-and-confidentiality`, `ai-in-research-ethics`, `fraud-and-self-correction` and `methods/the-replication-crisis`. **This run found the three defects a freshness loop exists to catch, none of which any gate can see.** (1) **An authoring instruction had shipped:** the replication-crisis lesson's low-power bullet ended "(link this to <a …>effect size &amp; power</a>)" — a note-to-self that survived into production, on an already-linked phrase. It is the only one on the site; a sitewide sweep for `TODO`, `FIXME` and similar found nothing else. (2) **A tool link pointed at the wrong tool:** the fraud lesson described statcheck as "roughly the idea already built into our tables calculator", but `tables.html` contains **zero** statcheck or consistency logic while `apa.html` has the actual recompute-and-warn check. Repointed, with a sentence saying what it does. (3) **The site's most time-sensitive claim had dated.** `ai-in-research-ethics` is deliberately built to age well (it names no tool, no version, no dated policy), but it rested on "a language model predicts plausible text; it does not know facts, look things up, or calculate" — which stopped being the whole story once assistants routinely search and run code. The pedagogy survives and the rule is unchanged, so the fix keeps the point and adds the nuance that matters: you usually cannot tell from the answer which mode produced it, a retrieved source can still be summarized wrongly, and code still runs on whatever inputs you gave it. The dependent bullet, the interactive's feedback text for the "ask AI for a statistic" case, and the FAQ on invented references were all updated to match, since leaving any of them would have made the lesson contradict itself. **FAQ freshness:** four swaps plus that consistency fix. The Ethics FAQs were the least repetitive of any tier so far, so the swaps went to genuinely absent questions: "How much do I have to change a sentence for it to count as a paraphrase?" (there is no word count, and the test is procedural), "Can I share my data publicly if I promised confidentiality?" (the open-data collision, decided at consent time or not at all), "I think I have found an error in a published paper. What should I do?" (assume a mistake, write it down precisely, author first then editor then PubPeer), "Does a failed replication mean the original was wrong?" (four things that produce a non-replication, only one of which is a false positive), and — replacing the one FAQ that named specific products — **"Is it unfair to use AI to improve my English if I am not a native speaker?"**, which the site had never addressed despite it being a live anxiety for much of its audience. **Drift:** four more of run 6's outstanding thirteen verified with no changes (post-hoc tests, non-parametric alternatives, regression diagnostics, survival analysis; SPSS's Post Hoc/Games-Howell, Nonparametric Tests + Legacy Dialogs, ZRESID/ZPRED + Cook's + Durbin-Watson, and Kaplan-Meier/Cox are all long-stable, and the JASP paths including the Survival module are current). **Tally now 24 of 33; the nine outstanding are** effect-size-and-power, factorial-anova-two-way, repeated-measures-anova, simple-linear-regression, multicollinearity-and-variable-selection, logistic-regression, power-analysis-for-complex-designs, generalized-linear-models, psychometric-functions. **Cross-pollination:** back-links **40 → 41**; remaining 16 listed in this run's output, still weighted to data (1/3), ml (0/2) and writing (2/4). **Quips:** all five reviewed. Ethics quips are deliberately earnest (CLAUDE.md records the decision that serious lessons should not carry a flippant line, which is why they print hidden), so the register was left alone; two that read as 22-word summaries rather than lines were tightened, and the three with a real turn — privacy, fraud's "distrusts water that's suspiciously clear", and the replication crisis's "Then remembered the first three" — were left exactly as they were. **Backlog item — finishing the spelling normalization for good.** Run 6 fixed `artefact` and `enrol`; this run found the list was still incomplete and swept the rest: **`honour`** (4 files, including the homepage accessibility statement and a JS string in an interactive), **`cancelling`/`cancelled`** (8), **`unravelled`**, plus `standardised`/`centred`/`colour` left in two generator scripts' comments. **21 more replacements across 13 files, and the site is now verifiably at zero** — a scan for the full British inventory returns nothing outside `snippets.js`, whose `color="grey"` and `"grey40"` are ggplot and matplotlib literals that must not change, the same exclusion the original sweep recorded. `enrolled`, `enrollment`, `programmed` and `analogue` were checked and correctly left, being valid American forms. **Verification:** `audit.js` **0 errors / 21 warn / 64 info**, `math-check.js` **270 assertions, 0 failures**, `prose-lint --strict` **exit 0**; all shared JS, both Python generators, and **27 changed pages'** inline scripts and JSON-LD parse; every prose link and `#fragment` on the five lessons resolves; **the AI lesson's eight-case interactive was driven end to end in the browser** to confirm the rewritten feedback string renders and the case still scores correctly; zero console errors. **`CACHE_VERSION` bumped sc-v25 → sc-v26** (two QUIPS changed, and `site.js` is precached). Process held this time: every prose diff was read back line by line before the gates ran, which caught two British spellings and a redundant clause in freshly drafted FAQ text.
  - **23 Jul 2026 — sixth run (the Methods design block).** `between-vs-within-designs`, `experimental-design-and-randomization`, `quasi-experiments`, `observational-designs` and `survey-and-questionnaire-design` — chosen because the staleness ranking and the practice-problem back-link gap pointed at the same five lessons, so both legs of the prompt landed on one coherent block. **The gaps were inward-facing:** §1.4 discusses random *sampling*, placebos and "the next two lessons" and linked **none** of `sampling-methods`, `bias-and-blinding`, `quasi-experiments` or `observational-designs`; §1.5 credited repeated-measures ANOVA for the within-subjects advantage without mentioning the paired *t*-test that is the two-condition case (and which links back here since run 2); §1.6's matching bullet now says which variables belong in a match is a question about causal structure, closing the loop with the DAG lesson that started pointing here in run 5; §1.9 now connects Likert items to their measurement level and to the cleaning guide, on the grounds that the reverse-coded items and optional questions you invent at design time are exactly what someone untangles later. **FAQ freshness:** five swaps, and this block needed them more than any tier so far — **all three answers on three of the five lessons restated their own prose**. New: "What is a mixed design?" (between × within, which the lesson never named despite being the commonest real design), "How do I actually randomize? Is alternating participants good enough?" (allocation concealment and block randomization, the practical half nobody teaches), "When does regression discontinuity fail?" (bunching at the cutoff, locality, bandwidth sensitivity), "Isn't a huge observational study better than a small randomized trial?" (precision is not bias, and §1.4's own playground is the proof), and "Should I make every question required?" (forcing an answer relocates the problem into the missingness mechanism). FAQ em-dashes across the five fell 11 → 7. **Drift:** the toolkit courses hold only **two** `SOFTWARE` entries between them (`reliability-and-validity`, verified in run 1, and `transformations-and-recoding`, verified now — SPSS `Compute Variable`/`Recode into Different Variables`/`Save standardized values`, `MEAN()` skipping missing, and JASP's computed columns all current), and Writing and Ethics have none at all, correctly. Their 16 Methods/Data snippets are base R, tidyverse, scipy and pandas throughout, all current. **Running tally worth keeping: 20 of the 33 `SOFTWARE` entries have now been verified across runs 1–6; the 13 outstanding are** effect-size-and-power, post-hoc-tests, factorial-anova-two-way, repeated-measures-anova, non-parametric-alternatives, simple-linear-regression, regression-diagnostics, multicollinearity-and-variable-selection, logistic-regression, power-analysis-for-complex-designs, generalized-linear-models, survival-analysis, psychometric-functions. **Cross-pollination:** back-links are now **40 of 57**, with Methods going **1/7 → 5/7** in a single run because the lessons being refreshed were the ones missing them. Remaining by course: stats-1 8/11, stats-2 8/10, stats-3 6/8, stats-4 8/10, methods 5/7, data 1/3, writing 2/4, ml 0/2 — 17 lessons left, listed in this run's output. **Quips:** all five reviewed, all five good (the survey one, which agrees with its own leading question, is among the best on the site), none changed. **Backlog item — a second spelling regression, same class as run 2's:** the 22 Jul American-spelling sweep worked from a word list, and the list did not contain **`artefact`** or **`enrol`**. Both had survived everywhere since: 10 `artefact`/`artefacts` and 11 `enrol`/`enrols` across `problems.html`, `quiz.html`, `checks.js`, `glossary-data.js`, `faq_data.py`, `methods/observational-designs`, `ml/clustering-kmeans` and `ml/dimensionality-reduction`. All 21 normalized to artifact/enroll, with `enrolled` and `enrollment` deliberately left alone (both are already correct American forms) and **`snippets.js` deliberately excluded** — its one `grey` is `color="grey"` inside runnable matplotlib, a code literal rather than prose, which is the same call the original sweep made and recorded in its commit message. Verified by rendering: no British form survives in the rendered text of any touched page. **Verification:** `audit.js` **0 errors / 21 warn / 64 info**, `math-check.js` **270 assertions, 0 failures**, `prose-lint --strict` **exit 0**; all shared JS and all 18 changed pages' inline scripts and JSON-LD parse; every prose link and every `#fragment` on the five lessons resolves; browser pass at 375px light with **zero console errors** and no horizontal scroll. **No `CACHE_VERSION` bump** — no precached shell asset was touched (`checks.js` and `glossary-data.js` are lazy-loaded). **One honest note on process:** an early edit to `methods/quasi-experiments` truncated a sentence mid-word and left a placeholder in the file; it was caught immediately, reverted, and the diff re-inspected to confirm only the intended change remained. Three "quietly"s and one "favourable" were also caught in freshly drafted FAQ text before the linter ran. Drafting at this pace needs the diff read back every time, not just the gates.
  - **23 Jul 2026 — fifth run.** The four 1 Jul Stats 4 lessons (`causal-dags-and-confounding`, `cross-validation-and-overfitting`, `meta-analysis`, `missing-data`) plus `stats-3/power-analysis-for-complex-designs`. These are visibly later-written than earlier tiers and were already well cross-linked, so the value this time was in **content gaps rather than missing links**. Three worth naming: the DAG lesson taught adjustment at length and **never mentioned randomization**, the one move that closes every backdoor path including the ones through variables you failed to measure, so it now hands off to `methods/experimental-design-and-randomization` and to quasi-experiments for when that is impossible; the cross-validation lesson had no warning that **CV stops being unbiased once you use it to choose something**, so it gained a section on the selection optimism and the two standard fixes (a once-only test set, or nested CV); and the missing-data lesson now points upstream at the codebook and cleaning-script habits that make the whole problem tractable. `meta-analysis` gained the two links its own text implied but never made (`the-replication-crisis`, `preregistration-and-open-science`) plus the effect-size converter, since pooling requires one scale first; the power lesson gained the ICC/multilevel link behind its own clustering bullet, and `plan.html` alongside `power.html`. **FAQ freshness:** five swaps — "What if I can't tell whether a variable is a confounder or a collider?" (the answer is temporal order, and when that fails, report both graphs), "Can I use cross-validation on time-series data?" (rolling-origin, and split by person not by row), "How many studies do I need for a meta-analysis?" (two is arithmetically possible; τ² is guesswork below about five; comparability outranks count), "How do I report missing data in a paper?", and "How big should a pilot study be?". **That last one also resolved a duplicate this loop created:** run 2 added "How many participants do I need to detect an interaction?" to `factorial-anova-two-way`, which left the power lesson's "Why do interaction effects need bigger samples?" answering the same question on another page. The interaction-N question now lives on one page only. FAQ em-dashes across the five fell 14 → 6. **Drift — the ML course, which no previous run had audited:** all twelve ML lessons have **no `SOFTWARE` entry at all**, which is correct rather than missing (SPSS and JASP do not do random forests), and their twelve Python snippets are current sklearn idiom. Checked against the 1.7/1.8 deprecation lists specifically: the `n_alphas` deprecation in `LassoCV`, the liblinear multiclass removal, `PassiveAggressive*`, and `stable_cumsum` **touch none of the site's calls**, and the snippets already do the right thing on the things that bite (scalers inside pipelines, explicit `n_init`, permutation importance over impurity importance). One small correction: the power snippet's R and Python halves return **different units** — `pwr.anova.test` gives *n* per group, `FTestAnovaPower().solve_power` gives total *N* — and only the Python line said so; the R line now does too (they agree: 53 per group, 159 total, the G*Power anchor `math-check.js` already asserts). **Cross-pollination:** the practice-problem back-link count is now **35 of 57**, up from 21 when this work started. The remaining gap is concentrated almost entirely in the **Research Toolkit**, whose whole problem set (p5-1…p5-6, added in P64) had **no back-links at all**; five of the six now do — questionable-research-practices, data-cleaning-workflow, reporting-statistics-apa, sampling-methods and tables-and-figures. Per-course coverage for the next run: stats-1 8/11, stats-2 8/10, stats-3 6/8, stats-4 8/10, methods 1/7, data 1/3, writing 2/4, ethics 1/1, ml 0/2, guides 1/1. **Quips:** all five reviewed, all five good, none changed. **Backlog item:** `guides/clean-survey-data` was the one non-lesson entry on the back-link list, and it now points at Problem 41 from its existing "Keep going" list rather than borrowing the lessons' `pb-xref` convention. **Also verified, both clean:** all **40** `pb-xref` → problem references are consistent in *both* directions (every lesson that claims "Problem N does X" points at a problem that actually cites that lesson), and run 4's new anchor check still passes sitewide. **Noted, not actioned — a design question for Hakan, not a session:** **`.pb-xref` has no CSS rule anywhere**, in `styles.css` or any inline `<style>`. It has been a semantic-only class since P64, so all 40 of those paragraphs render as ordinary body text. That may well be deliberate (a quiet closing sentence beats a boxed callout at the end of every lesson), and styling it would touch a precached shell asset, so it is left alone rather than decided unilaterally. **Verification:** `audit.js` **0 errors / 21 warn / 64 info**, `math-check.js` **270 assertions, 0 failures**, `prose-lint --strict` **exit 0** — and worth recording that "quietly" was caught and reworded **three times** while drafting this run, twice before the linter ever ran; it is the single easiest VOICE.md rule to break. All shared JS and all 11 changed pages' inline scripts and JSON-LD parse; browser pass at 375px light with **zero console errors**, no horizontal scroll, and the new DAG section picked up by the mini-TOC. **No `CACHE_VERSION` bump** (`site.js` untouched). Two probe artifacts, both diagnosed rather than assumed: three "404"s were my own URL rewriting mangling single-`../` sibling links (all three return 200 to curl, and `audit.js` proves every internal link resolves), and the screenshot tool hung on both attempts — an eval confirmed the page was fully rendered and responsive with the **Ko-fi image the only incomplete resource** (`naturalWidth: 0`), which is exactly the sandbox artifact CLAUDE.md documents.
  - **23 Jul 2026 — fourth run (the next tier).** `stats-1/confidence-intervals` and `probability-basics` (29 Jun) plus the three oldest **Stats 4** lessons, `bootstrap-and-resampling`, `bayesian-estimation` and `mixed-and-multilevel-models` (29 Jun) — the last core course no P39 run had reached. **`probability-basics` had zero outbound links of any kind**, on a lesson that is a prerequisite for half the site. It now reaches forward to the CLT (the law says where the proportion settles, the CLT describes the shape of the wobble on the way), to sampling distributions and confidence intervals from its own closing callout, and it gained **a section on conditional probability** — the lesson taught four rules and omitted P(A | B), which is the one the whole Bayesian strand and every association test rest on. §1.9 gained the two calculators that postdate it (`descriptives.html` builds the interval from pasted data, `tables.html` supplies the critical value); §4.1 gained the confidence-intervals link its own percentile-interval section implied, a cross-validation link, **and a paragraph on BCa intervals** (percentile intervals are biased for skewed or biased statistics, which the lesson never mentioned); §4.3 and §4.5 gained the `pb-xref`s they never had, plus the Beta distribution in `distributions.html` and the design lesson that decides whether you need a mixed model at all. **FAQ freshness:** five swaps, each displacing an answer that restated the lesson's prose. The new ones: "Can I tell whether two groups differ by seeing if their confidence intervals overlap?" (**derived, not recalled** — non-overlap does imply p < .05, but the converse fails, and for two equally precise means the difference reaches p = .05 while the intervals still overlap by **29.3%** of their length; 200,000 random configurations produced **zero** counter-examples to the non-overlap direction), "A test is 99% accurate and I tested positive. Am I 99% likely to have the disease?" (**recomputed exactly**: 1-in-1,000 prevalence, 99% sensitivity, 1% false-positive rate → 99 true and 999 false positives out of 1,098, so **P(ill | positive) = 9.02%**), "My bootstrap interval and my t-interval disagree. Which one do I trust?", "How do I test a hypothesis with a credible interval?" (the ROPE), and "Why doesn't my mixed model print p-values?" (the lme4 denominator-df question, with Satterthwaite/Kenward-Roger and the likelihood-ratio alternative). Duplicate-checked against all 291 existing Q&As; `bayesian-thinking` already owns "How do I choose a prior?", which is why the prior question was not the one to add. **software.js:** the Stats 4 family verified — SPSS Mixed Models, Bootstrapping, Bayesian Statistics (25+), Meta Analysis (28+), Multiple Imputation, and the JASP twins all current. The "JASP currently has no imputation module" claim was **re-checked rather than assumed** and still holds (it remains an open feature request). **Snippets — one factual error:** the meta-analysis snippet labelled `rma(yi, sei)` as "DerSimonian-Laird family", but metafor's default is **REML**; corrected, which also makes it agree with the JASP walkthrough that already said REML. Everything else in the Stats 4 snippets checked clean (`rng.choice` does resample with replacement by default, `combine_effects` does take variances and is passed `se**2`, `performance::icc` and `smf.mixedlm` current). **Cross-pollination:** measured the gap rather than guessing at it — **57 lessons are cited by a practice problem and only 21 linked back**, so `problems.html`, one of the newest tools, was largely a one-way street. Five of the most-trafficked orphans now link their problem: the CLT (the skewed-pizza-times problem), sampling distributions, hypothesis-testing logic (the newsletter that misreads *p* = .030), post-hoc tests, and non-parametric alternatives (Mann–Whitney by hand). That takes the count to 26 of 57; the remaining 31 are a standing, finishable list for later runs. **Quips:** all five reviewed, all five good, none changed. **Backlog item — a new audit check.** P38's remaining note is the worktrees it deliberately left for Hakan, so this run took a gap of its own making: I have now added ten `problems.html#pN-M` links across runs 2–4, and **nothing verified that those fragments exist.** `audit.js` proved every link resolved *as a file* while a typo'd `#p2-99` would have silently dropped the reader at the top of the page. CHECK 4 now also verifies that any `#fragment` on an internal link names a real `id` in the target file, with a short **`JS_IDS`** allow-list for the two anchors that only exist after `site.js` runs (`run-it`, `main-content`). It runs clean over the whole site today, and a deliberately corrupted anchor was confirmed to fail it. Documented in `CLAUDE.md` and `AGENTS.md`. Two related things were checked and found already correct: all 40 real cross-file fragments resolve, and all 13 `plan.html` leaves that deep-link `<lesson>/#run-it` point at lessons that actually have a `SOFTWARE` entry. **Verification:** `audit.js` **0 errors / 21 warn / 64 info** (the warning count spiked to 118 mid-run purely because re-injecting FAQs bumps all 97 lesson mtimes ahead of the search index — rebuilding restored the baseline, and the new check contributed nothing to it), `math-check.js` **270 assertions, 0 failures**, `prose-lint --strict` **exit 0** with no em-dash added on any of the five. All shared JS, all 18 changed pages' inline scripts and JSON-LD parse; every prose link and every anchor on the ten touched lessons resolves; browser pass mobile-light with **zero console errors**, no horizontal scroll at 375px, and the new §1.6 section picked up by the mini-TOC. **No `CACHE_VERSION` bump** (`site.js` untouched; `snippets.js`/`software.js` are lazy-loaded, not precached). One process note for the next run: a `DOMParser`-based link probe reported missing `pb-xref` blocks that were demonstrably present in the served bytes and in the live DOM — **verify against a real loaded document (an iframe) rather than a parsed string** before believing a negative result.
  - **23 Jul 2026 — third run (same day as the second; the next five lessons).** With run 2 committed, the staleness ranking cleared to a natural batch: `stats-2/chi-square-tests` (27 Jun) and the four oldest Stats 3 lessons, `categorical-predictors-and-dummy-coding`, `interactions-in-regression`, `model-comparison` (27 Jun) and `factor-analysis-pca` (29 Jun). **Stats 3 had never been touched by a P39 run, and it showed:** two of the five (`model-comparison`, `factor-analysis-pca`) had **no outbound links anywhere in their prose**, and `model-comparison`'s closing line promised "the bridge from classical statistics to machine learning" while linking nothing. Each got what it was missing: §2.7 the effect-size converter (for Cramér's *V* / OR) and `power.html?sc=chisq`; §3.3 a link forward to interactions **plus a new section on effect coding** (the −1/0/1 alternative, where the intercept becomes the grand mean, which is where factorial regressions get misread); §3.5 the power warning and a `pb-xref` it never had though two problems cite it; §3.9 cross-validation, the ML course, and a `pb-xref`; §3.10 `ml/dimensionality-reduction` and `methods/reliability-and-validity`. **FAQ freshness:** five swaps, again each displacing an answer that restated the lesson's own prose (§2.7's expected-count formula, §3.3's "choosing the reference", §3.5's coefficient interpretation, §3.9's "why R² always increases", §3.10's PCA-vs-FA callout) — replaced with "Which cell is responsible?" (adjusted standardized residuals), "What if my categorical variable has 20 or 50 levels?", "Do I have to keep the main effects if only the interaction is significant?" (the marginality principle), "Can I compare AIC across models fitted to different data?" (the missing-values trap that silently changes *n*), and "What is rotation, varimax or oblimin?". Duplicate-checked against all 291 existing Q&As first. FAQ em-dashes across the five fell 11 → 7. **Everything quantitative was verified rather than asserted:** effect vs dummy coding recomputed on a deliberately **unbalanced** 3-group design — dummy *b*₀ = 46 = the reference mean, effect *b*₀ = 60 = the mean of the group means and **not** the 60.83 mean of all observations (the nuance the wording had to get right), with predictions agreeing to 1.4e-14 and identical R²; and the adjusted-residual rule checked by simulation (3×4 table, *N* = 400, 4,000 replications under true independence → mean −0.000, SD 1.006, 5.14% beyond |1.96|, so "beyond about ±2" is calibrated). Two `pb-xref` descriptions were written, checked against the actual problems, and **one was wrong and rewritten**: Problem 37 supplies log-likelihoods and *k* to compute AIC/BIC *from*, it does not hand you finished criteria. **software.js:** the Stats 3 family (factor analysis, multiple regression, ANCOVA, MANOVA, model comparison, interactions, dummy coding) read as current against SPSS 31/32 and JASP 0.96/0.97 — no menu path has moved. One claim was **removed for being unverifiable rather than wrong**: the EFA step asserted parallel analysis is "JASP's default", which no authoritative source confirms, so it now says JASP offers it in the same dropdown. **Snippets — one genuine bug, not a deprecation:** `model-comparison`'s Python called `m1.compare_f_test(m2)` with the models the wrong way round (statsmodels wants the **larger** model as receiver and the restricted one as argument, so the shipped code returned a negative-df nonsense F), and it carried a leftover editorial note, "# wait: call on the LARGER model", into production. Fixed and confirmed against the statsmodels docs; the chi-square R snippet also gained `$stdres` so it now answers the new FAQ. **Cross-pollination:** the newest core course, **ML & AI, had zero inbound links from Stats 1–3** — every one of its four lesson inbounds came from Stats 4. It now has four more, all at genuine handoffs: §3.9 → the `ml/` **course landing page** (the first link any lesson has ever made to a P61 landing page, which are otherwise reachable only through JS-rendered nav), §3.10 → dimensionality reduction, §3.7 logistic regression → classification metrics, §3.2 multicollinearity → ridge and lasso. **Quips:** all seven touched pages reviewed, all seven good, none changed. **Backlog item cleared:** P38's remaining note is the stale worktrees, which it deliberately left for Hakan, so this run took a duplicate found in passing — `ml/dimensionality-reduction` and `stats-3/factor-analysis-pca` both answered "how many components should I keep?", on two lessons that now link to each other. The retention question stays where the lesson has a section on it (§3.10) and the ML one became "Do I need to scale my variables before PCA?", which nothing on the site covered. (The run's §3.10 swap had already resolved a second copy of the same problem: PCA-vs-factor-analysis is now answered on exactly one page.) **Verification:** `audit.js` **0 errors / 21 warn / 64 info**, `math-check.js` **270 assertions, 0 failures**, `prose-lint --strict` **exit 0** — it caught one violation mid-run, a "quietly" that VOICE.md bans even literally, and the AIC answer was reworded; all shared JS, all eight changed pages' inline scripts and JSON-LD parse; every prose link on every changed page fetches 200; browser pass desktop-dark and mobile-light, **zero console errors**, no horizontal scroll at 375px, and the new §3.3 section auto-appeared in the mini-TOC. **No `CACHE_VERSION` bump this time** (correctly: `site.js` was untouched, and `snippets.js`/`software.js` are lazy-loaded, not precached). Worth recording: the first browser check showed the *old* snippet, which is the documented **stale-while-revalidate** path doing its job rather than a bug — the SW served the cached copy, revalidated behind it, and the next navigation rendered the fix with no bump and no cache clearing. That is the behaviour P33 was built for, now observed end to end.
  - **23 Jul 2026 — second run.** **The 5 stalest lessons** (last *focused* commit per file, mass passes excluded, run-1's six skipped) were `stats-1/what-is-statistics` + `one-sample-and-paired-t-tests` (26 Jun) and `stats-2/one-way-anova` + `factorial-anova-two-way` + `repeated-measures-anova` (27 Jun). Content held up; every edit was a missing cross-link to something built later, plus one real content gap. §1.1, the site's front door, had **no link outside Stats 1** at all: it now points "well-chosen" at `methods/sampling-methods` (with a clause on why that word carries the weight) and the vocabulary section at the glossary (created 1 Jul, after the lesson). §1.11 gained `tables.html` for exact criticals and `methods/between-vs-within-designs`; §2.1 gained `power.html?sc=anova` and the missing in-course link to §2.5; §2.3 had **zero outbound links in its body** and gained `stats-3/interactions-in-regression` plus a `pb-xref`; §2.4 gained `methods/between-vs-within-designs`, a `pb-xref`, and **a new paragraph on mixed models** as the modern route past sphericity (Stats 4 was built after this lesson, and nothing pointed there). **FAQ freshness:** five swaps, one per lesson, each replacing an answer that restated the lesson's own prose — "Do I need to be good at math to do statistics?", "Is a paired t-test the same as a repeated-measures ANOVA with two conditions?" (**F = t² verified in Node: 7.477876 both ways, df 1/5**), "My ANOVA is significant but none of the post-hoc comparisons are. What happened?" (states the Scheffé guarantee precisely: *some* contrast is significant, not necessarily a pairwise one), "How many participants do I need to detect an interaction?" (**4× the total N for an attenuated interaction, ~1× for a full crossover — SE algebra derived and checked, matching Simonsohn 2014**), "What happens if a participant misses one condition?". Duplicate-checked against all 291 existing Q&As first — three earlier candidates were already taken (`ml/prediction-vs-explanation` owns ML-vs-statistics, `methods/from-question-to-hypothesis` owns one- vs two-tailed, `between-vs-within-designs` owns "are within-subjects designs always more powerful", which is why the RM power FAQ was the one to go). FAQ em-dashes on the five pages fell 10 → 5. **software.js drift — three real fixes, all verified against current vendor docs, not memory:** SPSS 31 made **Levene's test opt-in** in the Independent-Samples T Test (it used to print automatically — the student-blocking one); SPSS 31 added a direct **Analyze → Descriptive Statistics → Chi-Square** alongside Crosstabs; SPSS 32 (Apr 2026) ships a **native Analyze → Mediation Analysis**, so the PROCESS step now says to check that menu first while noting PROCESS is still what papers cite. JASP **0.96/0.97** release notes checked: no menu path the site names has moved (ANOVA → ANOVA, Repeated Measures ANOVA, Frequencies → Contingency Tables, Regression → Logistic Regression, Post Hoc Tests all current). **Snippets:** full sweep of every API in all 76 entries; **one deprecation — `pm.summary(idata)` → `az.summary(idata)`** (PyMC moved diagnostics to ArviZ; current docs never mention `pm.summary`). Checked and left alone: `enable_iterative_imputer` is still required (IterativeImputer remains experimental), `stats.t.interval` is called positionally so scipy's `alpha`→`confidence` rename doesn't bite, statsmodels' power `alpha=` is unchanged, and `dplyr::recode()` is superseded but not deprecated and still runs clean. **Cross-pollination:** the P72 capstone guide had **zero inbound links from any lesson** (same gap run 1 found for the P34 guides — nav and toolbox render from JS, so crawlers saw nothing); five natural spots now link it, spread across four courses: §2.3 factorial ANOVA (the guide's own analysis), `methods/from-question-to-hypothesis` (its step 1), `stats-1/effect-size-and-power` (its step 2), `stats-2/assumptions-and-when-they-break` (its four kept points, claim verified against the guide text), `writing/final-checklist` (the whole thing assembled). **Quips:** all ten touched pages reviewed; eight are good, two were blurbs rather than quips (`final-checklist`, `complete-worked-project`) and were rewritten short. **Backlog item cleared** (no Backlog section yet — P37 still pending — so this took the first of the two things P38's 23 Jul run noted but did not action): `stats-2/regression-diagnostics` now obeys the **frozen-noise law** — `seed()` fills shared x and standard-normal pools, `gen()` only re-maps them per scenario, and only "New sample" reseeds. Verified live in-browser: Healthy → Curve → Funnel → Outlier → Healthy returns **R² = 0.87 every time** by any path, and the button still gives a fresh cloud (0.87 → 0.83). **Found and fixed en route, a genuine regression nobody had noticed:** the 22 Jul American-spelling sweep normalized the lesson HTML and every `assets/js` source but **missed `tools/faq_data.py`**, the single source of truth for those baked FAQ blocks — so the first `inject-faqs.py` run of this session silently reverted "neighbors", "behavior", "standardized" and 30-odd others to British forms across ~20 lessons. Fixed at the source (**39 replacements, then 8 reverted by hand** after a word-level audit caught the substring rules mangling the American plural *analyses* into "analyzes" ×7 and *realistic* into "realiztic"); re-injection now leaves only the 10 lessons this run intentionally edited. **Verification:** `audit.js` **0 errors / 21 warn / 64 info** (baseline unchanged), `math-check.js` **270 assertions, 0 failures**, `prose-lint --strict` **exit 0** with all five refreshed lessons inside the ≤10 em-dash budget and none added; all shared JS + every touched inline script + every JSON-LD block parse; all new links fetch 200; browser pass on desktop dark and mobile light with **zero console errors** and no horizontal scroll at 375px; injected SPSS/snippet blocks confirmed showing the new text live. **`CACHE_VERSION` bumped sc-v24 → sc-v25** (site.js is a precached shell asset and two QUIPS changed).
  - **11 Jul 2026 — first run.** The 5 least-recently-touched lessons (per-file git history, mass passes excluded) were all early Stats 1, untouched since 26 Jun: types-of-data, describing-data, visualizing-data, z-scores, hypothesis-testing-logic. Each got surgical cross-links to tools/courses built since it was written (Distribution Playground, tables + descriptives calculators, which-test/which-chart choosers, Methods 1.1, Writing 5.3/5.5, Data 2.5) — 2–3 links each, no prose rewrites needed (content held up). **FAQ freshness:** each of the 5 had exactly one FAQ that repeated its own prose — swapped each for a better real-student question ("Is age nominal, ordinal, interval, or ratio?", "mean+SD or median+IQR?", "Why is α = .05?" — Fisher/5σ≈1-in-3.5M/5×10⁻⁸ figures verified, "How do I convert z to a percentile?" — Φ(1)=.8413/Φ(−1.5)=.0668 verified via node, "Why not pie charts?" — Cleveland & McGill 1984); duplicates against existing FAQs checked (first two candidates were already taken by sampling-distributions and methods 1.1); faq_data.py edited, re-injected 95/95, search index rebuilt. **software.js drift:** verified 4 walkthroughs (one-sample/paired t, independent t, one-way ANOVA, correlation, reliability) — added the SPSS 29+ *Compare Means and Proportions* submenu rename to the three t-test/ANOVA entries and SPSS 28+ McDonald's ω (Model → Omega) to Reliability Analysis; all JASP paths current (Unidimensional Reliability, Regression → Correlation ✓). **Snippets:** full package-API survey (scipy.stats, statsmodels, pingouin, dplyr/tidyr, car::, psych, metafor…) — zero deprecations (no gather/spread, no pairwise_ttests, binom.pmf/ttest_1samp/chi2_contingency all current). **Cross-pollination:** the P34 guides had *zero* static inbound links from content pages (nav/toolbox render from JS) — added 5 lesson one-liners (independent-t → JASP guide, writing-results → SPSS-to-APA, cleaning-workflow → clean-survey-data, final-checklist → cheat-apa poster, non-parametric → cheat-assumptions poster) plus which-test.html → dissertation guide, so all 4 guides now have crawlable inbound links. **Quips:** all 5 reviewed, none worn thin, none replaced. No Backlog section yet (P37 pending). Verification: audit 0 errors (21 warn = baseline), node --check clean, browser pass light+dark+mobile zero console errors on 3 changed pages (SPSS 29+ note confirmed live in the injected box); no CACHE_VERSION bump needed — no precached shell asset changed (quips untouched; software.js is lazy-loaded).
- [x] P40 · voice charter + prose linter
  - **12 Jul 2026 — shipped VOICE.md + `tools/prose-lint.js`; baseline measured.** The linter counts *rendered prose* (body text with scripts/styles/tags stripped; a lesson's FAQ block excluded and its answers linted separately from `faq_data.py`), so its numbers sit below the addendum's raw-file counts — these are the canonical Phase-10 baselines for P41–P46: **120 pages** (95 lessons · 4 guides · 21 root) · 285 FAQ answers · 120 meta descriptions. **Em-dashes:** 1,700 in page prose (lesson median 13, max 45), **90/120 pages over the ≤10 budget**; 389 in FAQ answers, **60 lessons over the ≤3/trio budget**. **Banned constructions:** contrast punch 38 · "Here's the …" setup 13 · "The point is" 4 · whole-point 0 · "quietly" 29 · FAQ verdict openers 19. **Already passing:** "Think of it as" 1 (≤3) · no page exceeds 1 "Notice how/that". **Meta descriptions on "…and watch…": 29/120 = 24.2%** (budget ≤15%). `--strict` = **241 violations** — Phase 10's number to drive to zero. Worst pages: the four P34 guides (36–44 dashes) + `datasets.html` (45) — all in the P45/P46 batches; worst lessons: ethics/fraud-and-self-correction 24, methods/sampling-methods 25.
- [x] P41 · voice pass: Stats 1 + Stats 2 (23 lessons)
  - **12 Jul 2026 — batch clean; all 23 pages at zero strict violations.** Baseline for the batch: 17 of 23 pages over the ≤10 em-dash budget (worst: effect-size-and-power & describing-data 20, CLT 17), 8 FAQ trios over ≤3, 4 contrast punches, 1 "quietly", 5 verdict openers, 14 of the sitewide 29 "…and watch…" descriptions. After: **every batch page ≤9 em-dashes** (page counts include the untouchable `—` stat-row placeholders, so prose sits well under), all FAQ trios ≤3, banned/verdict hits 0, and the 14 descriptions rewritten (og:/twitter:/JSON-LD copies synced via identical-string replace) — **sitewide "…and watch…" now 15/120 = 12.5%, under the 15% budget for good**. Fix mix per VOICE.md: parentheses > commas > periods > colons; ~10 earned dashes kept across the batch (no semicolon plague — 4 added sitewide); batch word count *down* on every touched page (e.g. what-is-statistics 405→397, non-parametric 393→387). De-templating: the "X — appositive — punch" lede pattern broken up (CLT now opens with the fact itself, z-scores with two raw numbers, sampling-distributions lost its "Here's an idea that quietly powers…" drumroll); definition-list dashes varied per page (colons in describing-data, sentences in regression-diagnostics/effect-size, kept as-is where under budget — deliberately not uniform). FAQ edits all via faq_data.py → inject (95/95) → search-index rebuild. Verification: prose-lint --strict = 0 batch violations (sitewide 241 → 207, all remaining in P42–P46 scope); audit 0 errors; browser pass CLT/effect-size/non-parametric in light+dark — prose renders, TOC anchors resolve, FAQ details + FAQPage JSON-LD parse, CLT draw button works, non-parametric slider round-trips to identical p-values (0.018/0.025). Read-aloud test on CLT start-to-finish: **passes** — one slightly clipped colon-list in the why-it-matters callout, kept as within-human variance.
- [x] P42 · voice pass: Stats 3 + Stats 4 (22 lessons)
  - **12 Jul 2026 — batch clean; all 22 pages at zero strict violations.** Baseline: 17 of 22 pages over the ≤10 em-dash budget (worst: manova 22, missing-data 17, ancova/assumptions-of-regression/power-analysis 16), 10 FAQ trios over ≤3 (worst: bootstrap 7), 12 banned hits (6 contrast punches incl. survival's lede "isn't a number — it's a *when*", 2 "Here's the…", 4 "quietly"/misc), 3 FAQ verdict openers, 6 of the batch's meta descriptions on "…and watch…" (one, missing-data, also carrying a banned "quietly"). After: **every page ≤10 total em-dashes** (counts include untouchable stat-row `—` placeholders — manova keeps 4, so its prose sits at 5), every FAQ trio ≤3, banned/verdict 0, and the 6 descriptions rewritten with og:/twitter:/JSON-LD copies synced (missing-data's shortened twitter variant fixed too) — **sitewide "…and watch…" now 9/120 = 7.5%**. Technical qualifiers preserved verbatim in meaning through every rewrite ("under equal variances", "given the shape of the cloud", "in expectation", the proportional-hazards caveat). Fix mix per VOICE.md: parentheses/commas/periods/colons varied per page, definition-list dashes varied across the batch (colons in missing-data/mixed/power-analysis, "is"-sentences in manova's family list, kept-as-dashes in manova's test-statistics list and model-comparison's tools list; the LINE acronym expansions became middots), ~8 earned dashes kept; word count *down* on every touched page (manova 807→800, ancova 760→743, missing-data 513→503). De-templating: multiple-regression's lede no longer twins manova's "Real X…" opener (now opens on the income/health examples); survival opens "the outcome is a *when*, not a number"; cross-validation's lede pivot inverted without the punch. FAQ edits all via faq_data.py → inject (95/95) → search-index rebuild. Verification: prose-lint --strict = 0 batch violations (sitewide 207 → 165, all remaining in P43–P46 scope); audit 0 errors; browser pass manova/missing-data/ancova — FAQPage JSON-LD parses, TOC anchors resolve, dark mode renders, zero console errors, slider round-trips return identical readouts (manova 0.424/0.977/0.642, ancova +9.7/&lt;.001/+4.3/0.041). Read-aloud test on MANOVA start to finish: **passes after two fixes it caught** — a stacked-colon "Why it matters" line (earned dash restored, page at 9 ≤ 10) and a dangling appositive in FAQ 1 (flourish deleted). No CACHE_VERSION bump — no precached shell asset changed.
- [x] P43 · voice pass: Methods + Data (22 lessons)
  - **13 Jul 2026 — batch clean; all 22 pages at zero strict violations.** Baseline (64 strict violation lines): 20 of 22 pages over the ≤10 em-dash budget (worst: sampling-methods 25, transformations-and-recoding & data-privacy-basics 23, data-entry/data-cleaning/outliers 19), 15+ FAQ trios over ≤3 (worst: wide-vs-long 6), 7 FAQ verdict openers, and banned constructions across the batch — contrast punches (tidy-data ×2, data-privacy, data-entry, reproducible-workflows ×2 incl. FAQ, merging, plus sampling/preregistration FAQ answers), "Here's the…" (experimental-design, reproducible-workflows), "The point is" (from-question-to-hypothesis), and "quietly" ×9 (transformations, data-cleaning, outliers, codebooks, survey, variables, replication-crisis, between-vs-within, merging ×3+FAQ). After: **every batch page ≤8 total em-dashes** (counts include untouchable stat-row `—` placeholders, so prose sits well under), all FAQ trios ≤2, banned/verdict 0. Fix mix per VOICE.md: parentheses/commas/periods/colons varied per page; the two- and three-item definition lists de-dashed *differently* across pages (colons in some, period-after-bold in others, kept-as-dashes where earned) so the batch doesn't read uniform; ~35 earned dashes kept across the 22 pages (e.g. sampling's "external validity — the right to generalise", replication's HARKing expansion, codebooks' two nested asides). De-templating: `data/tidy-data`'s lede — the exact "Most of the pain in data analysis isn't the statistics — it's the shape…" opener the Phase-10 addendum calls out — was rewritten from scratch ("Before any statistics happen, your data has a *shape*…"); several other ledes lost their em-dash-appositive rhythm. **Extra-care content preserved verbatim in meaning:** Sweeney's 87% re-identification, the 1936 *Literary Digest* poll, Clever Hans, the 2015 Reproducibility Project's 36–39%, Cronbach's α formula, the forking-paths/optional-stopping framing (still honest, non-alarmist), and every scenario/vignette string inside the interactives. FAQ edits all via `faq_data.py` → inject (95/95) → search-index rebuild (byte counts refreshed). Verification: prose-lint --strict = 0 batch violations (sitewide **165 → 101**, all remaining in P44–P46 scope); audit 0 errors; browser pass on sampling-methods / between-vs-within-designs / tidy-data in light+dark, zero console errors — FAQPage + all JSON-LD parse, 3 FAQ items each, renamed stat labels ("Between: people needed") render and readouts populate, and slider round-trips return identical values (between-vs-within nw 21→53→21). Read-aloud on sampling-methods start to finish: **passes**. No CACHE_VERSION bump — no precached shell asset changed (only lesson HTML, which is network-first).
- [x] P44 · voice pass: Ethics + ML (20 lessons)
  - **13 Jul 2026 — batch clean; all 20 pages at zero strict violations.** Baseline: 19 of 20 pages over the ≤10 em-dash budget (worst: fraud-and-self-correction 24, classification-metrics 22, questionable-research-practices & informed-consent-and-irb 20, random-forests 17; only deception-and-debriefing under, at 8), ~18 FAQ trios over ≤3 (worst: plagiarism 8, fraud 7, prediction-vs-explanation 7), 2 FAQ verdict openers ("No —" in questionable + plagiarism), and banned constructions across the batch — contrast punches (privacy, plagiarism, ai-in-research ×2, regularization, knn, neural-networks, dimensionality-reduction, train-test ×2), "Here's the…" (fraud, questionable, classification-metrics, prediction), "The point is" (informed-consent FAQ, ai-in-research), and "quietly" ×7 (questionable ×2, plagiarism, ai-in-research, classification-metrics, clustering, knn, prediction). After: **every batch page ≤10 total em-dashes** (counts include untouchable viz stat-row `—` placeholders — classification-metrics carries 9, so its prose sits at 0), all FAQ trios ≤3, banned/verdict 0. Fix mix per VOICE.md: parentheses/commas/periods/colons/semicolons varied per page; definition lists de-dashed *differently* across pages (colons in the confusion-matrix and consent-element lists, period-after-bold in the privacy/consent term lists, kept-as-dashes where earned) so the batch doesn't read uniform; earned dashes kept where natural (knn 8, roc/random-forests/decision-trees/regularization 9, several ethics pages 1–5), while placeholder-heavy pages drove prose near zero. De-templating: the two highest-irony pages got the deepest pass — `ethics/ai-in-research-ethics` lost both contrast punches (its lede "The skill isn't 'AI good' or 'AI bad' — it's…" became "The skill is neither…nor…. It's…"; the callout's "isn't the tool — it's what's at stake" rebuilt) while keeping the within-budget "Think of it as an eager…intern" analogy, and `ml/llms-and-ai-in-research` lost its "That's the whole trick —" drumroll. **Ethics kept its sober register — de-telling removed machine rhythm, added no levity** (Tuskegee/Milgram/Stanford/Stapel prose changed only where a banned construction or the dash budget demanded, and gently). **Extra-care content preserved verbatim in meaning:** Tuskegee (≈600 men, ≈400 infected, 1932–1972, penicillin withheld), Milgram (two-thirds to 450 V), the Stanford Prison critique, Stapel's 50+ retractions, Fanelli (2009) ≈2%, John/Loewenstein/Prelec (2012) 63/56/46/27%, Simmons/Nelson/Simonsohn (2011), the GRIM arithmetic and its 5.19/n=28 example, Belmont's three principles, ICMJE authorship criteria, Sweeney's 87% (FAQ), AUC = Φ(d/√2) → .76/.92/.50, and the unit-ball volumes 79%/52%/0.25% — plus every scenario/`why` string inside the interactives. FAQ edits all via `faq_data.py` → inject (95/95) → search-index rebuild; two out-of-lint-scope "quietly" residues also cleaned (knn's `og:description`, ai-in-research's viz `why` string). Verification: prose-lint --strict = 0 batch violations (sitewide **101 → 42**, all remaining in P45/P46 scope); audit 0 errors, 23 warnings; browser pass on fraud-and-self-correction / classification-metrics / llms-and-ai-in-research + read-through of ai-in-research-ethics in light+dark, zero console errors — GRIM returns the ✗-impossible verdict for 5.19/n=28, the confusion-matrix readouts populate (77.3%), the next-word predictor runs, and every FAQPage JSON-LD parses with 3 items. Read-aloud on ai-in-research-ethics start to finish: **passes** — sober register intact, machine rhythm gone. No CACHE_VERSION bump — only lesson HTML + injected FAQ HTML changed (both network-first), no precached shell asset touched.
- [x] P45 · voice pass: Writing + guides + posters (15 pages)
  - **13 Jul 2026 — batch clean; all 15 pages at zero strict violations.** Baseline: 13 of 15 pages over the ≤10 em-dash budget (worst: the four P34 guides — analyze-thesis-data-jasp 44, clean-survey-data 41, choose-statistics-dissertation 40, spss-output-to-apa 36 — then cheat-assumptions 15, plus every Writing lesson 9–19), 7 FAQ trios over ≤3 (worst: final-checklist 8, writing-results 7, abstracts/tables/nonsignificant 6), and banned constructions across the batch — 7 contrast punches (reporting-statistics-apa prose + FAQ, tables-and-figures prose, discussion FAQ, final-checklist FAQ, spss-output-to-apa prose, clean-survey-data prose), 3 "Here's the…" (imrad ×2, discussion), 5 "quietly" (writing-results, final-checklist FAQ, jasp prose, clean-survey lede + a quoted "delete quietly."), 2 FAQ verdict openers ("No —" in imrad + writing-results). After: **every batch page ≤9 em-dashes** (only the two already-passing posters, cheat-test-chooser/cheat-apa, were left untouched at 4), all FAQ trios ≤2, banned/verdict 0. Fix mix per VOICE.md: parentheses/commas/periods/colons/semicolons varied per page; the guides' repetitive **"Step N — Title" H2 headings** de-dashed *differently* across files (colons in jasp/choose-statistics, middots in clean-survey to match its own TOC), and the two annotated link-lists each guide carries (keep-going + spss's "classic misreadings") converted to colons/period-after-bold so no list reads uniform; cheat-assumptions' 11 card labels became `·` middot separators (matching that poster's TOC style, and *shorter* than the em-dash); ~40 earned dashes kept across the 15 pages. De-templating: imrad's "here's the question (I), here's how…" narrative rebuilt to "the question (I), how I looked (M)…"; every Writing lede opens differently (final-checklist on a scene, abstracts on a claim, choose-statistics "There is no test-choosing talent"), and the course that **teaches** prose now practices it. **Extra-care content preserved byte-identical:** every worked number in the guides (jasp d=0.45 / t(78)=2.02 / p=.047, all five SPSS analyses, clean-survey α=.90 / composite M=2.88 SD=0.93 / 51 blank cells / 45 discarded, choose-statistics' full test map) and every query-shaped heading's keyword text (only the separators changed, so the SEO surface is intact). FAQ edits all via `faq_data.py` → inject (95/95) → search-index rebuild. Verification: prose-lint --strict = 0 batch violations (sitewide **42 → 9**, all 9 remaining in P46 scope — index/toolbox/effect-sizes/descriptives/correlation/datasets over the dash budget, which-test/which-chart/plan on their lone banned hits); audit 0 errors, 21 warnings; browser pass on writing/imrad-structure in light+dark (parens lede + colon list render, the 12-sentence sorter works, FAQ 2 now opens "Reading order isn't writing order…", FAQPage JSON-LD parses) and guides/analyze-thesis-data-jasp in dark (H2s render "Step 0: Start tidy…", TOC + lede clean, zero console errors). **Poster one-page print fit re-measured the P30/P35 CSSOM way** after cheat-assumptions changed: A4 poster height 149 mm ≤ 279 mm, US-Letter 145 mm ≤ 261 mm — both still fit one page. Read-aloud on imrad-structure start to finish: **passes** — reads like a person, not a keynote. No CACHE_VERSION bump — only lesson/guide/poster HTML + injected FAQ HTML changed (network-first), no precached shell asset touched.
- [x] P46 · voice pass: tools, homepage & sitewide finish
  - **13 Jul 2026 — sitewide clean; Phase 10's exit number reached: `prose-lint --strict` passes SITEWIDE (241 baseline violations → 0).** Baseline: the 9 strict violations left after P45 — six pages over the ≤10 em-dash budget (datasets 45, correlation 20, effect-sizes 19, descriptives 15, index 13, toolbox 11) and three lone banned hits ("Here's the cheat…" in which-test + which-chart, "quietly switch tests" in plan). After: **0/120 pages over any budget** (max page em-dash count sitewide is now 10, and those are placeholder-heavy calculator pages — descriptives keeps its 10 untouchable stat-row `—` placeholders with zero prose dashes; effect-sizes 9 placeholders + 1 earned). Fix mix per VOICE.md: datasets' eight-times-repeated "Worked solution — the key numbers" summary template became colons, its four table-cell dashes became parentheses, and ~34 varied prose fixes kept 4 earned dashes (the r = −.40 reveal, "1.72 — that's metres"); toolbox's six-item thesis-pathway "text — link" template became colons; the homepage lost 11 of 13 dashes (persona cards, features, About bio, a11y statement — hero lead + offline note keep theirs). **Layer 2 (injected strings, by hand):** checks.js lost its one "quietly" ("wreck the error calculations without any warning in the output") and an "is the whole point —" flourish; quiz.html's BANK lost a "That's the whole reshape."; glossary-data.js lost its one "quietly" (reverse-coding); software.js/snippets.js swept — clean, modest earned-dash load left alone; site.js untouched (the regularization QUIP's "quietly evicts" is exempt brand voice), so **no CACHE_VERSION bump**. **Untouchables preserved byte-identical:** every datasets worked-solution number (verified in-browser: t(78)=2.02/p=.047/d=0.45, admit 45.6%/OR≈53.6), Anscombe's r ≈ .82 / ŷ = 3.00 + 0.50x, effect-sizes' d↔r math, the H1 brand line + title/og tags (no stronger line proposed — "Statistics You Can See, Touch, and Understand" still carries the site). Meta descriptions already sat at 7.5% "…and watch…" (≤15% cap) with audit-enforced dedupe, so no description pass was needed. Final sitewide summary vs the P40 baseline: **em-dashes 1,700 → 709 (lesson median 13 → 6, max 45 → 10, 90 → 0 pages over); FAQ-answer dashes 389 → 190 (60 → 0 trios over); every banned construction 0 (contrast punch 38 → 0, "Here's the…" 13 → 0, "quietly" 29 → 0, verdict openers 19 → 0, "The point is" 4 → 0); "Think of it as" 1/3, "Notice how" ≤1/page; "…and watch…" 29/120 → 9/120.** Verification: audit 0 errors, 21 warnings; search index rebuilt once at the end (95 lessons, 23 pages); browser pass on homepage + effect-sizes + datasets + 404 in light AND dark, zero console errors — hero canvas, curriculum grid, toolbox strip and resume banner render; effect-sizes converts d=0.50 → U₃ 69% / OVL 80% with the renamed "U₃: treated above control mean" label; all 8 datasets solution blocks open. Read-aloud on datasets.html start to finish: **passes** — the stories read like a colleague handing you a file, not a product page. Phase 10 complete.
- [x] P47 · interaction-feel polish
  - **13 Jul 2026 — all 8 feel items shipped; calm throughout, reduced-motion-safe, zero new dependencies.** **(1) Anchors:** one global rule (`[id] { scroll-margin-top: calc(var(--nav-h) + .85rem) }`) — a CLT TOC-chip jump that used to land the heading at 0px (under the 64px fixed nav) now lands at 77px, and a `plan.html → #run-it` deep link lands at 78px (measured before/after in-browser). **(2) Canvas cursors as a convention** (documented in CLAUDE.md): draggable → `grab`/`grabbing` (added to `ml/clustering-kmeans`; `ml/dimensionality-reduction` scopes it to its 3-D view — `grab`→`default` verified flipping with the PCA button), click-to-select → `pointer` (correlation.html's scatter, only once data exists — `default`→`pointer` verified on demo load), threshold drag keeps `ew-resize`, display-only stays default; the three already-correct lessons (describing-data, correlation, assumptions-of-regression) untouched. **(3) Copy feedback standardized:** `window.SC.copied(btn)` in site.js — "Copied!", 1.4 s revert, green `.copied` tint, `aria-live` polite — now used by the R/Python snippet block, apa.html (rich-HTML copy path kept), descriptives.html, correlation.html. **(4) Hover/press:** one shared `:active` press (`translateY(0) scale(.985)`, 70 ms) across buttons/cards/chips/segs; card lifts unified at −2px (features were −4, tool cards −3); prev/next links get a direction-aware ±2px nudge. **(5) `<details>` reveal animated** via the `::details-content` pattern (`interpolate-size: allow-keywords`, block-size 0→auto + discrete content-visibility, .25s) — every `::details-content` selector kept in its own rule so non-supporting browsers just snap; instant under reduced motion (verified 0s via the CSSOM media-flip); **print-safe**: `@media print` forces `block-size:auto`/no transition, and a beforeprint dispatch under the flipped print block rendered the force-opened FAQ at full height (100px answer, cv:visible) with 3 canvas snapshots — the one real scare during verification (open FAQs reading `block-size: 0`) turned out to be the preview pane freezing transition clocks while frame-throttled, not a CSS bug: once frames render it settles at auto/visible. **(6) Range inputs unified:** one sitewide family (6px soft-accent track, ringed round thumb, hover/active scale, `grab`/`grabbing` thumb cursor, 28px hit strip → 44px on coarse pointers) driven by `--range-accent`/`--range-thumb`; the hero slider is now just a two-variable override (orange, 26px) instead of three bespoke rules — verified in light+dark. **(7) Edge fades:** site.js `setupHScroll()` auto-wraps every `table.ref-table` in a `.hscroll` div (which also stopped the formulas page's sideways body-scroll on phones) and tags known overflow containers (`.try-code pre`, apa's `#ana-seg`, guide `.mock`s, lesson table wraps); `hs-l`/`hs-r` mask fades toggle from scrollLeft — verified at 375px on formulas.html (3 of 4 tables fit → no fade; the 4th shows the right fade, and the classes round-trip start→mid→end→back correctly) and apa.html's seg; fades disabled in print. **(8) 404 v2:** capybara mascot, honest copy, Back-home + **Search-the-site** buttons (a bare `?q=` now opens the search overlay — one-line site.js regex change), four popular-destination pills, all links still JS-computed and self-contained — and it now honors the site's saved theme toggle pre-paint (verified light AND dark). `CACHE_VERSION` bumped `sc-v3 → sc-v4` (styles.css + site.js are precached); the preview's SW verified swapping cleanly (only `sc-v4` cache remains). Verification: audit 0 errors/21 warnings, `prose-lint --strict` still exits 0 sitewide, search index rebuilt, `node --check` + inline-script parse pass on every touched page, zero console errors across homepage/CLT/apa/correlation/kmeans/dimred/404 in light+dark+mobile.
- [x] P48 · instructors page + embed mode + PNG export
  - **13 Jul 2026 — all three deliverables shipped; audit 0 errors/21 warnings, verified in-browser (root + subpath + real iframe).** **(1) `teachers.html`** — a calm, VOICE.md-compliant **For Instructors** page (3 prose em-dashes, no banned constructions), integrated the full six-place way: `TOOLBOX` (`group:"read"`, 🎓, `star`-less) + `SEARCH_PAGES` (tag "Guide") + a `QUIPS` key + `tools/build-search-index.py` (reran → 24 pages) + `sitemap.xml` + `ROOT_PAGES`, plus a `[BreadcrumbList]` head block (Home → Toolbox → For Instructors). Content: what students see (no accounts/tracking, progress in their browser), syllabus/LMS deep-linking, print handouts + the three posters, reproducible datasets for assignments, quiz/flashcards, a copy-ready `?embed=1` iframe snippet (wired to `window.SC.copied` — verified the label flips to "Copied!"), a **fourteen-week Stats 1–2 map** (`.sem-map` `.ref-table` in `.hscroll`, 14 rows, every lesson linked and audit-resolved), and a "free forever / link to us" note. **(2) Lesson embed mode** — `?embed=1` on a lesson adds `body.embed-mode`; `init()` short-circuits all chrome renders and `styles.css` trims `.lesson` to eyebrow + `h1` + `.viz` (nav/sidebar/footer/prose/FAQ/checks/software/prev-next all `display:none`), with `renderEmbedFooter()` appending one `target="_top"` link to the absolute canonical. Verified in-browser: chrome hidden, viz + controls live, 0 visible prose paragraphs, and — inside a **real injected `<iframe>`** — GA fires exactly once (`gaConfigCount:1`, `G-80HCM6EZVN`) and the back-link is `_top`; same on the **subpath** server (footer still the absolute canonical). **(3) Viz PNG export** — `injectVizExport()` adds a keyboard-accessible, aria-labelled **"PNG ↓"** button to every `.viz` with a canvas (next to `.viz-title` in a `.viz-head`, or a `.viz-exportbar` on titleless calculator vizzes); exports the block's largest canvas at 2× via an offscreen `toDataURL` with the theme background painted under it. Verified: CLT's 3-canvas block picks `sampling-canvas` and exports **1292×400 = exactly 2×** the 646×200 CSS (pixel-perfect against the DPR-2 backing store); correlation lesson → 2 buttons (one per block); descriptives → the `.viz-exportbar` variant; `apa.html`'s canvasless viz + the homepage hero → **no** button; hidden in `@media print`; theme-aware in dark. `CACHE_VERSION` bumped `sc-v4 → sc-v5` (styles.css + site.js precached). Docs: embed mode + PNG export + `teachers.html` documented in CLAUDE.md; the temporary `p48` launch config was removed after verification.
- [x] P49 · Stats 1 interactive upgrades — sampling-distributions population shapes + Draw 100, CLT Draw 100, P-Value Explorer shaded-fill gap (Powerful · Extra)
  - **16 Jul 2026 — all three shipped; audit 0 errors/21 warnings, search index rebuilt (95 lessons, 24 pages), verified in-browser (light + dark).** **(1) sampling-distributions population selector** — added a Skewed / Uniform / Bimodal `#pop-seg` reusing the *exact* samplers from `central-limit-theorem` (`uniform: rand×100`, `skewed: −ln(1−u)×16` clamped, `bimodal: gauss(28|72, 8)` clamped) so the two lessons agree; `buildPop()` rebuilds `popData`+`popMean` per shape, `changePop()` resets the draws, and the caption changed from "The population (fixed, and deliberately skewed)" → "The population you're sampling from (fixed). Pick a shape above." (no em-dash, to avoid adding VOICE em-dash load). Verified running means match each parent: skewed ≈15.9, uniform ≈49.1, bimodal ≈50.5 (n=10 over 400 draws), and the three population histograms render distinctly (left-decay / flat / two-hump). **(2) Draw 50 → Draw 100** in both lessons — button labels + `drawMany(50)/many(50)` → `100`; added a local `reducedMotion()` (mirrors `VIZ.reducedMotion()`; neither lesson loads viz.js) so the animated path **jumps straight to the final frame** under reduced motion — verified: with `matchMedia` stubbed, one click drew exactly 100 synchronously in both. (The rAF-animated path can't be timed in the headless preview — the tab reports `visibilityState:"hidden"`, which pauses `requestAnimationFrame`; logic is unchanged from the shipped Draw-50 version, only the total changed.) **(3) P-Value Explorer fill/curve gap** (`hypothesis-testing-logic`) — root cause was an **x-mapping mismatch**: the bell curve was stroked across the full canvas width (raw `px`) while the shaded tail, ticks and marker all used the padded `vx()`. Unified the curve onto `vx(v)`. Pixel check on the clean right tail: the curve stroke now sits ~6 device-px above the fill top = exactly the 2.5-CSS-px (×DPR 2 = 5px) stroke width → fill edge and outline coincide, no background gap; confirmed light + dark. **SWEEP** of the same tail-shading pattern found **no** other gap: `z-scores-and-the-normal-distribution` (both vizzes), `tables.html`, and `distributions.html` each already use a *single* consistent mapping for fill and curve (full-width in z-scores viz 1; padded `zx`/`toX` elsewhere), so their fills already meet their curves — left untouched. No precached shell asset changed → no `CACHE_VERSION` bump.
- [x] P50 · Stats 2 interactive fixes — post-hoc "fooled" stat layout, bigger Least-Squares plane (Powerful · High)
  - **17 Jul 2026 — both fixes shipped; audit 0 errors/21 warnings, search index rebuilt byte-identical, verified in-browser (light + dark, desktop + mobile widths).** **(1) `stats-2/post-hoc-tests` "fooled" readout** — split the single four-stat `.stat-row` into two: a top row (Comparisons per experiment · Bonferroni per-test α) and a second `flex-wrap:nowrap` row holding the two comparison rates as equal `flex:1 1 0;min-width:0` cells, so **"Experiments fooled · no correction" (#stat-unc, red) always sits directly LEFT of "· Bonferroni" (#stat-bonfr, green)** and the Bonferroni-α stat sits on the row above. Verified by layout `offsetTop`/`offsetLeft` at desktop (unc offLeft 401 / bonfr 776, equal 351 px, same row) and at forced 360 px + 320 px viz widths (the two rates stay same-row, equal-width, correct order, never wrap apart — only the non-comparison top row wraps, which is fine); run-20 populates 44%/4%. **(2) `stats-2/simple-linear-regression` Least-Squares Playground** — the plot region is a *centred square* (equal x/y scale is required so the "squared errors" patches render as true squares — both axes span 0–10), which the old fixed `H = 360` capped at ~304 px while wasting the canvas sides. `fit()` now computes a responsive `H = clamp(width, 320, 500)`: **desktop (~700 px) → H 500, plot side 444 px (+46% vs 304)**; mobile (~340 px) → 340×340 canvas, plot 284 filling it with no empty vertical margins. Added `--viz-ar: 4/3` on the canvas so the pre-JS reserved box ≈ final (CLS ~10–25 px on desktop, below-fold on mobile). Slider interaction (this lesson is slider-driven, not drag), frozen data, and squares/lines modes intact — best-fit SSR = best SSR (14.7), R² 0.78; canvas draws content in light + dark. **SWEEP:** the square-cap cramping is unique to this lesson (the only one using `Math.min(W−2pad, H−2pad)` — it needs it for true squares); the other draggable-scatter lessons (`stats-2/correlation` H320, `stats-3/assumptions-of-regression` H280, `stats-1/describing-data` H200) map data across the *full* canvas width (`vx` = `W−2pad`), so they stretch edge-to-edge and were never cramped that way — left unresized as instructed. No precached shell asset changed → no `CACHE_VERSION` bump.
- [x] P51 · Stats 3 interactive fixes — multicollinearity severity jitter, ANCOVA effect→20, mediation arrowheads, logistic steepness→0.5 (Powerful · High)
  - **17 Jul 2026 — all four shipped; audit 0 errors/21 warnings, search index rebuilt byte-identical (no indexed prose changed), verified in-browser via eval + canvas-pixel measurement.** **(1) `stats-3/multicollinearity-and-variable-selection` Severity jitter** — the `#stat-sev` value reflowed as its label changed width ("fine (VIF < 5)" ↔ "concerning" ↔ "severe"). Pinned it: added `display:inline-block;min-width:15ch` to the value span (min-width needs the inline→inline-block flip). Verified constant **144 px width and left edge (41 px)** across all three VIF regimes, so the `.stat` never resizes; slider round-trip identical (VIF 1.3 | +1.01 | ±0.23 away→back). The label strings themselves are unchanged (no stat/prose change). **(2) `stats-3/ancova` effect max 15→20** (`#eff-range max`). Confirmed the `YMIN 0 / YMAX 100` scale still frames it: at eff=20 with default slope/imbalance the **topmost drawn dot is data-y ≈ 101** (device row 18 of ~400, CSS y ≈ 9) — just above the 100 gridline but far below the ~104.3 data-y where a point would leave the canvas top, and nowhere near row 0. No `YMAX` bump needed. Round-trip identical (naive +15.1 / adj +7.3 at defaults; naive +29.1 / adj +21.3 at eff=20). **(3) `stats-3/mediation-and-indirect-effects` arrowheads** — the head was `ah = 7 + min(6, |val|*5)` (max 12 px) while the line was `lineWidth = 0.6 + min(7, |val|*6.5)` (max 7.1 px), so a full-strength beam swallowed its point (~1 px poke each side). Hoisted the width into `var lw` and made the head scale with it: **`ah = 10 + lw*1.8`** (max ≈ 22.8 px). Pixel-measured on the horizontal c′ arrow at |val|=1.0: **arrowhead ≈ 30 px vs ≈ 10 px shaft, juts ~10 px each side** (clearly visible). **SWEEP** — `stats-4/causal-dags-and-confounding` draws similar arrows but with a **fixed `lineWidth = 2` + 8 px head** (thickness never scales with a coefficient), so its heads are never swallowed → left unchanged. **(4) `stats-3/logistic-regression` steepness** — `#b1-range max 40→50` (slider is ×100) so the readout reaches **b₁ = 0.50** (OR ×1.65 = e^0.5, P(60)=99%). The curve can't clip — P(x)∈(0,1) inside the −0.18–1.18 y-range regardless of steepness. Round-trip identical. Note: the preview pane reported a **0×0 viewport** (a sandbox artifact — screenshots of below-fold canvases unavailable), but every canvas still rendered real pixels, so verification was done by eval/DOM-geometry/canvas-pixel reads per CLAUDE.md. No precached shell asset changed → no `CACHE_VERSION` bump.
- [x] P52 · Stats 4 interactive fixes — Bayesian x-axis overlap, GLM b₀/b₁ ranges (Powerful · High)
  - **17 Jul 2026 — both fixes shipped; audit 0 errors/21 warnings, search index rebuilt byte-identical (no indexed prose changed), verified in-browser (light + dark).** **(1) `stats-4/bayesian-thinking` "Prior → Data → Posterior" x-axis** — the label `"possible value of p →"` was drawn at `H − 3` (y ≈ 237) while the `0.00 … 1.00` tick numbers sat at `baseY + 14` (y = 234), so the centred label collided with the 0.50 tick. Gave the label its own row: raised canvas `H 240 → 258`, kept the axis line where it was (`baseY = H − 38 = 220`, so the plot area and all three curves are unchanged), ticks stay at `baseY + 14` (234), label moved to `baseY + 30` (250) — a clean 16 px gap, 8 px bottom margin. Verified in light + dark that the label sits clearly below the ticks with no overlap; curves + readouts still render (posterior mean 0.654 at defaults). **(2) `stats-4/generalized-linear-models` "One Line, Three Shapes" slider ranges** (set in the inline `FAM` object, not HTML min/max) — widened so the fitted mean curve travels further, leaving each family's `val` (and therefore the frozen data cloud, generated once in `regen()` from `val`) byte-identical: **linear** b₀ −30..40 → −60..70, b₁ −4..12 → −10..18 (y-axis auto-scales, always on-canvas); **logistic** b₀ ±12 → ±20, b₁ ±3 → ±6 (bounded 0–1, always on-canvas); **poisson** b₀ −2..3.5 → −4..3.2, b₁ ±0.6 → ±1 (I lowered the b₀ ceiling 3.5 → 3.2 on purpose: exp(3.2)=24.5 < the fixed yhi 26 so the curve's x=0 entry stays visible, whereas the old 3.5 gave exp(3.5)=33 > 26 and put the whole curve off the top). **On-canvas confirmed by an indigo-pixel count inside the plot rect at all four extreme corners + a mid point for each family** — every case > 0 visible curve pixels (linear 8600–9300, logistic 8000–10200, poisson 4100–9300; the one thin case, poisson b₀=3.2/b₁=1 = 145 px, is a correct exponential rocketing off the top yet still strictly better than the old max, which showed 0). Sliders never call `regen()`, so frozen-noise is preserved by construction. No precached shell asset changed → no `CACHE_VERSION` bump.
- [x] P53 · broken toolkit interactives — Data cleaning-pipeline empty bars, Writing abstract-grader rewrite (Powerful · Extra)
  - **17 Jul 2026 — both repaired; audit 0 errors/21 warnings, search index rebuilt byte-identical (no indexed prose changed), verified in-browser (light + dark).** Both turned out to be **pure CSS** — in each case the JS was computing the right values all along, so no logic, data or stats changed. **(1) `data/data-cleaning-workflow` "Cleaning Pipeline Simulator" empty bars** — `render()` was emitting correct markup (`<span class='cw-bar-fill' style='width:100.0%'>`), but `.cw-bar-fill` is a **`<span>` whose parent `.cw-bar-track` is a plain block**, not a flex container. Only the *track* got blockified (it's a flex item of `.cw-bar-row`); the fill stayed `display:inline`, and inline boxes ignore `width`/`height`, so every bar measured **0×0** — a grey rail with a count and no fill. Fix: **`display:block` on `.cw-bar-fill`** (one declaration). Verified the fill now measures **194×16 px**, and the whole pipeline drives end-to-end: raw 106 rows / 12 distinct labels / mean 115.5 (999s included) → all four steps on → **100 rows (raw 106), 2 labels (Control 59 / Treatment 41), mean 60.2, 9 missing** (4 planted + 5 range-checked), log lists all four ops. **(2) `writing/abstracts-and-titles` "Abstract grader" rewrite** — the *scoring* half was never broken (moves, warnings, word count and grade all rendered); the dead part was the before/after reveal. `#ag .ag-ba-body { display:grid }` is an **author rule, so it outranks the UA stylesheet's `[hidden] { display:none }` regardless of specificity** (cascade *origin* beats specificity) — the panel was permanently visible (measured 1195 px tall *with* the `hidden` attribute set), so the button toggled an attribute that did nothing. Fix: **`#ag .ag-ba-body[hidden] { display:none }`**. Verified closed on load (display:none, 0 px) → click reveals the red "Vague — invisible" / green "Findable — informative" cards → click again re-hides, button text round-trips. **SWEEP (all 50 toolkit lessons, nothing else dead).** Both bugs are greppable *classes*, so I scanned all 50 statically for each — a **validated** detector (it flags exactly these two on the pre-fix tree via `git stash`, zero on the fixed tree), finding **no other instance of either**. Also `node --check`ed all 50 inline viz scripts (**0 syntax errors**) and ran a **runtime probe** over all 50 (same-origin iframes at `?embed=1`, checking `window.onerror`, blank-canvas detection by pixel-sampling every `.viz canvas`, unpopulated `—` readouts, and zero-boxed inline elements): **no JS errors, no blank canvases, no collapsed elements.** One probe hit was a **false positive worth recording: `methods/sampling-methods`** shows "Avg of draws / Bias / Spread (SD)" as `—` on load — these are *accumulators over repeated draws* and are empty **by design** until you draw; confirmed correct (after "Draw ×40": avg 47.9% (40), bias +0.1 pts, SD 4.9 pts — a near-zero bias being the right answer for simple random sampling). No precached shell asset changed → no `CACHE_VERSION` bump.
- [x] P54 · site-wide clickable-link affordance — colour + underline in-prose links, exclude UI chrome (Extra Powerful · Max)
  - **17 Jul 2026 — shipped; audit 0 errors/21 warnings, verified in-browser across all six required page types in light + dark (+ a mobile spot-check), zero console errors.** **The rule** (styles.css, right under the quiet `a{color:inherit}` base it excepts): `.lesson :is(p, li, td, th, .callout, .faq-a) a:not(.btn, .lesson-toc a, .ds-index a)` + its `:hover` — the About-bio look generalised (600 weight, 1.5px underline at a 45% tint, `text-underline-offset:3px`; hover = full-colour + 2px). One selector, three exclusions, **no blanket `a{}` change**: an anchor-class inventory of the whole site (`.btn` variants, `.tool-card`, poster `.sec`, `.gloss-link`, `.fc-link`, 404's `.dest`) plus a `li a`/`td a` chrome sweep proved `.lesson-toc` (incl. the guides' hand-written TOCs) and datasets' `.ds-index` jump chips are the *only* navigation link-lists living inside articles — everything else is `.btn`-excluded, outside `.lesson`, or on self-contained pages. **Contrast:** new per-theme **`--link`** var, because raw `--primary` #6366f1 fails AA as body text (4.47:1 on white, 4.08 on light surface-2, 3.40–4.22 on dark surfaces): light = indigo-600 **#4f46e5** (5.74–6.29:1), dark = `--primary-light` **#818cf8** (5.09–6.31:1) — the `--text-faint` precedent applied to links. The About-bio + `.a11y-statement` rules were repointed to `--link` (quietly fixing their marginal dark-mode contrast) and the old `.faq-a a` colour rule was absorbed/deleted. **Print:** the shared `@media print` link rule gained `text-decoration-color:currentColor !important; text-decoration-thickness:auto !important` so the higher-specificity screen rule can't tint paper underlines (CSSOM-flip verified: black underline; the one tinted reading was the preview's frozen-transition artifact — proven by a `transition:none` snap-to-black, the same pane quirk that froze the pre-existing About-chip `--text` on theme flip); `plan.html`'s bespoke plain-links print card kept its design with `text-decoration:none !important`. The new rule deliberately transitions only decoration colour/thickness — never `color` — so theme flips are instant. **Verified:**
  - **17 Jul 2026 refinement (Hakan's feedback): switched to the Wikipedia model.** The first pass made prose links **bold + underlined at rest**, which Hakan found distracting inside lecture prose meant to be read peacefully. Reworked to: **coloured, no bold, no rest underline; underline appears on hover** (focus ring on focus). Bold in prose is now reserved for genuine `<strong>` emphasis. Dropping the rest underline means colour alone carries the cue at rest, so `--link` was **retuned to clear ≥3:1 vs body text as well as ≥4.5:1 vs surface** (the earlier values only cleared the surface gate): light `#4f46e5`→**`#5850ec`** (white 5.56:1, body-text 3.21:1), dark `var(--primary-light)`→**`#6f7bf2`** (bg 5.19:1 / surface 4.78:1, body-text 3.06:1) — searched with a node contrast script. The About bio + `.a11y-statement` were brought to the same calm treatment (they were the old bold-underline model). The shared `@media print` link rule was reverted to its clean `a { color:#000 !important; text-decoration: underline }` (the earlier `!important` decoration-colour/thickness guards existed only to fight the rest-underline tint, now gone); `plan.html` keeps its own `text-decoration:none !important`. One honest limitation recorded in CLAUDE.md item (g): inside callouts/FAQ the surrounding text is `--text-muted`, so link-vs-muted can't also reach 3:1 by luminance — hue + hover/focus carry it, as on Wikipedia itself. `CACHE_VERSION` stays `sc-v6` (styles.css already bumped this session, not yet deployed). Re-verified in the preview, light + dark. **Original pass (superseded above):** lesson (all 13 anchors on `independent-samples-t-test` classified right: prose/callout/`li`/FAQ/injected APA-formatter link styled; TOC, prev/next, sidebar, nav quiet), `datasets.html` (the prompt's example paragraph now obviously clickable; `.ds-index` chips + `.dl-btn` downloads quiet), `toolbox.html` (callout + thesis-path styled, `a.tool-card` grid quiet), JASP guide (prose incl. the external JASP link + keep-going styled; hand-written TOC quiet; mock SPSS tables are anchor-free by audit), `cheat-assumptions.html` (0 underlined poster anchors — `.sec` chips untouched, poster lives outside `.lesson` and its print `!important` outranks the shared rule), homepage About (bio + a11y at `--link`; about-chips, personas, course cards, toolbox strip, resume link all quiet). **styles.css is precached → `CACHE_VERSION` bumped `sc-v5` → `sc-v6`.** Search index untouched (no rendered prose changed — the only HTML edit is a CSS comment inside `plan.html`'s print block). Convention documented in CLAUDE.md (interaction-feel list, item g).
- [x] P55 · front door — Toolbox nav prominence, un-collapse homepage toolbox, remove "Why it's different", repoint hero CTA (Extra Powerful · Max)
  - **17 Jul 2026 — all four shipped; audit 0 errors/21 warnings, prose-lint --strict still 0 sitewide, search index rebuilt (95 lessons, 24 pages), verified in-browser (light + dark + mobile; nav on a lesson AND a tool page), zero console errors.** **(1) Nav prominence** — the Statistics Toolbox item now wears a **tinted pill**: 🧰 (aria-hidden, wrapped with the label in `.ndb-label` so the mobile `space-between` keeps two flex children) + weight 700 + `--primary`-tint background (7%)/border (30%), fully rounded; hover deepens the tint (14%/55%), and `.active` on a tool page keeps the usual primary text and adds a 60% border — all three states verified distinct. Hover/focus dropdown intact (focus-within opened the 24-item panel with the rotated chevron + focus ring), click still lands on `toolbox.html`, and the mobile menu renders it as a single highlighted full-width link row (the 860px block re-squares the radius; dropdown stays suppressed). **(2) Homepage un-collapsed** — `renderToolbox()` now renders the **full 24-card toolbox grouped** under the same four headings as `toolbox.html`, as `.tbx-head`/`.tbx-desc` rows spanning the grid exactly like the curriculum's track heads (new CSS mirrors that visual language); the group titles/blurbs moved into `site.js` as **`TOOLBOX_GROUPS`** (`window.TOOLBOX_GROUPS`, which toolbox.html's grid now reads — one source for the wording), and the now-consumerless `star: true` flags, the dashed "All N tools →" card, and its `.tool-card-all` CSS were removed. A new TOOLBOX entry now appears on the homepage automatically. Section-head copy rewritten to match ("Choosers, calculators, cheat sheets, datasets, flashcards, guides, and the quiz…"). **(3) "Why it's different" deleted** — the whole `.features` section is gone from `index.html` (shared `.feature` CSS left in place per the prompt); homepage section rhythm improves to alt → plain → alt. **(4) Hero CTA repointed** — "Start with Stats 1" → **"See statistics in motion" → `stats-1/central-limit-theorem/`** (the draw-100 CLT build is the wow demo); the "Begin with 1.1" persona card deliberately kept as the start-from-the-beginning path, secondary `plan.html` CTA untouched. Guards held: `data-count` spans still fill 9/95/Nine, OG/JSON-LD counts untouched (audit checks 6/7 pass). **site.js + styles.css are precached → `CACHE_VERSION` bumped `sc-v6` → `sc-v7`.** CLAUDE.md's nav/homepage-toolbox/guides/hero passages updated. (Verified on a temporary port-8099 launch config — both standard ports were held by other sessions — removed after verification, P48-style.)
- [ ] ~~P56 · "Capybara says" replaces "Why it matters" (site-wide, 95 lessons)~~ — **DROPPED 17 Jul 2026** (Hakan reconsidered; the "Why it matters" callouts stay as they are — do not run)
- [x] P57 · new lesson: Psychometric Functions & the PSE (Temporal Bisection worked example) (Extra Powerful · Ultra)
  - **17 Jul 2026 — shipped as Stats 4 §4.11 (appended, no eyebrow renumbering); audit 0 errors/21 warnings, prose-lint clean (6 em-dashes/1k ≤ budget, no pattern hits), verified in-browser (light + dark + mobile + embed), zero console errors.** New lesson `stats-4/psychometric-functions/` frames any 2-choice task (proportion of "long"/"yes" vs stimulus level) with **temporal bisection** as the worked example: fit logistic / cumulative-Gaussian / Weibull by MLE, read off **PSE** (50% point) and **JND** (half the 25–75% span), and dial a manipulation that either **shifts the PSE** or **changes the slope**, shown as a two-curve baseline-vs-manipulated compare. **Interactive** (`pf-canvas`, frozen-noise: one uniform draw per condition×level×trial-slot, sliders re-analyze the same trials, "New trials" reseeds): a 2-param Nelder–Mead maximizes the binomial likelihood live; readouts for both conditions' PSE/JND/slope + ΔPSE; family selector + compare toggle + trials/σ/shift/×noise sliders. **Math independently verified via `node -e`:** all three curve equations are textbook-exact (F(PSE)=.5000, F at ±JND = .2500/.7500, slopes exact); PSE/JND recovery from a known Gaussian observer (μ=500, σ=80) is exact for the matched family (PSE 500.0, JND 54.0 = σ·0.6745); the "logistic fit to Gaussian data reports a JND ~10% smaller" claim confirmed (48.4 vs 54.0 = 10.4%). **One prose correction the verification caught + fixed:** the family-choice callout (and matching FAQ) originally said fitting *all three* families moves the PSE "by a millisecond or two" — true for the symmetric logistic/Gaussian (~1 ms), but the **asymmetric Weibull sits a robust ~8 ms off** (the genuine global MLE, stable across 400 restarts and the whole μ range, and visible in the interactive itself: Weibull PSE 528 vs 520 for the other two). Reworded both to distinguish the symmetric pair from the Weibull (whose offset is *because* it's a zero-anchored detection function fit awkwardly to symmetric bisection data) — a more accurate and pedagogically richer point, and now prose and interactive agree. **Round-trips all pass** (slider away-and-back, family logistic→weibull→gauss→logistic, compare on/off — every readout byte-returns). **Standard integration** done: curriculum.js (ready, appended → Stats 4 now 11), 3 checks, 3 FAQs (+inject-faqs), a QUIPS line, **3 glossary terms** (PSE, JND, psychometric function), a `c:"stats-4"` quiz question, R+Python snippet (logistic-GLM PSE/JND + psignifit/quickpsy pointer), a SPSS/JASP+APA `software.js` entry (binary logistic → PSE=−b₀/b₁, JND=ln3/b₁; condition×duration interaction = PSE-shift vs slope-change), sitemap, homepage ItemList (Stats 4 → 11) + static og/twitter counts (95→96) + CLAUDE.md count. Cross-links logistic-regression and GLM both ways. Ran `make-og-images.py --retag-only` (og already correct) + rebuilt search-index (96 lessons, 24 pages). **curriculum.js + site.js are precached → `CACHE_VERSION` bumped `sc-v7` → `sc-v8`.**
- [x] P58 · new lesson: Signal Detection Theory (Stats 4 §4.12 — d′/criterion/ROC playground) (Extra Powerful · Ultra)
- [x] P59 · Stats 2.6 rebuild — per-test logic + tabbed playgrounds (Mann–Whitney, Wilcoxon, Kruskal–Wallis, Friedman) (Extra Powerful · Ultra)
- [x] P60 · front door v2 — track-based nav dropdowns, grouped Toolbox menu, resume into hero, personas + Curriculum/About tabs retired (Extra Powerful · Max, then /code-review ultra)
- [x] P61 · course landing pages — 9 × `<course>/index.html`, BASE "../", entry points + ItemList retargeted (Extra Powerful · Max)
- [x] P62 · quiz v2 — exam mode + bank 128 → 208 questions (≥2/lesson everywhere, Stats 1–2 weighted), `sc-exam` best scores, printable mock-exam report (Extra Powerful · Ultra)
- [x] P63 · worked problems — problems.html + Stats 1–2 sets (22 problems), print-without-solutions (Extra Powerful · Ultra)
- [x] P64 · worked problems — Stats 3–4 + toolkit "spot the problem" sets (43 problems total) (Extra Powerful · Ultra)
- [x] P65 · touch & small-screen ergonomics — touch-action sweep, finger-sized grab targets, 360px sweep (Powerful · Extra)
- [x] P66 · guard scripts — tools/math-check.js gate + tools/extlinks.js for P38 (Powerful · Extra)
  - **21 Jul 2026 — both shipped; the site's math passed clean on the first run.** `tools/math-check.js`: **218 assertions, 0 failures, 0.84 s** (zero-dependency; `viz.js` loaded in a `vm` under a `window`/`document` shim). Every assertion cites its published source and takes that source's rounding precision as its tolerance. Coverage: normal family + symmetry identities; `tInv`/`chiSqInv`/`fInv` vs printed table criticals (t.975,10 = 2.2281, χ².95,3 = 7.8147, F.95,3,20 = 3.0984, …) plus cross-distribution identities (χ²α,1 = z².975, F.95,1,20 = t².975,20, the F reciprocal rule); the statcheck case t = 2.05, df = 28 → .0498 plus **252 quantile↔CDF round-trips** over a 7×6 α/df grid; `gammaln`/`gammp`/`betai` identities exercising *both* `gammp` branches and *both* `betacf` branches; density integrals ≈ 1 and pdf-vs-CDF derivative checks; all four **G*Power anchors reproduced exactly** (64/group · 84 · 159 · 88) plus ncp = 0 collapse-to-central; and the documented tool-page constants. Two method notes worth keeping: the χ²(1) and F(1,·) integrals need the **x = u² substitution** (plain Simpson can't handle their x^−½ singularity — this was a bug in the *test*, caught during the build), and `erf(0)` lands ~1e-9 off zero because A&S 7.1.26 is a rational approximation, so its tolerance is the formula's own 1.5e-7 bound. **Negative-tested four ways** (perturbed erf coefficient → 3 failures, gammaln Lanczos coefficient at 1.3e-5 relative → 27, `tUpper`'s 0.5 factor → 11, `invUpper` 200→12 bisection iterations → 143, dropping `nctCdf`'s `normCdf(−δ)` term → 3); every section is load-bearing, and `viz.js` was restored byte-identical after each. `tools/extlinks.js`: the site's whole external surface is **10 unique URLs** — **7 ok** (GA tag, JASP, RUG profile, schema.org, both Scholar variants, the ko-fi button image), **2 bot-blocked-known-fine** (ko-fi 403, LinkedIn 999), **0 broken**. The `ALLOW` table is status-specific so a genuine 404 can't hide behind a familiar failure (negative-tested: flipping LinkedIn's allowed status to 200 reclassified its real 999 as BROKEN with an explanatory note); planted dead links (bad host + a 404 service) were both flagged with correct file attribution and `--strict` exited 1. One false positive fixed during the build: `xmlns="http://www.w3.org/2000/svg"` in `offline.html` is a namespace identifier, not a link (w3.org 403s this script), now excluded by context rather than by a hardcoded namespace list. No site files changed — no `CACHE_VERSION` bump needed. **Noted, not actioned:** a stale git worktree `.claude/worktrees/cool-shaw-e6dba5` is present again (P38's July run removed a different one).
- [x] P67 · lesson state presets — SC.preset + ~10 flagship lessons + teachers.html docs (Extra Powerful · Extra)
- [x] P68 · trust & polish — feedback link, PWA shortcuts, section share-links, optional view transitions (Powerful · Extra)
  - **21 Jul 2026 — three of four shipped; the optional fourth was built, could not be verified, and was deliberately dropped.** (1) **Feedback path:** `renderFooterFeedback()` puts a quiet "Spotted a mistake? Tell me" beside the About link in every footer — a `mailto:` whose subject carries the page's own path, read from `<link rel="canonical">` so a report names the clean public URL (`/stats-2/correlation/`) even when the page was served from a subpath. Verified on **119 of 120 pages**; the one without it is `404.html`, which is self-contained by design. (2) **PWA shortcuts:** three in `site.webmanifest` — Continue learning → `progress.html`, Statistics Toolbox → `toolbox.html`, Course quiz → `quiz.html` — with relative `url`s, verified under the `subpath` server to resolve against the manifest's own URL and stay inside `scope`. (3) **Section share-links:** a copy-link button on every `h2` of every lesson and guide — **473 buttons across 101 pages**, swept at **360px** with **0 layout shift, 0 horizontal overflow, 0 errors**. `ensureH2Ids()` was extracted from `renderTOC()` so the TOC and the share-links share one id scheme; its dedupe now checks every id on the page, and a pre-flight scan of all 111 lesson/guide files found **0 slug collisions**, so no existing anchor moved. Two things the verification actually caught and changed: the sweep found **3 headings** (psychometric-functions, sampling-methods ×2) where an in-flow button wrapped to a line of its own in a narrow column, fixed by switching below 700px to an absolutely positioned button in a permanently reserved right gutter — still zero shift, and the re-sweep came back clean; and focus reveal was moved from `:focus-visible` to **`:focus`**, because a keyboard user could otherwise land on an invisible control. Confirmation is a green-check icon swap in a fixed box rather than a "Copied!" label (which would widen the button and shove the heading), so `flashCopied()` gained a `data-copy-flash` visually-hidden live region for the wording — the existing text copy buttons were regression-checked and behave byte-identically. Hidden in `@media print` (verified by the P30 CSSOM flip). (4) **View transitions — DROPPED, and the reason is not "it broke".** `@media not (prefers-reduced-motion: reduce) { @view-transition { navigation: auto } }` was written and parsed correctly (confirmed in the CSSOM, and the browser supports the feature), but **no transition ever ran**: the preview pane serves its document with `visibilityState === "hidden"`, and Chrome skips view transitions for hidden documents. A top-level opt-in behaved the same, which isolates it to the environment rather than the media nesting. So the two risks the prompt named — the SW's `controllerchange` auto-reload firing mid-transition, and a dark-theme flash — could not be observed at all, and shipping an unverifiable visual change to every page of the site is a bad trade for a cross-fade. The removed rule and the reasoning are left as a comment in `styles.css` where it would go. `CACHE_VERSION` **sc-v17 → sc-v18** (site.js, styles.css and site.webmanifest are all precached). Gates: `audit.js` PASS (0 errors), `math-check.js` PASS (218/218), `prose-lint --strict` exit 0.
- [x] Ad-hoc (not a numbered prompt) · curriculum reorder + glossary rebuild + homepage/CLT tweaks
  - **21 Jul 2026 — five requested changes, all verified in-browser.** (1) **Homepage:** the `.hero-eyebrow` ("Meet StatsCapybara, your chill guide to statistics", with its inline capybara SVG) was removed outright; the h1 now leads. (2) **ML & AI moved from The Research Toolkit into The Statistics Core, after Stats 4**, and **Ethics moved to last in the Toolkit.** Because both moves created number collisions (Core would have shown two §4.1s) the affected courses were **renumbered to match the new order — ML 4.x → 5.x, Writing 5.x → 3.x, Ethics 3.x → 4.x** — by a script that reads `curriculum.js` and rewrites each lesson's `Section N.n` eyebrow, so the HTML follows the single source of truth (28 files). The prev/next chain now runs Core 1.1 → 5.12, then Toolkit 1.1 → Ethics 4.8. Ripple fixes: the homepage **ItemList JSON-LD** reordered (positions 5–9) with ML's description retracked; `ml/index.html`'s eyebrow + Course JSON-LD retracked; the **9 prose cross-references** carrying old § numbers in `problems.html` and `cheat-apa.html`; `teachers.html`'s Toolkit sentence; both **`TRACKS` descs** (which named machine learning under the Toolkit); and **`assets/og-ml.png` regenerated** (its baked eyebrow read THE RESEARCH TOOLKIT — the other nine cards came back byte-identical). **ML's `educationalLevel` was raised `Intermediate` → `Advanced`** to match its new place closing the Core — 13 pages (12 lessons + the landing page) plus the homepage ItemList entry — and **`ml` was added to `CORE_LEVELS` in `audit.js`** so the value is *enforced* rather than merely set: `expectedLevel()` returns null for a core course **missing** from that map, which silently disables the check instead of failing it, so every future core course must be added there too (the constant's comment and the file header now both say so). Negative-tested — reverting one ML lesson to `Intermediate` produced exactly one audit error, restoring it went back to PASS — and the other eight courses' levels were confirmed untouched. (3) **Glossary rebuilt** — see the CLAUDE.md note: collapsed `<details>` per term + a sticky A–Z jump bar + Expand/Collapse all, with inert (not vanishing) letters, filter-implies-open, and a beforeprint/afterprint pair so the printed sheet still carries every definition. (4) **CLT playground defaults to Uniform.** (5) The reported "bug" — the dashed curve sitting left of centre on the Skewed population — **was not a bug**: that population is exponential with μ ≈ 16 on a 0–100 axis, and the bell is correctly centred on μ (Uniform and Bimodal both have μ = 50, which is why they look centred). Per the user's call the math was left alone and the existing faint reference line was **labelled `μ = <value>`** so the offset reads as intentional; the "what you should notice" callout now says so in words. `CACHE_VERSION` **sc-v18 → sc-v19** (styles.css, site.js and curriculum.js are precached). Gates: `audit.js` PASS (0 errors), `math-check.js` PASS (218/218), `prose-lint --strict` exit 0.
- [x] P69 · privacy & honest claims — privacy.html, GA posture, claims sweep, README rewrite (Extra Powerful · Extra)
  - **21 Jul 2026 — all five items shipped; the GA decision was made empirically, not by argument.** (1) **`privacy.html`** — a ~900-word plain-language page (0 em-dashes, 0 prose-lint hits): what is collected (one anonymous page view), the full `localStorage` inventory as a table (`sc-progress` / `sc-last` / `sc-checks` / `sc-exam` / `sc-cards` / `sc-name` / `theme`) with an honest note that device-local means a phone shows none of a laptop's progress and that clearing site data wipes it, the two external services a page contacts (Google Analytics, the Ko-fi button image) and what each can see, the never-list (no ads, accounts, selling, dark patterns), a children/classrooms note for instructors, and the footer feedback mailto as the contact route. **The "What is collected" section is deliberately written to absorb P70's event list in one sentence.** (2) **GA posture — Consent Mode SHIPPED, not just disclosed.** `gtag('consent', 'default', …)` with all four storage types `denied` was added before `gtag('js')` in all **134** GA-carrying pages (byte-identical block; `offline.html` has no GA by design). **Verified in-browser rather than assumed:** with cookies cleared, a full load writes **no `_ga` cookie at all**, and the `page_view` still reaches `/g/collect` with `gcs=G100`. The cost, stated plainly here and on the page: each pageload gets a fresh `cid` with nothing persisted, so **per-page view counts (what P37/P39 actually read) stay accurate while Users/Sessions become unreliable** — a good trade, and it also means no cookie banner is owed. (3) **Claims sweep** — `teachers.html`'s twitter:description ("no tracking") and `toolbox.html`'s progress bullet reworded to what is true; `progress.html`'s "no account and no server" and `teachers.html`'s "nothing is sent to a server" scoped to progress, each now linking the privacy page, and `teachers.html` gained an explicit paragraph naming the analytics so instructors read it before assigning. Final sweep returns **only** the planning docs describing the problem plus one unrelated "untracked choices" in the p-hacking lesson. (4) **Footer:** `renderFooterPrivacy()` / `.footer-privacy`, BASE-aware (verified resolving `../../privacy.html` → `/privacy.html` from lesson depth). (5) **README rewritten** — the old "51 lessons across 5 courses" is gone, and it now points at `curriculum.js` and the homepage for live counts rather than baking new numbers that will stale the same way. Registered as a root page everywhere *except* `TOOLBOX` (it is not a tool): audit `ROOT_PAGES` + a new **`NON_TOOL_ROOT`** set so its two-level Home → Privacy breadcrumb is named correctly in the error text, prose-lint `ROOT_PAGES`, `SEARCH_PAGES` (searching "privacy" finds it — confirmed live), `QUIPS`, `build-search-index.py` (36 pages), `sitemap.xml`. `CACHE_VERSION` **sc-v19 → sc-v20**. Gates: `audit.js` PASS (0 errors, 21 warnings = baseline), `prose-lint --strict` exit 0, zero console errors, verified light + dark and at 360px with no horizontal scroll.
- [x] P70 · GA4 interaction events for the maintenance loops (Powerful · Extra)
  - **21 Jul 2026 — ten events shipped, every one verified on the wire rather than in the console.** `window.SC.track(name, params)` in `site.js` is now **the only thing on this site that calls `gtag`**; the three tool pages that own an event call `SC.track` and guard on `window.SC && SC.track`. The set: **`search_used`** (overlay opened, `page_type`, never the query), **`viz_png_export`**, **`print_used`**, **`feedback_click`** (all `page_type`), **`lesson_embed_view`** (`with_preset`), **`pwa_installed`**, **`quiz_practice_finished`** + **`exam_finished`** (`scope`, `length`, `score_bucket`), **`problem_solution_opened`** (`course`), **`cert_downloaded`** (`course`). **Verification was network-level, not gtag-spy-level:** each event was read back out of the real `google-analytics.com/g/collect` request (`en=` plus its `ep.`/`epn.` params) via `performance.getEntriesByType("resource")`, because the preview's own network log is same-origin-only and GA batches multi-event hits into a POST body. Confirmed on the wire: `search_used`/`viz_png_export`/`print_used`/`pwa_installed` from a lesson, `feedback_click` (`ep.page_type=lesson`), `lesson_embed_view` flipping `with_preset` **false → true** between `?embed=1` and `?embed=1&n=40`, `quiz_practice_finished` (`scope=stats-1`, `length=10`), and `exam_finished` whose **`scope` string is byte-identical to the `sc-exam` key** (`stats-1+stats-2`) with the retry run correctly reporting `scope=retry`, `length=9`, and writing nothing to `sc-exam`. **Three suppressions were tested, not assumed:** `problems.html`'s "Print with solutions" force-opening all 43 solutions fired **zero** events (the `mode` guard) and reverted to the reader's own 3; reopening one solution fired once, not twice (the `seen` map); and a double `beforeprint` counted once while a genuine second print run counted again (`trackPrint`'s `active` flag, which exists because Safari drives print off the media query and others off `beforeprint`). `pageType()` resolves all five kinds — verified home/tool/lesson/guide/course by same-origin iframe sweep. **Adblock case: `window.gtag` deleted entirely, then every instrumented surface exercised** — lesson search/PNG/print/feedback/install, a full practice quiz (4/10 rendered), a full exam (1/10, `sc-exam` still written), problems open + both print modes, cert download (filename still produced). **Zero console errors, zero broken features.** `privacy.html` extended as P69 designed it to be: "one thing" → "two things", a five-bullet plain-language event list with the three **Never**s bolded (never the query, never the actual score, never the name on the certificate), plus a note that no identifier is attached beyond a page view's non-persisting one. Honesty sweep caught two claims P70 itself had just falsified and fixed both — the page's own lede + short-version callout, and `teachers.html`'s analytics paragraph (instructors are exactly who needs the full picture). Documented in CLAUDE.md as a table with the "ten is the ceiling, not a starting point" rule. `CACHE_VERSION` **sc-v20 → sc-v21**. Gates: `audit.js` PASS (0 errors, 21 warnings = baseline), `math-check.js` 218/218, `prose-lint --strict` exit 0, verified dark + 360px with no horizontal scroll.
- [x] P71 · search v2 — typo tolerance, glossary instant answers, zero-result states (Powerful · Extra)
  - **21 Jul 2026 — shipped, and the ranking fixes came from running the case list rather than from reasoning about it.** Three helpers do the forgiving matching: `norm()` (case/diacritics/hyphens), `squash()` (separator-free, so `chi-square` === `chisquare`), and `flexRe()` — a regex over the squashed query allowing one optional separator between characters. `flexRe` **replaced** the old `toLowerCase().indexOf()` full-text scan and is **faster than what it replaced** (~1–2 ms vs ~2–3 ms over the whole 620 KB corpus, because the old scan re-allocated every lesson's text per keystroke), so the responsiveness requirement was met by *removing* work and **no debounce was added**; measured end-to-end per-keystroke cost across the case list was **0.9–7 ms**. Typo tolerance is `within1()`, Damerau-Levenshtein ≤ 1 (transposition included — "teh" is the real typo), on title/term tokens only; full text stays exact-modulo-separators by design. **A first pass over the written case list found three genuine defects that were then fixed:** (1) "bonferoni" returned nothing at all, because the glossary headword rule only stripped a trailing parenthesis and nobody types "bonferroni correction" — added a one-word-query-vs-first-token match; (2) "corelation" put three cheat sheets above the Correlation lesson, because keyword hits were hard-capped at 50 and so a *fuzzy* keyword outranked a fuzzy title — replaced the cap with `aside()` scaling (×0.55), which keeps an exact keyword above a guessed title spelling while dropping a fuzzy keyword below it; (3) "what is power" returned zero, since "what" and "is" match no title — the title pass is now re-run on `askTerm()`'s stripped term when a question form comes back empty. After the fixes: "corelation" → Correlation & Regression Calculator + the Correlation lesson, "regresion" → Simple Linear Regression + Regression Diagnostics, "what is power" → 40 results led by the three power pages. **Glossary instant answers** lazy-load `glossary-data.js` beside the index (confirmed loading BASE-aware from lesson depth) and render a definition card above the results; its `glossary.html#<term>` deep link was verified to **land on the right `<details>` and open it** (`#power-statistical` → "Power (statistical)"). **Zero results give three states, all verified:** "Did you mean:" + up to 3 near titles (`boostrapp` → Bootstrap & Resampling; `corelasion` → the two Correlation pages), a glossary-only hit ("That term is defined above…" for `hetroscedasticity`, whose def card still carries the lesson link), and a true miss (`asdfghjkl`, `zzzqqq`) — each ending in quiet Browse-the-glossary/toolbox links. **Case list run in full: 5 typos, 3 spacing/hyphen variants, 3 glossary questions, 2 nonsense strings**, plus 6 distance-2 typos to exercise the did-you-mean branch that the original 13 never reached (their misses were genuinely far from any title). **Keyboard verified, not assumed:** the def card's links are in the Tab trap in DOM order (the trap now queries the whole panel, not just the list), arrows still drive the results, arrows drive the *suggestions* on a did-you-mean screen while the two browse links stay outside the selection (`wireRows()` only wires anchors below `searchMatches.length`), and Escape closes and restores focus to the search pill. `?q=` deep link verified from lesson depth with relative hrefs intact. Light + dark + **360 px with no horizontal scroll**, zero console errors. `CACHE_VERSION` **sc-v21 → sc-v22**. Gates: `audit.js` PASS (0 errors, 21 warnings = baseline), `math-check.js` 218/218, `prose-lint --strict` exit 0.
- [x] P72 · capstone guide — one dataset, the whole research journey (Extra Powerful · Ultra)
  - **21 Jul 2026 — shipped, and the independent verification agent earned its keep by catching three defects in the author's own audit trail.** `guides/complete-worked-project/` ("One Study, Start to Finish", ~3,050 words) carries **`memory-2x2.csv`** end to end across eight steps: research question → operationalization → 2×2 between-subjects design + power → data checks → assumptions → two-way ANOVA → simple effects with effect sizes and CIs → APA paragraph → limitations + the ethics review. **`sleep-experiment.csv` was deliberately avoided** (it stars in the JASP guide); the 2×2 was picked over the correlation and logistic files because it supports the richest honest arc, and no other page on the site quotes its numbers except `datasets.html`, whose worked solution the guide matches exactly. **Two imperfect moments, both load-bearing rather than decorative.** (1) *The underpowered wish:* the design needed **N = 128 (32/cell)** for a medium interaction and the file has **25/cell**, giving **70% power**, stated in the design section and again in the limitations. The guide also names the trap that produces most bad 2×2 proposals — plugging k = 4 into an ANOVA power calculator returns **N = 179**, which powers the *omnibus* test, not the interaction — and gives the fix as a memorable rule: a 2×2 interaction with effect *f* is powered like a **two-sample t with d = 2f** at the same total N, so `power.html` → Two-sample t → d = 0.50 → 64/group → **N = 128**. That instruction was verified **live on power.html itself**, which prints "you need 64 per group (128 total)", and the equivalence was checked numerically (agrees to <0.001 power units for N ≥ 40; the claim is scoped to that range in the prose because it breaks down below N ≈ 40). (2) *The outlier that isn't:* the 1.5×IQR rule flags **four points in one cell only** (recall 3, 15, 16, 17), purely because that cell's IQR is 2.0; their within-cell z-scores are −1.91 to 2.32 and the cell's SD matches its three neighbours. Deleting them would push the interaction from **F(1,96) = 8.27, p = .005, ηp² = .08** to **F(1,92) = 12.62, p < .001, ηp² = .12**, i.e. *toward* the hypothesis, which is exactly why they stay. **Verification was genuinely independent:** a separate agent recomputed every statistic from the CSV without reading the page's own verification comment, using two independent routes for the ANOVA. Every reader-facing number verified. It found **three errors confined to the HTML verification comment** — the encoding p-value written as `1.4e-21` when the true value is `1.7948e-20`, the deep simple-effect p written as `1.1e-6` when it is `8.4772e-7` (both had been transcribed as plausible approximations rather than computed values), and a residual excess kurtosis of `−0.1511` computed with a **hybrid estimator** that mixes an /n fourth moment with an /(n−1) variance (the G2 that pairs with the G1 skew on the same line is `−0.0356`). All three were fixed after reproducing them independently. The agent also surfaced a **latent library bug**: `VIZ.fUpper` underflows to exactly 0 for d1 = 1 and F ≳ 110 (`betai` cancellation), so the encoding p-value is unobtainable by the method the comment claimed; the comment now records the `2*tUpper(√F, df)` identity as the correct route, and the library fix is flagged as separate work since no rendered number depends on it. Two prose claims were tightened on the same evidence: the power-equivalence range, and an instruction to paste partial η² into `effect-sizes.html`, whose η² is a *total* η² (.03 here, not .08). Cross-page agreement confirmed against `datasets.html`, `descriptives.html`'s actual formulas (type-7 quantiles, G1 skew, t-based CI), `tables.html`, and `apa.html` — whose own statcheck `recomp` returns OK on all five rendered F/t triples. **Registrations: seven, not the six CLAUDE.md documented** — `tools/prose-lint.js` keeps its own `GUIDES` list mirroring the audit's, and a guide missing from it goes silently unlinted; CLAUDE.md now says so. Cross-links added to `plan.html`'s plan card (a fifth "See a full worked project" button, verified rendering and resolving), a new `teachers.html` section framing it as a semester-project template, all four existing guides' Keep going footers, and the Methods / Stats 2 / Writing landing pages. Browser-verified: zero console errors, nav dropdown entry BASE-aware from lesson depth, `?q=` search returns it as the top hit, 9 section share-links, both mock tables scrolling inside themselves with **0 page-level horizontal overflow at 360px** (measured against an existing guide as a control), light + dark. `CACHE_VERSION` **sc-v22 → sc-v23**. Gates: `audit.js` PASS (0 errors, 21 warnings = baseline), `math-check.js` 218/218, `prose-lint --strict` exit 0 (7 em-dashes against a budget of 10, no pattern hits).
